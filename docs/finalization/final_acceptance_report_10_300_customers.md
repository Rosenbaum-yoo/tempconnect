# Final Acceptance Report — 10-300 Customers (Phase 5)

> Skelett, wird über die Phasen aufgebaut, final nach Phase R. Stand: 2026-05-31 (Phase A initialisiert).

## Geprüfte Bereiche
- Phase A: Reifeprüfung abgeschlossen → siehe 10_300_customer_readiness_matrix.md
- Phase J / Block 1: Theme-System (Bestand dark/light verifiziert, ultra_premium ergänzt)
- Phase B / Slice 1: Commercial-Spine geprüft — `/customer-requests/*` + `/subscription-requests/*` (≈Phase C) bestehen; organizations.customer_stage/plan + subscription_requests = dekomponiertes Lifecycle. Lücke = Customer-Operations-Roster (ergänzt).
- Phase C: Commercial Desk verifiziert (read-only, KEIN Code) — Kundenseite (subscriptionRequests.js: upgrade/downgrade+preview/cancellation/mine/history/detail) + Staffseite (staffControlCenter.js: list/detail/transition/approve/reject/activate/offer/documents, alle mit Step-up+Confirm+Reason+Audit). Spec erfüllt, keine Lücke. Nächster Code = Phase D (Billing, Owner-Gate für Provider-Architektur).
- Phase D / Slice 1: BillingProviderService gebaut (Provider-Abstraktion stripe/manual/disabled, manual-first). Owner-Entscheidung „zukunftssicher, wenig Skalierungsaufwand". Additiv + preserve-first auf den Money-Path (Webhook seiteneffekt-identisch). Backwards-Compat verifiziert.
- Phase D / Slice 2: read-only SCC-Billing-Sicht gebaut (staffBillingOverviewService + /billing/overview|/billing/meta + SCC-Modul `billing`). Macht Provider-Status (describeBilling) + plattformweite Rechnungs-Summen je Status + letzte Rechnungen für Operator sichtbar. Keine Migration, keine Mutation, Zero-State. Isolierter Billing-Diff.
- Phase D / Slice 3: Enterprise-Dunning-Observability gebaut. Operator-getriebene Inkasso-Worklist (überfällige Forderungen, älteste zuerst, days_overdue-Severity) im bestehenden /billing/overview-Aggregat + `payment_failed`-Webhook-Naht (logger.warn, Observability). KEIN Auto-Cancel, KEINE Kunden-Mail, KEINE Status-Mutation — kommerziell korrekt für direkt zahlende Enterprise-Kunden (manual/Net-14 Default). Keine Migration. Sichtbarkeits-Hälfte von R1-Dunning geschlossen.
- Phase E: Email-Provider-Abstraktion gebaut (`emailProviderService` resolve/describe, console/smtp/sendgrid/disabled). Wiederverwendung des bewährten Billing-Provider-Musters (resolve/describe, ehrliche Capabilities, sichere Default-Kein-Konfig-Stellung). Wirtschaftlicher Kernpunkt: SendGrid/Mailgun/SES/Brevo laufen als SMTP-Relay über den bestehenden `nodemailer` → KEINE neue Dependency (`@sendgrid/mail` unnötig, schließt R3-Lücke ohne Paketzuwachs). Default `console` = nur Logging, byte-identisch zum bisherigen Dev-Verhalten (rückwärtskompatibel). Echte Keys = Owner/Gate-50.
- Phase I / Slice 1: Provider-Readiness (Mail+Billing) in die bestehende `getSystemDiagnostics` (hinter `/admin/system-health`, requireAuth+requireAdmin) integriert — schließt die Phase-I-Lücke „DB/Redis/Mail/Billing-Health". Nutzt describeBilling/describeEmail (Phase D/E) wieder, kein neuer IO-Pfad. Spec-konforme Semantik: nicht konfigurierter optionaler Provider = `unconfigured` (NICHT „failed", zieht Gesamtstatus nicht); nur echte Fehlkonfiguration (externer Provider gewählt aber nicht lieferfähig) = `degraded`. Öffentlicher `/service-status` bewusst unangetastet (preserve-first). Keine Secrets in der Antwort.
- Phase I / Slice 2: Live-Service-Health für den Staff-Operator sichtbar gemacht — `loadOperationsSnapshot` (hinter `/staff/api/operations`, requireStaff) reichert sich additiv um `service_health = getSystemDiagnostics(pool)` an; SCC-Modul `operations` zeigt jetzt einen „Live-Service-Health"-Abschnitt (db/redis/api/process/billing/email mit Status + Detail + secret-freien Provider-Warnungen) zusätzlich zu Infra-Health + Runbook-Log. Reuse statt Duplikat (ein Round-Trip), Soft-Fail (Diagnostics-Fehler kippt den Snapshot nie), keine Migration/Mutation, kein RBAC-/Surface-Change.
- Phase Q / Audit Slice 1 (read-only, KEIN Code): Skalierungs-Posture der in dieser Welle ergänzten plattformweiten Read-Aggregate (Phase B Roster, Phase D Billing-Totals/Attention) gegen den bestehenden Index-Layer + Pagination geprüft. **Befund: bereits abgedeckt, keine neue Migration nötig** (preserve-first, „keine spekulativen Features"). (a) **Indizes** — die Dunning-/Attention-Query (`status='overdue' OR (status='issued' AND due_at<NOW())` ORDER BY due_at) trifft exakt den partiellen `inv_overdue_idx ON invoices(due_at,status) WHERE status IN ('issued','overdue')` (Mig 032); Status/Org/User-Pfade über `invoices_status_due_idx`/`invoices_org_status_idx`/`invoices_user_status_idx` (Mig 030); Audit-Pfade über `audit_actor_time_idx`/`audit_entity_time_idx`, Roster-Joins über `om_org_role_idx`/`om_user_org_active_idx` (Mig 032); `organizations` = ≤ Kundenzahl Zeilen (bei 300 trivialer Seq-Scan, Index wäre kontraproduktiv); die Totals-`GROUP BY status` ist ein inhärenter Voll-Aggregat (kein Index hilft, ≤ #Status Ergebniszeilen). (b) **Pagination/Bounding** — Attention `LIMIT $1` (clamp ≤ 50), `listInvoices` `LIMIT 50` Default, Roster `clampLimit(max 200)`+`clampOffset`+`LIMIT/OFFSET`, Detail-Subqueries `LIMIT 1`/`LIMIT 20`; keine unbounded plattformweite Liste in den neuen Pfaden. Ergebnis: die Read-Aggregate dieser Welle halten bei 300 Kunden ohne Zusatzcode.

## Umgesetzte Änderungen
- Phase J / Block 1 (Design-first, Owner-Vorgabe): `ultra_premium`-Theme ("Obsidian Sapphire") als opt-in ergänzt. Default bleibt `dark`. Theme-Registry (dark/light/ultra_premium) in theme.js mit Flag-Gating (`window.__TC_THEME_FLAGS__`), `cycle()`/`list()`; `toggle()` rückwärtskompatibel (dark↔light). Reiner Design-Diff, kein Misch-Diff über Billing/SCC/Auth.
- Phase B / Slice 1 (Customer Operations Backend): read-only Kundenroster (`staffCustomerOperationsService`) + SCC-Endpunkte `/customers`, `/customers-meta`, `/customers/:orgId`. Aggregiert bestehende Wahrheiten (KEINE Migration). Zero-State garantiert, Scope+generated_at, konservatives Risiko-Signal aus realen Spalten.
- Phase B / Slice 2 (Customer Operations Frontend): SCC-React-Modul `customer-operations` (Master-Detail nach `scc-inbox`-Muster) über die Slice-1-Endpunkte. Filter (Stage/Plan/Risiko/Suche), Pagination + Datenstand, Detail mit KPI-Grid und Drilldown in „Abo / Tarif-Anfragen" (dort die auditierten Mutationen). Reiner SCC-Frontend-Diff, keine Mutation, Default-UI unverändert.
- Phase D / Slice 1 (Billing Provider Abstraction): `billingProviderService` (resolve/describe/mapStripeEvent) — rein funktional, manual-first, ohne externe Keys baubar. `/payment/config` additiv um `billing`-Block (Provider+Capabilities+Warnings) erweitert; Stripe-Webhook dispatcht jetzt über normalisierte Event-Intents (activation/cancellation/payment_failed/ignored) bei byte-identischen Seiteneffekten. Reiner Billing-Diff. Echte Stripe-Keys = Owner/R2.
- Phase D / Slice 2 (SCC Billing View): `staffBillingOverviewService.getBillingOverview` aggregiert describeBilling + plattformweite invoices-Summen je Status (eine Aggregat-Query) + letzte Rechnungen (invoiceService.listInvoices, global). SCC-Endpunkte `/billing/overview` + `/billing/meta` (requireStaff, read-only). SCC-Modul `billing` (Single-Pane: Provider-Karte mit Capability-Pills + ehrlichen Warnungen, Totals-KPI-Grid, Summen-je-Status, Letzte-Rechnungen mit Status-Filter). Totals immer global; Filter verengt nur die Recent-Liste. Zero-State garantiert, keine Migration/Mutation, kein neues CSS.
- Phase D / Slice 3 (Enterprise Dunning Observability): `staffBillingOverviewService` um `loadAttention` (überfällige Forderungen: status='overdue' ODER 'issued'+über due_at = Cron-Lag-sicher, älteste zuerst, berechnetes days_overdue) + `attention[]`/`scope.attention_limit=50` im selben /billing/overview-Aggregat (3. parallele Query, Totals/Recent unverändert). `payment.js`-Webhook: `payment_failed`-Zweig = `logger.warn` (Observability) ohne Seiteneffekt (KEIN Auto-Cancel/Mail/Mutation; returnt weiter received:true). SCC-Modul `billing`: Abschnitt „Überfällig · Inkasso-Worklist" (Severity >14 Tage danger, Zero-State, Limit-Hinweis, `dunningTone()`-Helper). Operator-getriebenes Dunning (manual/Net-14 Default) — kein neues CSS, keine Migration. Reiner Billing-Diff.
- Phase E (Email Provider Abstraction): `emailProviderService` (resolveEmailProvider/describeEmail/EMAIL_PROVIDERS) — rein funktional, kein DB/IO, ohne externe Keys baubar. Ableitung sendgrid (wenn Key) > smtp (wenn Host) > console; explizite `EMAIL_PROVIDER`-Env gewinnt; Capabilities immer ehrlich (Provider ohne Key erzwungen → capability false + Warnung). `emailService` provider-fähig gemacht (`buildTransportOpts`: SendGrid → smtp.sendgrid.net:587/user=apikey/pass=SENDGRID_API_KEY über bestehenden nodemailer; SMTP unverändert; console/disabled = Log-only) + `describe()`-Export. `sendMail`/`verifyConnection`-Signaturen unverändert, Default-Pfad (kein SMTP_HOST) byte-identisch. Service entkoppelt (eigene Placeholder-Konstante, kein Cross-Import zu Billing). Reiner Email-Backend-Diff.
- Phase I / Slice 1 (Provider Readiness in System Health): `healthService.js` um `countTrue` + `billingHealth`/`emailHealth`-Mapper + Integrations-Block in `getSystemDiagnostics(pool, opts={})` erweitert (additiv, nach api-Metriken, vor Overall-Rollup, in try/catch). `components.billing`/`components.email` exponieren Provider-Name/Quelle/Booleans (automated/outbound)/Capability-Zähler + secret-freie Warntexte — **keine Schlüsselwerte**. safe-default-Provider (manual/console/disabled) sind immer `unconfigured` (auch mit Dev-Hinweis); `degraded` nur wenn externer Transport gewählt aber nicht lieferfähig; `unconfigured` zieht den Gesamtstatus nicht (neutral wie redis). `opts.config`-Injektion macht den Pfad ohne Ambient-Env testbar. Reiner admin-scoped Health-Diff, keine Migration/Mutation, kein Public-Surface-Change.
- Phase I / Slice 2 (Live-Service-Health im SCC-Operations-Modul): `staffControlService.loadOperationsSnapshot(pool)` reichert den Snapshot additiv um `service_health = getSystemDiagnostics(pool)` an (Import `healthService`; bewusst als LETZTE pool-Berührung → bestehende Pool-Sequenz-Tests bleiben index-gültig; Soft-Fail → `errors[]`, kein Snapshot-Kippen). SCC-Modul `operations` rendert einen neuen „Live-Service-Health"-Abschnitt (Karten db/redis/api/process/billing/email mit Status-Pill + `componentDetail` + secret-freien Provider-Warnungen) über bestehende `scc-*`-Klassen, kein neues CSS, keine Emojis. Reuse von Slice 1 statt Duplikat (ein Round-Trip via requireStaff), keine Migration/Mutation, kein RBAC-/Surface-Change.
- Phase E/F (SCC Mail Center, read-only): `staffMailCenterService.getMailOverview(pool, opts)` aggregiert `describeEmail(config)` (Provider-Status) + plattformweite Zustell-Summen je `mail_status` (inkl. NULL→`none` = nur in-app) + Kanal-Verteilung je `dispatched_via` + letzte Notifications — alles aus `subscription_notification_log` (Migration 103) über drei parallele Read-Queries (Index `…_created_idx` für die Recent-Liste). Empfänger werden PII-maskiert (`maskEmail` → `a***@domain`), `mail_error` auf 240 Zeichen begrenzt. SCC-Endpunkte `/mail/overview` + `/mail/meta` (requireStaff, read-only). SCC-Modul `mail` (Provider-Karte mit Capability-Pills/Warnings, Zustell-KPI-Grid, Summen-je-Status, Zustellkanäle, letzte Notifications mit Status-Filter). Zero-State garantiert, Status-Sanitisierung + Limit-Clamp (≤100). Entkoppelt (importiert `describeEmail` direkt aus der Provider-Abstraktion, nicht den Transport). Keine Migration/Mutation/Versand, kein neues CSS. Reiner SCC-Mail-Diff.

## Neue Dateien
- docs/finalization/* (5 Arbeitsdateien), .claude/learning/* (Lernschleife)
- api/test/themeRegistry.test.js (Phase J Block 1)
- api/services/staffCustomerOperationsService.js + api/test/staffCustomerOperations.test.js (Phase B Slice 1)
- frontend/src/staff/modules/customer-operations/index.tsx (Phase B Slice 2)
- api/services/billingProviderService.js + api/test/billingProviderService.test.js (Phase D Slice 1)
- api/services/staffBillingOverviewService.js + api/test/staffBillingOverview.test.js + frontend/src/staff/modules/billing/index.tsx (Phase D Slice 2)
- api/services/emailProviderService.js + api/test/emailProviderService.test.js (Phase E)
- api/services/staffMailCenterService.js + api/test/staffMailCenter.test.js + frontend/src/staff/modules/mail/index.tsx (Phase E/F Mail Center)

## Geänderte Dateien
- → changed_files_index.md (Phase J Block 1: design-system.css, theme.js, THEME-SYSTEM.md)
- Phase B Slice 2: frontend/src/staff/components/shell/{Sidebar,AppShell}.tsx (Nav-Eintrag + Route für customer-operations) + api/routes/staffControlCenter.js (Slice 1, /customers*)
- Phase D Slice 1: api/config/index.js (BILLING_PROVIDER-Env) + api/routes/payment.js (/payment/config +billing-Block, Webhook-Dispatch via mapStripeEvent — seiteneffekt-identisch)
- Phase D Slice 2: api/routes/staffControlCenter.js (/billing/overview + /billing/meta) + frontend/src/staff/components/shell/{Sidebar,AppShell}.tsx (Nav-Eintrag + Route „Billing", Gruppe Strategie)
- Phase D Slice 3: api/services/staffBillingOverviewService.js (loadAttention/mapAttention/ATTENTION_LIMIT + attention[] im Aggregat) + api/routes/payment.js (payment_failed-Webhook-Zweig, logger.warn, behavior-preserving) + frontend/src/staff/modules/billing/index.tsx (Inkasso-Worklist-Abschnitt) + api/test/{staffBillingOverview,payment.route}.test.js (Attention-/payment_failed-Assertions)
- Phase E: api/config/index.js (EMAIL_PROVIDER + SENDGRID_API_KEY-Env, leer=Ableitung) + api/services/emailService.js (provider-fähiger Transport via buildTransportOpts + describe()-Export, sendMail/verifyConnection unverändert) + .env.example (Provider-Auswahl-Block im E-Mail-Abschnitt)
- Phase I Slice 1: api/services/healthService.js (Imports config + describeBilling/describeEmail; countTrue/billingHealth/emailHealth; getSystemDiagnostics(pool, opts={}) +Integrations-Block) + api/test/systemHealth.test.js (neuer Block „Provider-Readiness billing/email", 5 Tests)
- Phase I Slice 2: api/services/staffControlService.js (Import getSystemDiagnostics; `service_health`-Feld + Befüllung als letzte pool-Berührung mit Soft-Fail→errors[]) + frontend/src/staff/modules/operations/index.tsx (HealthComponent/ServiceHealth-Typen + „Live-Service-Health"-Abschnitt: HEALTH_ORDER/HEALTH_LABELS/healthTone/healthLabel/componentDetail, kein neues CSS) + api/test/staffControlService.test.js (+2 Tests im loadOperationsSnapshot-Block)
- Phase E/F Mail Center: api/routes/staffControlCenter.js (Import staffMail + GET /mail/overview + /mail/meta, requireStaff) + frontend/src/staff/components/shell/{Sidebar,AppShell}.tsx (AreaKey `mail` + AREAS-Eintrag „Mail & Notifications", Gruppe Operations + lazy-Import/ActiveModule-Branch)

## Migrationen
- (keine — Phase D Slice 1 ist reine Service-/Route-Abstraktion ohne Schema-Änderung)

## Env-Variablen (neu)
- `BILLING_PROVIDER` (Phase D Slice 1): stripe|manual|disabled. Leer = Ableitung aus PAYMENT_MODE + Stripe-Key-Präsenz (rückwärtskompatibel). Default-Verhalten ohne Keys = manual (Rechnung/Vertrag, DE-B2B).
- `EMAIL_PROVIDER` (Phase E): smtp|sendgrid|console|disabled. Leer = Ableitung (sendgrid wenn SENDGRID_API_KEY, sonst smtp wenn SMTP_HOST, sonst console). Default ohne Konfig = console (nur Logging, byte-identisch zum Dev-Verhalten).
- `SENDGRID_API_KEY` (Phase E): SendGrid-API-Key (`SG.…`). Wird als SMTP-Relay über bestehenden nodemailer genutzt — keine neue Dependency. Leer = SendGrid-Capability false + Warnung wenn explizit erzwungen.
- (weiter folgt — Theme-Flags Phase J Block 2)

## Neue SCC-Module
- `customer-operations` (Phase B Slice 2): read-only Kundenroster (Master-Detail, Filter/Suche/Pagination, Drilldown ins Abo-Modul) — Gruppe Arbeitsplatz.
- `billing` (Phase D Slice 2): read-only Billing-Sicht (Provider-Status + Capabilities/Warnings, Totals-KPI, Summen je Status, letzte Rechnungen mit Status-Filter) — Gruppe Strategie.
- `mail` (Phase E/F): read-only Mail & Notifications (Provider-Status + Capabilities/Warnings, Zustell-KPI, Summen je Mail-Status, Zustellkanäle, Verteilung je Event-Typ mit Fehlerquote, letzte Notifications PII-maskiert mit Status-Filter) — Gruppe Operations.

## Neue API-Routen
- GET /staff/api/customers (Roster, requireStaff, read-only) · GET /staff/api/customers-meta · GET /staff/api/customers/:orgId (Detail) — Phase B Slice 1
- GET /staff/api/billing/overview (Provider+Totals+Recent, requireStaff, read-only) · GET /staff/api/billing/meta (Statusliste) — Phase D Slice 2
- GET /staff/api/mail/overview (Provider+Zustell-/Kanal-Summen+Event-Typ-Breakdown+letzte Notifications, requireStaff, read-only) · GET /staff/api/mail/meta (Status-/Kanalliste) — Phase E/F

## Neue Tests
- api/test/themeRegistry.test.js — 9/9 grün (Default aktiv, Ultra Premium auswählbar, Fallback, cycle, Flag-Gating)
- api/test/staffCustomerOperations.test.js — 11/11 grün (Zero-State, Mapping, Filter-Bindings, Enum-Sanitisierung, Clamp, Detail-404/UUID, Risiko-Ableitung)
- api/test/billingProviderService.test.js — 26/26 grün (Provider-Auflösung+Präzedenz, Capability-Ehrlichkeit inkl. Degradierung, Event-Mapping activation/cancellation/payment_failed/ignored, Garbage-Safety)
- api/test/staffBillingOverview.test.js — 8/8 grün (Provider-Shape+Capability-Booleans, Zero-State Summen 0 + `attention:[]` + `scope.attention_limit=50`, Aggregation+Invoice-Mapping + Attention-Worklist-Mapping/days_overdue, Status-Sanitisierung+Limit-Clamp, Status-Forward in listInvoices-Params) [Slice 2+3]
- api/test/payment.route.test.js — 32/32 grün (Phase D Slice 3: +1 Test „invoice.payment_failed = received:true + logger.warn + keine DB-Berührung + keine Mail"; Backwards-Compat aller Slice-1-Webhook-/config-Pfade erhalten)
- api/test/emailProviderService.test.js — 21/21 grün (Phase E: Provider-Auflösung+Präzedenz sendgrid>smtp>console, explizit-gewinnt, Placeholder-Erkennung, Capability-Ehrlichkeit inkl. erzwungen-ohne-Key→Warnung, unbekannter Provider→Ableitung+Warnung, Garbage-Safety describeEmail(null)/resolveEmailProvider(undefined))
- api/test/systemHealth.test.js — 17/17 grün (Phase I Slice 1: +5 Provider-Readiness-Tests — no-config→billing manual/email console = unconfigured + kein „critical"; echte Keys→ok+automated/outbound; erzwungen-ohne-Key→degraded+Warnung+Gesamtstatus degraded; **Secret-Safety** serialisierte Komponenten ohne Key-Wert; Shape-Garantie billing+email immer da)
- api/test/staffControlService.test.js — 48/48 grün (Phase I Slice 2: +2 Tests im loadOperationsSnapshot-Block — `service_health` via getSystemDiagnostics befüllt mit db/billing/email-Komponenten; Soft-Fail bei DB-Fehler in Live-Diagnostics → `database = critical`, aber Snapshot kippt nicht (recent_runs/infra_health bleiben Zero-State))
- api/test/staffMailCenter.test.js — 10/10 grün (Phase E/F: meta exposes mail_statuses+dispatch_channels; Provider-Shape+Capability-Booleans; Zero-State Summen 0 + alle Status-/Kanal-Buckets präsent; Aggregation Status inkl. NULL→`none` + Kanäle + Recent-Mapping mit **PII-Maskierung** `a***@domain` (Local-Part nicht geleakt); null-recipient→null ohne Crash; Status-Sanitisierung→null + Limit-Clamp 100; valider Status-Passthrough + Default-Limit 25; Status als **gebundener Param** in parametrisiertem WHERE (kein Inlining); Garbage-Status→nur Limit-Param, kein WHERE)

## Bestandene Checks
- Phase J Block 1: `node --check theme.js` OK · ESLint 0 Warnings · CSS Brace-Balance 332/332 · themeRegistry.test.js 9/9
- Phase B Slice 1: `node --check` Service+Route OK · ESLint 0 Warnings (neue Dateien) · staffCustomerOperations.test.js 11/11
- Phase B Slice 2: `npm run build:scc` grün (tsc --noEmit + vite build, 61 Module) · TS6133 (ungenutzter Helper) bereinigt → 0 Errors
- Phase D Slice 1: `node --check` Service+Route OK · billingProviderService.test.js 26/26 · payment.route.test.js 31/31 (Backwards-Compat, /config-Felder + Webhook-Pfade unverändert) · paymentService+billingMetrics+wave09BillingLifecycle 47/47 · ESLint 0 Warnings (3 Dateien)
- Phase D Slice 2: `node --check` Service+Route OK · staffBillingOverview.test.js 8/8 · payment.route.test.js 31/31 (kein Regress) · ESLint meine 2 Backend-Dateien 0 Warnings (einzige Restwarnung = vorbestehende `flagRating`/M-07, nicht aus dieser Welle) · `npm run build:scc` grün (tsc --noEmit + vite, 62 Module)
- Phase D Slice 3: `node --check` Service+Route OK · staffBillingOverview.test.js 8/8 (mit Attention-Worklist) · payment.route.test.js 32/32 (+1 payment_failed, kein Regress) · ESLint 4 Dateien (Service/payment.js/2 Tests) 0 Warnings · `npm run build:scc` grün (62 Module)
- Phase E: `node --check` emailProviderService.js + emailService.js + config/index.js OK · emailProviderService.test.js 21/21 · subscriptionNotifications.test.js 43/43 (kein Regress — nutzt Mock-sendMail, Default-Pfad unberührt) · ESLint neue/geänderte Dateien 0 Warnings
- Phase I Slice 1: `node --check healthService.js` OK · systemHealth.test.js 17/17 · healthService+billingProviderService+emailProviderService 58/58 (kein Regress) · ESLint 2 Dateien (healthService.js + systemHealth.test.js) 0 Warnings
- Phase I Slice 2: `node --check staffControlService.js` OK · staffControlService.test.js 48/48 (war 46, +2 service_health; bestehende Pool-Sequenz-Tests index-stabil) · ESLint 2 Backend-Dateien (staffControlService.js + Test) 0 Warnings · `npm run build:scc` grün (tsc --noEmit + vite, kein TS6133)
- Phase E/F Mail Center: `node --check` Service+Route OK · staffMailCenter.test.js 10/10 · ESLint meine 2 Backend-Dateien (staffMailCenterService.js + Test) 0 Warnings (einzige Restwarnung = vorbestehende `flagRating`/M-07, nicht aus dieser Welle) · `npm run build:scc` grün (tsc --noEmit + vite, 63 Module = +1 Mail-Chunk, kein TS6133)
- (Gesamt-Suite folgt: cd api && npm run lint && npm run test:ci; cd frontend && npm run build:scc)

## Bekannte Vorbefunde (nicht aus dieser Welle)
- staffControlCenter.js Z.77: eslint-Warnung `flagRating` ungenutzt — stammt aus uncommitteter M-07-Arbeit (nicht in HEAD), nicht angefasst (Scope-Disziplin). Mit M-07 bereinigen.

## Fehlgeschlagene Checks
- (folgt)

## Offene Risiken
- → open_risks_and_blockers.md (R1-R9)

## Go-live-Blocker
- R1 Billing-Automatisierung (Phase D — Slice 1 Provider-Abstraktion + Slice 2 SCC-Sicht + Slice 3 Dunning-Observability/Inkasso-Worklist erledigt; **Sichtbarkeits-Hälfte geschlossen**; offen nur payment-status→Subscription-Lifecycle-Reaktivierung = Owner/Provider-abhängig), R2 Stripe-Keys (Owner), R9 Preise/Rechtstexte (Owner)

## Empfehlung pro Gate
- Für 10 Kunden: Phase D (Billing-Tiefe) + Stripe-Keys schließen; Monitoring-Basis vorhanden (Phase I Slice 1: Mail/Billing-Readiness in `/admin/system-health`; Slice 2: Live-Service-Health im SCC-Operations-Modul für den Staff-Operator), Support bestätigen.
- Für 50 Kunden: Phase E (Email-Provider) **Backend + Operator-Sicht erledigt** (console/smtp/sendgrid/disabled ohne neue Dependency + SCC Mail Center für Zustell-/Fehler-Beobachtbarkeit aus `subscription_notification_log`; echte Keys = Owner/Gate-50), Phase J (Theme), Incident-Modell (**erledigt**, siehe Nachtrag R6).
- Für 100 Kunden: Monitoring detailliert, DB-Index-Review (**erledigt**, Mig 122/123), Customer Health, AI-Ops mit Review.
- Für 300 Kunden: Skalierbare Listen (**erledigt** — Phase Q Slice 1–4 + Index-Review), Restore-Drill (**Drill-Fidelity gefixt**, realer Drill = Owner), Kostenindikatoren, Enterprise-Doku.

---

## Nachtrag 2026-06-03 — Phase J Block 2 / R6 Incident-Modell / Phase Q Slice 1–4 + Index-Review / Phase O+P / N+1-Sweeps

> Dieser Nachtrag synchronisiert den Report mit dem `finalization_worklog.md` (dort der vollständige Detailstand). Alle Slices: **inkrementell, preserve-first, KEIN Commit (Owner-Gate)**. Self-Verifikation dieser Session: `node --test` der neuen Tests + Boundary-Regression = **45/45 grün** (6 support-N+1 + 36 infra-snapshot + 3 support-occ-boundaries), node v24.

### Umgesetzt seit letztem Stand
- **Phase J Block 2 (Theme Tier-2, SCC-Scope):** THEME_SWITCHER_ENABLED + ULTRA_PREMIUM_THEME_ENABLED → /bootstrap → Topbar-Registry-Cycle + `ultra_premium` im Operator-Shell. Env in allen 3 `.env*.example` + `envValidator.js` (Boolean-Token-validiert, Default-AN). platform/worker_portal-Scope-Injektion + SCC-Theme-Control-Modul bleiben owner-gated. (R4/R5 aktualisiert.)
- **R6 Incident-Modell (Owner-freigegeben):** `sql/migrations/121_ops_incidents.sql` (operativer Incident-Lebenszyklus, eigene Domäne — NICHT mit support_cases vermischt) + `services/staffIncidentService.js` + SCC-Route in `staffControlCenter.js` + `test/staffIncidents.test.js`. Schließt die operative Incident-Hälfte von R6 (Ops-Alert-Notify-Verdrahtung über emailProviderService bleibt optionaler Folge-Slice).
- **Phase Q Slice 2 (unbounded list, Variante DB-Truncation):** `admin.js` `/admin/organizations` (war `LIMIT 200`-Volldump) auf das kanonische paginierte Muster der Schwester-Route `/admin/users` umgestellt (parseInteger-Clamp limit/offset + `COUNT(*)`-Total + `{items,total,limit,offset}`).
- **Phase Q Slice 3 (unbounded list, Variante „Seite-1-only"):** `admin.js` `/admin/requests` + `/admin/strategic-collaboration/requests` gaben `{items}` OHNE `total` zurück (stiller Paging-Defekt). Paralleler `COUNT(*)::int AS total` mit identischer WHERE-Klausel ergänzt → Frontend kann Pagination bauen. Rückwärtskompatibel.
- **Phase Q Slice 4 (unbounded list, Variante kein-LIMIT + N+1-Write→Bulk):** `productReleaseService` list*-Sweep auf no-LIMIT-Voll-Reads geprüft (editoriale Volumina = kein akuter Defekt, dokumentiert); `markAllSeenForUser` N+1-Write-Schleife → ein `INSERT … SELECT FROM UNNEST($2::uuid[]) … ON CONFLICT DO UPDATE` (Spaltentyp per Migration verifiziert).
- **Phase Q Index-Review (Mig 122 + 123):** Cron-Sweep-Index-Audit über internal.js (~20 Sweeps). Befund reifes Repo (Sweeps bereits zweckindiziert, Mig 009/011/013/030/076). Echte Lücken: `122_cron_sweep_indexes.sql` (5 btree-Partials für Status-Prädikate) + `123_analytics_events_time_brin.sql` (**BRIN** auf product_analytics_events.occurred_at — schont den heißen Event-Insert-Pfad, btree wäre Write-Amplification). Beide add-only, IF NOT EXISTS, Rollback-Block.
- **Phase O (Test-Sicherung der Index-Slices):** `test/integration/scaling-indexes.flow.test.js` — DB-gated (`skip: !hasDb`, CI-integration-Job nach Migrationen) introspektiert `pg_indexes.indexdef` je Index (Existenz + Zugriffsmethode btree/brin + Tabelle/Spalte + Partial-Prädikat) inkl. Anti-btree-Assert für den BRIN.
- **Phase P (DR-Drill-Fidelity, R8):** `scripts/restore-test.sh` pg_restore-Aufruf um `--single-transaction --exit-on-error` ergänzt → der Drill spiegelt jetzt die Flags des echten `restore.sh` (sonst meldet ein nur teilweise eingespielter Dump fälschlich „bestanden"). `bash -n` OK. PLUS `test/infrastructureSnapshotService.test.js` — pinnt die R8-Backup-Staleness-Severity-Schwellen (12/24/48h) + cpu/ram/disk/docker/tls + Input-Härtung (36/36).
- **N+1-Read-Sweep (Request-Pfad):** `routes/support.js` `GET /support/lookup/orgs` lud per_page bis 100 Orgs und feuerte dann eine recent-cases-SELECT PRO Org (bis zu 100 Round-Trips/Request). Fix: exportierte `loadRecentOpenCasesByOrg(pool, orgIds, agent)` — EINE windowed Query (`ANY($1::uuid[])` + `ROW_NUMBER() OVER PARTITION BY org`, `rn<=10`), behavior-preserving. Tests: DB-frei `test/support.recentCasesByOrg.test.js` (6/6, Anti-N+1: genau 1 Query) + DB-gated `test/integration/support-recent-cases.flow.test.js` (SQL-Gültigkeit gegen reales Schema). **Sweep danach über die GANZE Codebase** (routes/ + 38 services/): kein weiterer kundenskalierender Request-Pfad-Read-N+1 — alles bounded (`.slice(0,N)`/festes Enum), `ANY()`-gebatcht, Grep-False-Positive (Query nach JS-only-Loop) oder Kunden-Cardinality-Cron.
- **N+1-Write-Triage (geschlossen):** 3 agent-gemeldete Kandidaten am echten Code geprüft. notification-prefs (festes event_category-Enum) + requests/broadcast (Plan-Sendelimit-gekappt) = **Nicht-Defekte**. assignmentStaffingService.createStaffingCampaignInternal = echter N+1, aber input-skaliert + `withTransaction`+`RETURNING`-verwoben (inviteMap/invites[]/Per-Zeile-Audit) → **Owner-gated** (Mutations-/Money-Pfad-Umbau, nicht autonom). Achse als verifizierte Triage geschlossen.

### Neue Migrationen (Nachtrag)
- `121_ops_incidents.sql` (R6, operativer Incident-Lebenszyklus) · `122_cron_sweep_indexes.sql` (5 btree-Partials) · `123_analytics_events_time_brin.sql` (BRIN). Höchste Migration jetzt **123**.

### Neue/geänderte Tests (Nachtrag)
- `test/staffIncidents.test.js` (R6) · `test/support.recentCasesByOrg.test.js` (6/6) · `test/integration/support-recent-cases.flow.test.js` (DB-gated SQL-Validität) · `test/integration/scaling-indexes.flow.test.js` (DB-gated Index-Introspektion) · `test/infrastructureSnapshotService.test.js` (36/36) · admin.js-Routen-Tests (Pagination/Total) · productReleaseService-Test (Bulk statt N+1).

### Offene Punkte nach diesem Nachtrag (alle Owner-gated bzw. bewusst deferred)
- R2 (Stripe Keys/Price-IDs), R9 (finale Preise + Rechtstexte), R8-Rest (realer Restore-Drill auf echter Infra mit Owner), R1-Rest (payment-status→Subscription-Lifecycle-Reaktivierung, provider-abhängig), R6-Rest (Ops-Alert-Notify-Hook), R7-Hälfte-B (AI-Unsafe-Prompt-Classifier — erst bei AI-Ops-Verdrahtung), R10 (Feature-Flag-Single-Source-Konsolidierung), N+1-Write-#3 (Staffing-Batch). Plus: `scripts/backup-verify.sh` toter fail-Zweig (eigenes Mini-Ticket).
- **Ehrliche Gesamtbewertung:** Die autonom umsetzbare Phase-5-Finalisierungsarbeit (Härtung bestehenden Codes, Skalierungs-Sweeps, Beobachtbarkeit, Tests, Doku) ist abgeschlossen. Was „100% Go-Live" noch fehlt, ist **ausschließlich Owner-gated** (externe Keys, finale Preise, Rechtstexte, realer Infra-Drill) — nicht von Claude allein schließbar (siehe 99_GOLIVE_GATE Teil 4: „Owner bestätigt finalen Status, nicht Claude").
