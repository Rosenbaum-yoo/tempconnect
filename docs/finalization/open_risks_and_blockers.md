# Open Risks and Blockers — Phase 5

> Laufend gepflegt. Bei Stop-Regeln (00_RULES.md) hier eintragen statt nur im Chat.

| # | Blocker / Risiko | Auswirkung | Betroffene Kunden | Priorität | Lösungsvorschlag | Phase | Status |
|---|---|---|---|---|---|---|---|
| R1 | Voll-automatisches Stripe-Billing unvollständig (Subscriptions/Price-IDs, Stripe Invoicing Auto-Rechnung, SEPA, Dunning, Billing-Portal, idemp. invoice.paid/payment_failed) | Kein skalierbares Billing; manueller Aufwand wächst pro Kunde | alle zahlenden | P1 | BillingProviderService (stripe-Standard) + Webhook-Härtung; Owner setzt Keys/Price-IDs | D | teilweise: Slice 1 Provider-Abstraktion + Slice 2 SCC-Sicht + Slice 3 Dunning-Observability (Inkasso-Worklist + payment_failed-Naht) erledigt. **Sichtbarkeits-Hälfte geschlossen** (Operator sieht Provider-Status, Summen, überfällige Forderungen). Offen: payment-status→Lifecycle-Reaktivierung (Owner/Provider-abhängig) + echte Keys/Price-IDs (R2) |
| R2 | Stripe Keys/Price-IDs/SEPA/Tax/Billing-Portal fehlen (extern) | Auto-Billing nicht produktiv testbar → Gate 10 nicht grün | alle zahlenden | P1 (Owner) | Owner-Aufgabe (MANUAL_TASKS Phase D) | D | offen |
| R3 | Email: keine Provider-Abstraktion (console/smtp/sendgrid/disabled), kein @sendgrid/mail | Kein professioneller Versand/Bounce-Tracking; Gate 50 blockiert | alle | P2 | EmailProviderService; SMTP/console reicht für Gate 10 | E | **erledigt (Backend):** emailProviderService (resolve/describe, console/smtp/sendgrid/disabled) + emailService provider-fähig (SendGrid via SMTP-Relay, KEINE neue Dependency). 21/21 Tests. Default `console` = nur Logging. `describe()` in System-Health (`components.email`) + SCC-Operations-Sicht **erledigt** (Phase I Slice 1+2). Offen: echte Keys (Owner/Gate-50) |
| R4 | ~~Theme-System fehlt komplett~~ KORREKTUR: dark/light existierten bereits. Phase J **Block 1** ergänzt `ultra_premium` (auswählbar via Toggle-Cycle, Default unverändert, Flag-Gating, 9 Tests). **Offen (Block 2, Triage 2026-06-02):** Frontend-Consumer EXISTIERT bereits (`theme.js` liest `window.__TC_THEME_FLAGS__={switcherEnabled,ultraPremiumEnabled}`, Default beide an) — fehlende Hälfte ist rein serverseitig: (a) KEIN Server→Frontend-Injektionspunkt für `__TC_THEME_FLAGS__` vorhanden (statische HTML-Seiten → Injektions-Mechanismus muss erst gebaut werden, kein preserve-first-Pfad), (b) THEME_SWITCHER_ENABLED/ULTRA_PREMIUM_THEME_ENABLED env→Flag, (c) SCC Theme Control (Owner) per-Scope (platform/worker_portal/scc) + Audit | Premium-Differenzierung jetzt sichtbar; Owner-Steuerung/Whitelabel noch offen | Premium/Enterprise | P2 | Block 2: ThemeControlService + SCC-Panel + envValidator (THEME_SWITCHER_ENABLED/ULTRA_PREMIUM_THEME_ENABLED) | J | Block 1 erledigt / Block 2 SCC-Scope erledigt (Tier-2-Flags THEME_SWITCHER_ENABLED+ULTRA_PREMIUM_THEME_ENABLED → /bootstrap → Topbar-Registry-Cycle + ultra_premium im Operator-Shell; env in .env*.example + envValidator, R5). platform/worker_portal-Scope (static-page Runtime-Config-Injektion) + SCC-Theme-Control-Modul (Preview/Reset/Audit, Settings-Store) owner-gated |
| R5 | Provider-Env-Modell (BILLING_PROVIDER/EMAIL_PROVIDER/INFRASTRUCTURE_PROVIDER/AI_OPS_ENABLED/THEME_SWITCHER_ENABLED) nicht in .env*.example | Inkonsistenz Doku↔Code; Feature-Flags fehlen | intern | P2 | Env-Schema + envValidator erweitern (additiv, Defaults sicher) | D/E/G/H/J | **teilweise (real verdrahtete Vars):** BILLING_PROVIDER/EMAIL_PROVIDER/SENDGRID_API_KEY in allen 3 `.env*.example` einheitlich dokumentiert + in `envValidator.js` als optionale Keys mit Fail-Fast bei unbekanntem Wert (DRY: Konstanten aus billing/emailProviderService, leer=Auto-Derive). **Footgun behoben:** `.env.prod.example` `PAYMENT_MODE=stripe`→`live` (war Validator-ungültig, Boot-Crash). **THEME_SWITCHER_ENABLED/ULTRA_PREMIUM_THEME_ENABLED** (Phase J Block 2 verdrahtet) jetzt in allen 3 `.env*.example` dokumentiert + in `envValidator.js` Boolean-Token-validiert (Default-AN, leer=an; Tippfehler wie `=nein` → Fail-Fast statt still-AN). 18/18 Tests. Offen (nicht verdrahtet): INFRASTRUCTURE_PROVIDER (G), AI_OPS_ENABLED (H) — erst bei Code-Verdrahtung |
| R6 | Incident-Modell (incident_events/status/notes/notifications) Vollständigkeit unklar | Betrieb nicht voll beobachtbar | alle | P2 | Phase I prüfen/ergänzen (110_soc_phase3 als Basis) | I | **operativer Lebenszyklus erledigt (2026-06-03, Owner-freigegeben + gebaut):** `ops_incidents`-Kern-Slice umgesetzt — Mig **121** (`ops_incidents`-Tabelle, CHECK-Enums severity/status/source, opened_reason NOT NULL, *_by→users ON DELETE SET NULL, 3 Indizes, In-File-Rollback) + `staffIncidentService` (read-only `listIncidents`/`meta` + Mutationen open/acknowledge/resolve mit strengen Übergängen open→ack→resolved via SELECT…FOR UPDATE in withTransaction) + 5 SCC-Routen (requireStaff·mfaGuard·requireStepUp·requireConfirmAndReason, Audit area=operations entityType=ops_incident) + SCC-Incidents-Modul. **§6.2 Nachzug:** read-only Feed offener Signale OHNE Incident (failed warp_executions + nach event_key aggregierte Mail-Fehler, je mit `suggested`-Block, „→ Incident" vorbefüllt). 25/25 Tests. Schließt die verwaiste Mig-108-`warp_executions.incident_id`-Lücke (FK bewusst nicht verdrahtet). **Residual (owner-gated):** automatischer Ops-Alert-Notify-Hook (notify-on-critical über bestehenden emailProviderService) — Signale werden heute angezeigt, aber nicht aktiv per Mail eskaliert. Support-Incident-Domäne (Mig 110) unverändert vollständig. |
| R7 | Hetzner-Action-Gating (keine destruktiven Aktionen) + AI Unsafe-Prompt-Classifier verifizieren | Sicherheitsrisiko bei Fehlkonfiguration | intern/Owner | P2 | Phase G/H Audit; Stop-Regel falls Gating fehlt | G/H | **Hälfte A erledigt (Triage 2026-06-02, read-only):** Hetzner/Warp-Gating ist safe-by-design & mehrschichtig erzwungen — eingefrorene 4-Aktions-Whitelist (`DEFAULT_ACTION_COMMANDS`, nicht gelistet→`null`), `spawn(ssh,argv)` ohne `shell:true` (keine Injektion), Regex-Eingangsvalidierung, per-Host-`allowed_actions`-Allowlist erzwungen (`ACTION_NOT_ALLOWED_ON_HOST`), `WARP_SSH_ENABLED` off-by-default. **Keine Stop-Regel-Verletzung (2/3 erfüllt).** Offen: **Hälfte B** (AI-Unsafe-Prompt-Classifier) noch nicht anwendbar — kein AI-Ops-Code (Phase H nicht verdrahtet, vgl. R5 `AI_OPS_ENABLED`); Classifier erst am AI-Ausführungspfad baubar (sonst spekulativ). Phase-G: `warp_hosts`-Seed/Provisioning = Owner/Infra |
| R8 | Backup/Restore-Drill + Runbooks (docs/operations/) | Kein verifizierter Wiederherstellungspfad | alle | P2 (Owner) | Phase P; Restore-Drill mit Owner | P | **teilweise (2026-06-03):** Drill-Fidelity-Bug behoben — `scripts/restore-test.sh` pg_restore um `--single-transaction --exit-on-error` ergänzt (glich vorher NICHT den echten `restore.sh` ab → ein nur teilweise eingespielter Dump konnte fälschlich „bestanden" melden = False-Confidence). Zusätzlich Severity-Matrix von `ingestInfrastructureSnapshots` gepinnt (`infrastructureSnapshotService.test.js`, 36/36: Backup-Staleness-Schwellen 12/24/48h als strikte `>`-Boundaries + cpu/ram/disk/docker/tls-Signale + Input-Härtung). **Offen (Owner):** echter Restore-Drill gegen reale Hetzner-Infra + Runbook-Abnahme — Skript ist jetzt korrekt/streng, aber ein produktiver Wiederherstellungslauf mit Owner steht aus. |
| R9 | Finale Preise + Rechtstexte (DSGVO/AVV/AGB) fehlen (extern) | Kein rechtssicherer Marktstart | alle | P1 (Owner) | Owner/Anwalt (MANUAL_TASKS Phase C/N) | C/N | offen |
| R10 | Feature-Flag-Parsing dreifach dupliziert: `config/featureFlags.js`-Register (Introspektion, jetzt in System-Health + SCC-Operations surfaced) vs. `config/index.js` vs. die 3 echten Consumer (`supportAccess`/`internal`/`warpExecutionService`) parsen dieselben Flags unabhängig | Drift-Risiko: ein an einer Stelle geänderter Default könnte ein Security-Gate (SUPPORT_OPS/WARP_SSH/INFRA) unbemerkt anders schalten als das Register anzeigt | intern | P3 | Single-Source-Konsolidierung auf `isFeatureFlagEnabled(env,key)`; **owner-gated** (verdrahtet Security-Gates um → größerer Blast-Radius, eigener Slice) | Config | offen (bewusst deferred; Register-Defaults wurden 2026-06-02 an die Consumer-Realität angeglichen → heute konsistent, kein akuter Drift) |

## Stop-Regel-Funde (00_RULES.md)
- Aktuell keine harten Stop-Regel-Verletzungen festgestellt. R7 (Hetzner/AI-Gating) gegen Stop-Regeln 2/3 geprüft → safe-by-design (4-Aktions-Whitelist, `spawn` ohne `shell:true`, per-Host-Allowlist, off-by-default); kein Verstoß.

## Geschlossene Skalierungs-Achsen (10→300, nicht als R# geführt, weil keine offenen Blocker mehr)
- **A1 — Unbounded HTTP-Listen:** admin-orgs/-requests/-strategic + usage-limit-scan auf bounded Pagination/Batch gebracht (`{items,total,limit,offset}` / Cron-Batch DEFAULT 100/MAX 500). Status: erledigt (uncommitted, Owner-Gate).
- **A2 — N+1-Write-Loops:** `searchSlaScan`/`demandSlaScan`/`productReleaseService.markAllSeenForUser` auf set-based `UPDATE … RETURNING` + Bulk-`UNNEST` gebracht (verhaltensgleich, No-Duplicate-Event-Semantik erhalten). Verzweigte Scans als dokumentierter Follow-up belassen; `createStaffingCampaignInternal` (INPUT-skaliert + withTransaction/RETURNING/per-Row-Audit) bewusst **owner-gated** (kein autonomes Batchen). Status: Triage abgeschlossen.
- **A3 — Cron-Sweeps ohne Stütz-Index:** ALLE ~20 Sweeps gegen „wächst die gescannte Menge unbegrenzt?" geprüft. Mig **122** (5 Partial-Indizes) + Mig **123** (BRIN auf `product_analytics_events(occurred_at)`); Rest gedeckt durch Mig 009/011/013/030/076 oder kunden-cardinality/trivial. DB-gated Existenz-/Form-Test `scaling-indexes.flow.test.js`. Status: vollständig (uncommitted, Owner-Gate).
- **A4 — N+1-Read-Loops (HTTP-Request-Pfad):** `GET /support/lookup/orgs` (bis zu 100 Round-Trips/Request) auf EINE windowed Query (`loadRecentOpenCasesByOrg`, ROW_NUMBER PARTITION BY, rn≤10, ANY($1::uuid[])) gebracht; Mock-Pool-Unit-Test (Anti-N+1-Zählung) + DB-gated SQL-Validitäts-Smoke. Gesamter routes/+services/-Sweep sonst sauber (bounded/false-positive/batched/cron). Status: erledigt (uncommitted, Owner-Gate).
