# Finalization Worklog — Phase 5 (10-300 Customers)

> Branch: release/enterprise-premium-market-ready. Kompakter Eintrag pro Block. Token sparen.

## 2026-05-31 — Phase A (Reifeprüfung, read-only)
- Geändert: keine Codeänderung (read-only). Neu angelegt: docs/finalization/{10_300_customer_readiness_matrix, finalization_worklog, open_risks_and_blockers, changed_files_index, final_acceptance_report_10_300_customers}.md + .claude/learning/{insights_inbox, config}.md
- Geprüft: 87 Routen, ~100 Services, 20 Middleware, 120 Migrationen, api/config/index.js, api/package.json, emailService.js, paymentService.js
- Ergebnis: Repo sehr reif. Größte Lücke = Theme-System (Phase J, fehlt). Gate-10-Blocker = Billing-Tiefe (Phase D). Email SMTP-only (Phase E, Provider-Abstraktion fehlt).
- Risiken: in open_risks_and_blockers.md eingetragen (Billing-Automatisierung, Provider-Env-Modell, Theme-Neubau, externe Owner-Keys).
- Nächster Block: Owner-Entscheidungen (Gruppen-Reihenfolge, Lernschleife Auto-Approve) → dann Gruppe 2 (Commercial: B→C→D) ODER Phase J (Theme) je nach Owner-Prio.

## 2026-05-31 — Phase J / Block 1 (Theme: ultra_premium — Design-first, Owner-Vorgabe "Design zuerst")
- KORREKTUR zu Phase A: Theme-System existiert bereits (html[data-theme=dark|light], theme.js mit TC.theme-API, vollständiger [data-theme=light]-Tokenblock). Phase J ist **Erweiterung, kein Neubau** (Preserve-first). enterprise_dark≈Default, enterprise_light≈Light.
- Geändert: frontend/public/css/design-system.css (`[data-theme="ultra_premium"]` Tokenblock "Obsidian Sapphire" + 10 restrained Refinement-Regeln, keine Animationen), frontend/public/js/theme.js (Theme-Registry dark/light/ultra_premium + `__TC_THEME_FLAGS__`-Gating + `cycle()`/`list()`, rückwärtskompatibel: `toggle()` bleibt dark↔light), docs/THEME-SYSTEM.md (Doku).
- Neu: api/test/themeRegistry.test.js — 9/9 grün. Checks: node --check OK, eslint 0 Warnings, CSS Brace-Balance 332/332.
- Scope-Disziplin: **reiner Design-Diff** (kein Misch-Diff über Billing/SCC/Auth). Default unverändert (dark). Ultra Premium opt-in.
- Offen (Block 2, separater Diff): SCC Theme Control (Owner, Vorschau/Reset/Audit), per-Scope-Schaltung (platform/worker_portal/scc), env-Flags THEME_SWITCHER_ENABLED/ULTRA_PREMIUM_THEME_ENABLED → inject in window.__TC_THEME_FLAGS__.
- Sichtbar für Owner: App über HTTP öffnen (Docker/nginx, NICHT file://) → Theme-Toggle (Topbar oben rechts bzw. schwebend) mehrfach klicken → Sparkle-Icon = Ultra Premium.

## 2026-05-31 — Phase B / Slice 1 (Customer Operations Backend — read-only Aggregat)
- BEFUND (Preserve-first): Commercial-Spine existiert bereits tief. SCC hat `/customer-requests/*` + `/subscription-requests/*` (offer/approve/reject/activate/transition/documents) — das ist **weitgehend Phase C**. organizations trägt `customer_stage` ('demo','contract_requested','pilot','live', Mig 067/086), `plan` ('DEMO/BASIS/PLUS/PRO/INDIVIDUELL', Mig 102/114), pilot/commercial-Spalten (070/074/080/085), Subscription-Lifecycle (Mig 099/104). Das 15-Status-Chain der Spec ist bewusst **dekomponiert** (customer_stage = grobe Segmentierung, subscription_requests = granularer Prozess).
- ENTSCHEIDUNG: KEINE neue org-Lifecycle-Spalte, KEINE Migration (würde dekomponiertes Modell duplizieren). Echte Lücke = konsolidierter **Customer-Operations-Roster**. "Domain owns truth, SCC owns aggregation."
- Neu: api/services/staffCustomerOperationsService.js (listCustomers paginiert/gefiltert + getCustomerDetail + deriveRiskLevel + meta). Aggregiert org + aktive Members + letzter/offene subscription_requests; Risiko-Signal aus REALEN Spalten (live ohne Onboarding=elevated, needs_clarification=elevated, offered/contract_requested=watch). Zero-State garantiert (leere Menge → customers:[]+total:0). Scope+generated_at in jeder Antwort.
- Geändert: api/routes/staffControlCenter.js — GET /customers, /customers-meta, /customers/:orgId (requireStaff, read-only; Mutation NICHT hier — Statuswechsel laufen über bestehende /subscription-requests/*-Transitions mit Step-up+Confirm+Reason+Audit).
- Neu: api/test/staffCustomerOperations.test.js — 11/11 grün. Checks: node --check (Service+Route) OK, eslint 0 Warnings (meine Dateien).
- HINWEIS (nicht meins): staffControlCenter.js hat eine **vorbestehende** eslint-Warnung `flagRating` ungenutzt (Z.77, aus uncommitteter M-07/Marketplace-Visibility-Arbeit, NICHT in HEAD). Scope-Disziplin: nicht angefasst → Owner mit M-07 bereinigen.
- Offen (Phase B / Slice 2): SCC-Frontend Customer-Operations-Ansicht (Liste/Filter/Detail) über die neuen Endpunkte. Danach Phase C-Verifikation (was fehlt am bestehenden Commercial Desk?) → Phase D (Stripe Billing, Keys=Owner/R2).

## 2026-05-31 — Phase B / Slice 2 (Customer Operations SCC-Frontend — read-only Master-Detail)
- Neu: frontend/src/staff/modules/customer-operations/index.tsx — Master-Detail-Modul nach bestehendem `scc-inbox`-Muster (gespiegelt von subscription-requests). Filterzeile (Stage/Plan/Risiko-Selects + Such-Input, Enter/Button), Pagination (PAGE_SIZE=50, Prev/Next, „from–to von total" + Datenstand), Risiko-/Plan-/Stage-Pills tonal konsistent zum Abo-Modul. Detail-Pane: KPI-Grid (Aktive Nutzer, offene Anfragen, Plan, Stage, Onboarding, Billing-Kontakt, Angelegt) + Tabelle der letzten Abo/Tarif-Anfragen mit **Drilldown** `nav.navigate("subscription-requests", id)` (dort liegen die auditierten Mutationen).
- Geändert: frontend/src/staff/components/shell/Sidebar.tsx (AreaKey-Union + AREAS: „Customer Operations" in Gruppe Arbeitsplatz, zwischen Kundenanfragen und Abo/Tarif), frontend/src/staff/components/shell/AppShell.tsx (lazy-Import + ActiveModule-Branch).
- Scope-Disziplin: **reiner SCC-Frontend-Diff** (kein Misch-Diff über Billing/Auth/Design). Keine Mutation im Modul — Read-only-Ansicht + Drilldown ins bestehende Abo-Modul. Backend-Sicht (requireStaff, plattformweit) unverändert aus Slice 1.
- Checks: `npm run build:scc` grün (tsc --noEmit + vite build, 61 Module, +1 Code-Split-Chunk fürs Modul). Ein TS6133 (ungenutzter `riskPill`-Helper) sofort bereinigt, danach 0 Errors.
- Offen (Phase C-Verifikation): Was fehlt am bestehenden Commercial Desk (`/subscription-requests/*`) gegen die Spec? → dann Phase D (Stripe Billing, Keys=Owner/R2).

## 2026-05-31 — Phase C (Commercial Desk — Verifikation, read-only, KEIN Code)
- BEFUND: Commercial Desk ist **vollständig vorhanden**, zweiseitig getrennt:
  - Kundenseite (`api/routes/subscriptionRequests.js`, requireAuth): POST upgrade · GET+POST downgrade(+preview) · POST cancellation · GET mine · GET :id/history · GET :id.
  - Staffseite (`api/routes/staffControlCenter.js`, requireStaff): GET list · GET :id · POST :id/{transition,approve,reject,activate,offer} · GET+POST :id/documents. Alle Mutationen mit `mfaGuard + requireStepUp(High) + requireConfirmAndReason` → Step-up + Confirm + Reason + Audit (Spec-konform).
- ERGEBNIS: Spec-„Commercial Desk" (Angebot→Annahme/Ablehnung→Aktivierung + Dokumente + Verlauf) ist erfüllt. Das 15-Status-Chain bleibt bewusst dekomponiert (customer_stage grob + subscription_requests granular). **Keine Lücke, keine neue Route, keine Migration.** Phase B (Roster) war die echte Lücke — erledigt (Slice 1+2).
- NÄCHSTER CODE-PHASE = Phase D (Billing-Tiefe). Architekturentscheidung (BillingProviderService stripe/manual/disabled) ist Blueprint-relevant → Owner-Gate vor Bau. Code mit LEEREN Keys baubar; echte Stripe-Keys = Owner/R2.

## 2026-05-31 — Phase D / Slice 1 (BillingProviderService — Provider-Abstraktion, Owner-Entscheidung "zukunftssicher, wenig Skalierungsaufwand")
- OWNER-GATE geklärt: Option „Provider-Abstraktion, manual-first" gewählt. Default ohne externe Keys = `manual` (Rechnung/Vertrag, DE-B2B). Echte Stripe-Keys bleiben Owner/R2.
- Neu: `api/services/billingProviderService.js` — rein funktional, ohne DB/IO:
  - `resolveBillingProvider(config,opts)` → {provider,source,payment_mode,stripe_configured}. Explizites `BILLING_PROVIDER` (stripe|manual|disabled) gewinnt; sonst rückwärtskompatible Ableitung (Stripe nur wenn echter Key UND mode≠demo, sonst manual).
  - `describeBilling(config,opts)` → +capabilities{self_service_checkout,recurring,customer_portal,webhooks,manual_invoicing} +warnings (ehrliche Degradierung: provider=stripe ohne Key → Self-Service aus + Warnung).
  - `mapStripeEvent(event)` → normalisierte Absicht: activation / cancellation / payment_failed (Dunning-Naht Slice 3) / ignored. EINE Stelle für Event-Semantik.
  - `looksLikePlaceholder`/`isStripeConfigured` spiegeln config-Platzhalterliste (sk_test_xxx = echt, sk_test_DEIN/xxxxxxxx = Platzhalter).
- Geändert (additiv, preserve-first): `api/config/index.js` (BILLING_PROVIDER-Env, Slice 0). `api/routes/payment.js` — (a) `/payment/config` um `billing: describeBilling(config)` ergänzt (bestehende Felder unberührt); (b) Webhook-Dispatch über `mapStripeEvent` statt String-`event.type` — **alle Seiteneffekte byte-identisch** (checkout→activation, subscription.deleted→cancellation); Dunning-Kommentar ehrlich auf neue Naht aktualisiert.
- Neu: `api/test/billingProviderService.test.js` — 26/26 grün (Auflösungs-Präzedenz, Capability-Ehrlichkeit inkl. Stripe-ohne-Key-Degradierung, Event-Mapping inkl. payment_failed + ignored + Garbage-Safety).
- Checks: node --check Service+Route OK · billingProviderService.test.js 26/26 · payment.route.test.js 31/31 (Backwards-Compat) · paymentService+billingMetrics+wave09BillingLifecycle 47/47 · ESLint 0 Warnings (3 Dateien). **Reiner Billing-Diff** (kein Misch über SCC/Design/Auth).
- Offen: Slice 2 (SCC-Billing-Read-Model-Sicht über describeBilling + payment_sessions/invoices), Slice 3 (payment_failed-Handler/Dunning-Verhalten + payment-status→Lifecycle-Mapping). Echte Stripe-Keys = Owner/R2-Blocker für Gate-10.

## 2026-05-31 — Phase D / Slice 2 (SCC Billing-Sicht — read-only Operator-Aggregat)
- Neu: `api/services/staffBillingOverviewService.js` — `getBillingOverview(pool,opts)` = describeBilling(config) (Provider+Capabilities+Warnings) + plattformweite Rechnungs-Summen je Status (EINE Aggregat-Query auf invoices: COUNT + SUM total/amount/tax) + letzte Rechnungen über das bestehende `invoiceService.listInvoices` (ohne org/user = global). `meta()` (Zero-Query Statusliste), `INVOICE_STATUSES`. Zero-State garantiert (leere invoices → Summen 0 + by_status alle {0,0} + leere Liste). Status-Filter sanitisiert gegen INVOICE_STATUSES (unbekannt→null), Limit geclampt (1..100, Default 25). **Service importiert `config` direkt** (Router-deps führen kein config). Totals bleiben IMMER global; der Status-Filter verengt nur die Recent-Liste.
- Geändert: `api/routes/staffControlCenter.js` — GET /billing/overview + /billing/meta (requireStaff, read-only, try/catch→500 SCC_INTERNAL_ERROR-Muster). Import `staffBillingOverviewService`.
- Neu: `frontend/src/staff/modules/billing/index.tsx` — Single-Pane Read-only-Sicht: Provider-Karte (Provider-Pill tonal: stripe-aktiv=live/ohne-Key=danger/manual=neutral, source+payment_mode+Key-Status, Capability-Pills ●=aktiv/○=inaktiv nicht nur Farbe, ehrliche Warnungen), Totals-KPI-Grid (Rechnungen/Brutto/Netto/Steuer via fmtCents), Summen-je-Status-Tabelle (kanonische Reihenfolge), Letzte-Rechnungen-Tabelle mit Status-Filter-Select. Reused: fmtCents/fmtNum/fmtDate aus @scc/utils/format, scc-card/scc-table/scc-status/scc-pill — **kein neues CSS**.
- Geändert: Sidebar.tsx (AreaKey + AREAS „Billing", Gruppe Strategie neben Revenue), AppShell.tsx (lazy-Import + ActiveModule-Branch).
- Scope-Disziplin: **isolierter Billing-Diff** (Backend read-only-Aggregat + reiner SCC-Frontend-Diff, kein Misch über Auth/Design). Keine Mutation, KEINE Migration.
- Checks: node --check Service+Route OK · staffBillingOverview.test.js 8/8 (Provider-Shape+Capability-Booleans, Zero-State, Aggregation/Mapping, Status-Sanitisierung+Clamp, Status-Forward in listInvoices-Params) · payment.route.test.js 31/31 (kein Regress) · ESLint: meine 2 Backend-Dateien 0 Warnings (einzige Warnung = vorbestehende `flagRating`/M-07, nicht meins) · `npm run build:scc` grün (tsc --noEmit + vite, 62 Module, +1 Billing-Chunk).
- Offen: Slice 3 (payment_failed-Handler/Dunning + payment-status→Lifecycle-Mapping). Echte Stripe-Keys = Owner/R2.

## 2026-05-31 — Phase D / Slice 3 (Enterprise-Dunning-Observability — Inkasso-Worklist, operator-getrieben)
- ENTSCHEIDUNG (Owner delegiert: „das sinnvollste für direkt zahlende Enterprise-Kunden, dann wirtschaftliche/Effizienz-Reihenfolge"): Default-Provider ist `manual` (Rechnung/Net-14). Auto-Cancel bei Zahlungs-Hickup wäre kommerziell falsch für direkt zahlende Enterprise-Kunden. Slice 3 = **Sichtbarkeit + Operator-Worklist**, KEIN Auto-Cancel, KEINE Kunden-Mail, KEINE Status-Mutation im Webhook. Terminaler Fall läuft weiter über `customer.subscription.deleted`.
- Geändert (additiv, preserve-first): `api/services/staffBillingOverviewService.js` — `loadAttention(pool,limit)` zieht überfällige Forderungen (status='overdue' ODER status='issued' UND due_at<NOW() → fängt auch Cron-Lag), älteste zuerst, `days_overdue` aus `due_at` berechnet; `mapAttention`; `ATTENTION_LIMIT=50`. `getBillingOverview` um 3. parallele Query (Promise.all) erweitert → `attention[]` + `scope.attention_limit`. Totals/Recent unverändert (Reihenfolge der pool.calls erhalten → bestehende Tests gültig). KEINE Mutation, KEINE Migration.
- Geändert (verhaltenswahrend): `api/routes/payment.js` — `else if (intent.kind === "payment_failed")`-Zweig im Stripe-Webhook: `logger.warn` (Observability) mit stripe_subscription_id/stripe_invoice_id/attempt_count/next_payment_attempt; KEIN Seiteneffekt (returnt weiter `received:true`). Dunning-Kommentar aktualisiert.
- Neu: SCC-Frontend `frontend/src/staff/modules/billing/index.tsx` — Abschnitt „Überfällig · Inkasso-Worklist" prominent nach dem Totals-KPI-Grid: Tabelle (Nr./Kunde/Plan/Status/Betrag/Fällig/Überfällig), `days_overdue`-Severity (>14 Tage danger, sonst warn), Zähler-Pill, Zero-State „Keine überfälligen Forderungen" (scc-status--ok), Limit-Hinweis bei Abschneidung. Reused-Helper `dunningTone()`, **kein neues CSS**.
- Tests: `api/test/staffBillingOverview.test.js` — sequencePool auf 3 Antworten erweitert, Attention-Assertions (length/id/status/total_cents Number/days_overdue Number) + Zero-State `attention:[]` + `scope.attention_limit=50` → 8/8 grün. `api/test/payment.route.test.js` — neuer Test „invoice.payment_failed is logged for observability without side effects" (Pool wirft bei Query = beweist keine DB-Berührung; mailSent=false; warnCalled=true) → 32/32 grün.
- Checks: node --check Service+Route OK · staffBillingOverview.test.js 8/8 · payment.route.test.js 32/32 (+1, kein Regress) · ESLint 4 Dateien (Service/Route/2 Tests) 0 Warnings · `npm run build:scc` grün (tsc --noEmit + vite, 62 Module). **Isolierter Billing-Diff** (kein Misch über Auth/Design).
- Offen (R1-Rest): payment-status→Subscription-Lifecycle-Mapping (Reaktivierung nach Zahlungseingang) ist weiterhin Owner/Provider-abhängig; echte Stripe-Keys = Owner/R2 (Gate-10). Die Sichtbarkeits-Hälfte von R1-Dunning ist mit Slice 3 geschlossen.

## 2026-06-01 — Phase E (Email-Provider-Abstraktion — console/smtp/sendgrid/disabled, ohne neue Dependency)
- BEFUND (Preserve-first): emailService.js war SMTP-only über nodemailer (kein SMTP_HOST → Dev-Log). Keine Provider-Abstraktion (R3), KEIN @sendgrid/mail im package.json. **Schlüssel-Erkenntnis:** SendGrid läuft als SMTP-Relay (smtp.sendgrid.net, user "apikey") durch das bestehende nodemailer — **keine neue Dependency nötig**. `.env.example` dokumentiert diesen Weg bereits (Z.122-127).
- ENTSCHEIDUNG: Muster aus billingProviderService.js (resolve/describe) spiegeln, aber entkoppelt (eigene Platzhalter-Erkennung statt Cross-Import) → isolierter Email-Diff. `console` = sicherer Default ohne Konfiguration (nur Logging, byte-identisch zum bisherigen Dev-Verhalten).
- Neu: `api/services/emailProviderService.js` — rein funktional, ohne IO: `resolveEmailProvider(config,opts)` (explizit EMAIL_PROVIDER gewinnt; sonst Ableitung sendgrid>smtp>console) + `describeEmail` (+capabilities{outbound_delivery,pooled_connections,bounce_tracking,dev_logging} +warnings: ehrliche Degradierung wie bei Billing — provider=sendgrid ohne Key → outbound aus + Warnung) + `isSmtpConfigured`/`isSendgridConfigured`/`looksLikePlaceholder`.
- Geändert (additiv, preserve-first): `api/config/index.js` (EMAIL_PROVIDER + SENDGRID_API_KEY-Env). `api/services/emailService.js` — `getTransporter()` wählt Transport je Provider (SendGrid via SMTP-Relay, SMTP-Block unverändert, console/disabled → null = nur loggen); `buildTransportOpts(provider)` extrahiert; neuer Export `describe()` (= describeEmail(config)) für künftige Health-/SCC-Sicht. **sendMail-Signatur byte-identisch**, Default-Pfad (kein SMTP_HOST) unverändert (Dev-Log + Dummy-Return). `.env.example` um Provider-Auswahl-Block ergänzt (adressiert R5 für Email-Dimension).
- Neu: `api/test/emailProviderService.test.js` — 21/21 grün (Provider-Set, Platzhalter, Auflösungs-Präzedenz inkl. opts-Override, Capability-Ehrlichkeit inkl. sendgrid/smtp-Degradierung, unbekannter Provider→Warnung+Ableitung, Garbage-Safety inkl. null).
- Checks: node --check Service/emailService/config OK · emailProviderService.test.js 21/21 · subscriptionNotifications.test.js 43/43 (kein Regress, mock-sendMail unberührt) · ESLint 4 Dateien 0 Warnings · KEINE neue npm-Dependency. **Isolierter Email-Diff** (kein Misch über Billing/SCC/Auth/Design). Keine Migration, keine Mutation.
- Offen: Email-Versand IST jetzt provider-fähig (R3 Backend geschlossen). Optionale Folge-Slice: describe() in einer Health-/SCC-Ops-Sicht surface (analog Billing Slice 2). Echte SendGrid/SMTP-Keys = Owner/Betrieb (Gate-50).

## 2026-06-01 — Phase I / Slice 1 (Provider-Readiness in System-Health — Mail/Billing in Diagnostics, admin-scoped)
- BEFUND (Preserve-first): `getSystemDiagnostics(pool)` (hinter `/admin/system-health`, requireAuth+requireAdmin) deckte db/redis/process/api ab — **Mail+Billing-Readiness fehlten** (Phase-I-Lücke „DB/Redis/Mail/Billing/… Health"). Genau die Provider-Selbstauskunft (describeBilling/describeEmail) aus Phase D/E lag bereits bereit → maximale Wiederverwendung, KEIN neuer IO-Pfad.
- ENTSCHEIDUNG: rein additive Anreicherung der bestehenden Diagnostics. Kein Eingriff in den öffentlichen `/service-status`-Endpunkt (der prüft smtp/stripe weiter direkt — preserve-first, kein Public-Surface-Change). Semantik Spec-konform: **nicht konfigurierter optionaler Provider = `unconfigured` (NICHT „failed"), zieht den Gesamtstatus nicht**; nur echte Fehlkonfiguration (externer Provider gewählt, aber nicht lieferfähig) = `degraded`.
- Geändert (additiv): `api/services/healthService.js` — Imports (config + describeBilling/BILLING_PROVIDERS + describeEmail/EMAIL_PROVIDERS); Helfer `countTrue` (Capability-Zähler ohne Secret-Leak), `billingHealth(desc)`/`emailHealth(desc)` (Mapper Selbstauskunft→Component-Status); `getSystemDiagnostics(pool, opts={})` (+`opts.config` für Test-Injektion, Default = globale config); Integrations-Block (components.billing/email) NACH api-Metriken, VOR Overall-Rollup, in try/catch (describe* sind rein funktional/sync → garbage-safe, aber defensiv abgesichert). Exponiert nur Provider-Name/Quelle/Booleans/Zähler + bereits secret-freie Warntexte — **keine Schlüsselwerte**.
- Mapper-Detail: safe-default-Provider (billing manual/disabled, email console/disabled) sind IMMER `unconfigured` — auch mit informellem Dev-Hinweis (console-Log-Warnung ist KEINE Fehlkonfiguration). `ok` nur wenn externer Provider lieferfähig+warnungsfrei; sonst `degraded`.
- Neu: `api/test/systemHealth.test.js` — Block „Provider-Readiness (billing/email)" (5 Tests): no-config→unconfigured+kein „critical"; stripe+sendgrid mit echten Keys→ok+automated/outbound; erzwungen-ohne-Key→degraded+Warnung+Gesamtstatus degraded; **Secret-Safety** (serialisierte Komponenten enthalten Stripe-/SendGrid-Key NICHT); Shape-Garantie (billing+email immer da, status/provider String, warnings Array).
- Checks: node --check OK · systemHealth.test.js 17/17 · healthService+billingProviderService+emailProviderService 58/58 (kein Regress) · ESLint 2 Dateien 0 Warnings. **Isolierter Health-Diff** (admin-scoped, kein Misch über Billing/SCC/Auth/Design). Keine Migration, keine Mutation, kein Public-Surface-Change.
- Offen (Phase I-Rest): Incident-Modell (incident_events/status/notes/notifications) + Alert-Verdrahtung (ALERT_EMAIL) + Storage/Worker-Health-Tiefe. SCC-Ops-Surface von billing/email-Readiness (analog Billing Slice 2) optional.

## 2026-06-02 — Phase I / Slice 2 (Service-Health im SCC Operations-Modul — Operator-Sicht)
- BEFUND (Preserve-first): `getSystemDiagnostics` (Slice 1, inkl. billing/email) war nur über das Admin-Panel (`/admin/system-health`) sichtbar. Der Staff-Operator sieht im SCC-Modul `operations` (`/staff/api/operations` → `loadOperationsSnapshot`) bisher nur Runbook-Läufe + Infra-Snapshots, KEINE Live-Service-Health. Lücke = Surface, nicht Backend.
- ENTSCHEIDUNG: rein additive Anreicherung des bestehenden Operations-Aggregats (KEIN neuer Endpunkt, KEIN Mischen von Business-Logik in die Route). `loadOperationsSnapshot(pool)` ergänzt um `service_health = await getSystemDiagnostics(pool)`. Wiederverwendung statt Duplikat — db/redis/process/api/billing/email in einem Round-Trip.
- Geändert (additiv): `api/services/staffControlService.js` — Import `getSystemDiagnostics` aus `./healthService.js`; `service_health`-Feld im Snapshot; Diagnostics-Aufruf als **LETZTE pool-Berührung** (getSystemDiagnostics macht `SELECT 1`) → bestehende Pool-Sequenz-Tests (runbook_runs, infra_health = calls[0],[1]) bleiben index-gültig. Soft-Fail: try/catch → `errors[].area="service_health"`, Snapshot kippt nie. Kein Zyklus (healthService importiert staffControlService NICHT).
- Geändert (Frontend): `frontend/src/staff/modules/operations/index.tsx` — Abschnitt „Live-Service-Health" prominent NACH dem Header, VOR Infra-Health: Karten-Grid (Datenbank/Redis/API/Prozess/Billing/E-Mail) mit Status-Pill (healthTone: critical→danger, degraded→warn, ok→ok, unconfigured/configured→neutral) + Detailzeile (DB-Latenz+Pool%, API-Fehlerrate+p95, Uptime+RSS, Provider+automated/outbound) + **secret-freie Provider-Warnungen** (billing/email) inline. Reused: scc-card/scc-status/scc-grid/scc-muted — **kein neues CSS**, keine Emojis.
- Tests: `api/test/staffControlService.test.js` — +2 Tests im bestehenden `loadOperationsSnapshot`-Block: service_health befüllt (db/billing/email-Komponenten vorhanden) + Soft-Fail (DB-Fehler in Live-Diagnostics → database=critical, Snapshot kippt nicht). 46→48 grün. Bestehende 3 Sequenz-Tests unverändert gültig (neue Query ans Ende).
- Checks: node --check OK · staffControlService.test.js 48/48 · ESLint 2 Backend-Dateien 0 Warnings · `npm run build:scc` grün (tsc --noEmit + vite, operations-Chunk neu). **Isolierter Diff** (SCC-Ops-Backend-Anreicherung + reiner SCC-Frontend-Diff, kein Misch über Billing/Auth/Design). Keine Migration, keine Mutation, kein RBAC-/Surface-Change (Route bleibt requireStaff, nur Payload additiv).
- Offen (Phase I-Rest): Incident-Modell (incident_events/status/notes/notifications) + Alert-Verdrahtung (ALERT_EMAIL) + Storage/Worker-Health-Tiefe.

## 2026-06-01 — R5 (Provider-Env-Modell konsolidieren — BILLING_PROVIDER/EMAIL_PROVIDER/SENDGRID_API_KEY + Fail-Fast)
- BEFUND (Preserve-first): Die in Phase D/E real verdrahteten Provider-Vars (`api/config/index.js`: EMAIL_PROVIDER, SENDGRID_API_KEY, BILLING_PROVIDER) waren in den `.env*.example` uneinheitlich: `.env.example` hatte nur EMAIL_PROVIDER-Block (kein BILLING_PROVIDER), `.env.dev.example`/`.env.prod.example` hatten keinen Provider-Block. `envValidator.js` (Boot-Gate via server.js:16) kannte die Keys nicht → unbekannte Werte (Tippfehler) wären erst zur Laufzeit als stille Fehl-Ableitung aufgefallen. **Footgun gefunden:** `.env.prod.example` Z.50 `PAYMENT_MODE=stripe` — UNGÜLTIG laut Validator-Enum `["demo","live"]` → ein Prod-Deploy nach Vorlage wäre beim Boot gecrasht.
- ENTSCHEIDUNG: nur REAL verdrahtete Vars dokumentieren/validieren (BILLING_PROVIDER, EMAIL_PROVIDER, SENDGRID_API_KEY). INFRASTRUCTURE_PROVIDER/AI_OPS_ENABLED/THEME_SWITCHER_ENABLED NICHT — nicht im Code (Phasen G/H/J offen) → „Keine spekulativen Features". DRY: gefrorene Konstanten-Sets aus den Services importieren statt Wertelisten duplizieren. Leerer Wert = Auto-Derive (verhaltenswahrend), nur gesetzter unbekannter Wert = harter Fehler.
- Geändert (additiv, preserve-first): `api/config/envValidator.js` — Import `BILLING_PROVIDERS`/`EMAIL_PROVIDERS` (zyklusfrei: beide Services sind rein funktional, 0 Imports); 3 optionale Keys im Schema (EMAIL_PROVIDER, SENDGRID_API_KEY, BILLING_PROVIDER); superRefine: gesetzter+unbekannter (lowercased) Provider-Wert → addIssue (Fail-Fast mit Erlaubt-Liste). `.env.example` — BILLING_PROVIDER-Doku-Block bei PAYMENT MODE (EMAIL_PROVIDER war schon da). `.env.dev.example` — kommentierte EMAIL_PROVIDER/BILLING_PROVIDER mit Auto-Derive-Hinweis. `.env.prod.example` — **Footgun-Fix `PAYMENT_MODE=stripe`→`live`** + explizit `BILLING_PROVIDER=stripe` + EMAIL_PROVIDER/SENDGRID_API_KEY-Doku; irreführenden Kommentar („stripe = echte Zahlungen") korrigiert (PAYMENT_MODE ist demo|live; „stripe" gehört konzeptuell in BILLING_PROVIDER).
- Tests: `api/test/envValidator.test.js` — 5 neue Tests (EMAIL_PROVIDER unbekannt→throw, BILLING_PROVIDER unbekannt→throw, gültige Werte→pass, leer→pass/Auto-Derive, Case-Insensitivity „SMTP"/„Stripe"→pass) → 13/13 grün.
- Checks: node --check envValidator.js OK · envValidator.test.js 13/13 · billingProviderService+emailProviderService 47/47 (kein Regress) · zyklusfreier Import verifiziert (beide Services 0 Imports). **Isolierter Env-Modell-Diff** (keine Migration, keine Mutation, kein Public-Surface-Change). KEIN Commit — wartet auf Owner-Freigabe.
- Offen (R5-Rest): INFRASTRUCTURE_PROVIDER (Phase G), AI_OPS_ENABLED (Phase H), THEME_SWITCHER_ENABLED (Phase J) erst dokumentieren, wenn im Code verdrahtet. Stale `finalization/README.md`-Statustabelle (zeigt WAVE_00-15 als „offen") = separater Doku-Fix.

## 2026-06-01 — Config-Taxonomie + Feature-Flag-Register (Owner-Freigabe: Drei-Ebenen-Modell als kanonisches Muster)
- OWNER-ENTSCHEIDUNG (2026-06-01): Config wird in DREI strikt getrennte Ebenen modelliert — kanonisch für TempConnect + die 20+ Blueprint-Folgeprojekte:
  - **A) Provider-Abstraktion** (Env, Deploy-Zeit, „welches Backend?"): billing/email/künftig infrastructure — gefrorenes `*_PROVIDERS`-Set + `resolve*`/`describe*`, sicherer Default, Capability-Ehrlichkeit, fließt in System-Health.
  - **B) Plattform-Kill-Switch** (Env, Deploy-Zeit, „darf das überhaupt laufen?"): Boolean-Flags → neues Register `api/config/featureFlags.js`.
  - **C) Pro-Kunde-Entitlement** (DB/runtime, KEIN Redeploy, „darf DIESER Kunde?"): `planFeatures.hasFeature` / `requireFeature` / `entitlementService`.
  - **Skalierungs-Regel (bis 300):** Env = Plattform-Fähigkeit/Kill-Switch; Pro-Kunde = Entitlement/DB/runtime. Niemals vermischen (Env-Flag pro-Prozess + Redeploy-gebunden → bricht bei „welcher von 300 Kunden bekommt X").
- Neu (additiv, preserve-first): `api/config/featureFlags.js` — getipptes Register `FEATURE_FLAGS` (FEATURE_GATE_BYPASS, SUPPORT_OPS_ENABLED, WARP_SSH_ENABLED, INFRA_SNAPSHOT_INGEST_ENABLED) mit `{default, description, productionConstraint}`; Helfer `featureFlagFields()` (zod-Shape fürs envSchema), `applyProductionFlagConstraints(data,ctx)` (deklarative Prod-Safety, z. B. `forbidden_when_true`), `isFeatureFlagEnabled(env,key)` (kanonischer Read), `listFeatureFlags(env)` (Introspektion ohne Secret-Leak).
- Geändert (verhaltenswahrend): `api/config/envValidator.js` — 4 hartkodierte Flag-Enums entfernt, durch `...featureFlagFields()` ersetzt; inline FEATURE_GATE_BYPASS-Prod-Block durch `applyProductionFlagConstraints(data,ctx)` ersetzt. **Boot-Verhalten byte-identisch** (Default false, Prod-Verbot bei =true bleibt), jetzt deklarativ aus einer Quelle.
- AI_OPS_ENABLED (Phase H, Owner-Freigabe vorab): wird als Register-Eintrag `{default:"false", productionConstraint:"forbidden_when_true"}` (bzw. destruktiv-gated) ergänzt, SOBALD der AI-Ops-Service + Unsafe-Prompt-Classifier verdrahtet sind (R7). Default OFF. Heute NICHT als Phantom-Flag angelegt (kein Code zum Gaten).
- Tests: `api/test/featureFlags.test.js` — 8/8 (Frozen-Set, Shape/Default, Reject-Non-Boolean, Prod-Constraint an/aus + nicht-Prod-No-op, Read-Fallback, Introspektion). `envValidator.test.js` 13/13 unverändert grün (inkl. „FEATURE_GATE_BYPASS=true in Produktion verboten" → Regression-Beweis).
- Checks: node --check featureFlags+envValidator OK · 21/21 grün · ESLint 3 Dateien 0 Warnings · zyklusfrei (featureFlags importiert nur zod). **Isolierter Config-Diff** (keine Migration, keine Mutation, kein Public-Surface-Change). KEIN Commit — wartet auf Owner-Freigabe.
- Offen (Folge): optionale Migration verstreuter `process.env`-Boolean-Reads auf `isFeatureFlagEnabled` (größerer Blast-Radius → eigener Slice); `listFeatureFlags` in OCC/SCC-System-Health surface (analog Provider-describe()).

## 2026-06-02 — Phase Q / Audit Slice 1 (Skalierungs-Posture der neuen Read-Aggregate — read-only, KEIN Code)
- ANLASS: In dieser Welle kamen plattformweite Read-Aggregate dazu (Phase B Customer-Roster, Phase D Billing-Totals/Attention-Worklist). Genau diese „scan über ALLE Kunden"-Queries sind der klassische „bei 10 ok, bei 300 kaputt"-Pfad (CLAUDE.md Wirtschaftlichkeits-Lernfeld: „teure Pfade … Index prüfen"). Vor einer Index-Migration zuerst geprüft, ob der Bestand schon trägt.
- BEFUND (Preserve-first, „keine spekulativen Features"): **bereits abgedeckt — keine neue Migration.**
  - **Indizes:** Attention-Query (`status='overdue' OR (status='issued' AND due_at<NOW())` ORDER BY due_at) trifft exakt den PARTIELLEN `inv_overdue_idx ON invoices(due_at,status) WHERE status IN ('issued','overdue')` (Mig 032 Z.95-97). Status/Org/User-Pfade: `invoices_status_due_idx`, `invoices_org_status_idx`, `invoices_user_status_idx` (Mig 030). Audit (SCC `listStaffAudit`: actor_id-Filter + created_at-Range/Order): `audit_actor_time_idx`, `audit_entity_time_idx` (Mig 032). Roster-Joins (org_memberships): `om_org_role_idx`, `om_user_org_active_idx` (Mig 032). `organizations` = ≤ Kundenzahl Zeilen → bei 300 trivialer Seq-Scan (Index auf customer_stage/plan wäre kontraproduktiv). Totals-`GROUP BY status` = inhärenter Voll-Aggregat (≤ #Status Ergebniszeilen, kein Index hilft).
  - **Pagination/Bounding:** Attention `LIMIT $1` (clamp ≤ 50), `listInvoices` `LIMIT 50` Default, Roster `clampLimit(def 50,max 200)`+`clampOffset`+`LIMIT/OFFSET`, Detail-Subqueries `LIMIT 1`/`LIMIT 20`. Keine unbounded plattformweite Liste in den neuen Pfaden.
- ERGEBNIS: Die Read-Aggregate dieser Welle halten bei 300 Kunden ohne Zusatzcode. Eine zusätzliche Index-Migration wäre redundant zu 030/032/086 und verstößt gegen preserve-first → bewusst NICHT geschrieben. Acceptance-Report „Geprüfte Bereiche" + Gate-300 entsprechend ergänzt.
- Offen (Phase Q-Rest, eigene Slices): breiter Pagination-Sweep über ALTbestehende Staff/Admin-Listen (außerhalb dieser Welle), SCC-Skalierungsanzeige (langsamste Endpoints/Queue-Tiefe — teils in `operations` schon da), Kosten-/Ressourcenindikatoren (braucht reale Kostendaten → nicht spekulativ bauen).

## 2026-06-02 — Feature-Flag-Register: Default-Ehrlichkeit + System-Health-Surface + Rollen-Konsolidierung

### Teil 1 — Feature-Flag-Register in System-Health verdrahtet (Folge aus Config-Taxonomie 2026-06-01)
- BEFUND (Korrektheits-Bug im eigenen Register VOR der Verdrahtung gefunden): `featureFlags.js` führte SUPPORT_OPS_ENABLED/WARP_SSH_ENABLED/INFRA_SNAPSHOT_INGEST_ENABLED als `default:null` (→ `.optional()`), während die REALEN Consumer abweichende Defaults haben: `supportAccess.js:199` SUPPORT_OPS on-by-default, `internal.js:26` INFRA on-by-default (`!== "false"`), `warpExecutionService.js:93` WARP_SSH off-by-default. Ein Introspektions-Register mit null-Default hätte über den tatsächlichen Laufzeit-Default gelogen.
- ENTSCHEIDUNG: Register-Defaults an die echten Consumer-Defaults angleichen (SUPPORT_OPS="true", INFRA="true", WARP_SSH="false"; FEATURE_GATE_BYPASS bleibt "false"+forbidden_when_true). **Laufzeit-neutral bewiesen:** `server.js:16` ruft `validateEnv()` nur als Boot-Gate (Fail-Fast) und verwirft den Rückgabewert; `config/index.js` liest `process.env` direkt → die validierte Ausgabe ist inert, die Default-Änderung verändert kein Laufzeitverhalten.
- Geändert (additiv/verhaltenswahrend): `api/config/featureFlags.js` (3 Defaults korrigiert). `api/services/healthService.js` — Import `listFeatureFlags`; `opts.env` (Default `process.env`, für deterministische Test-Injektion); top-level `feature_flags`-Feld in `getSystemDiagnostics` (try/catch, rein funktional/kein IO). **Bewusst NICHT in `components`:** ein Kill-Switch an/aus ist Konfigurationszustand, kein Liveness-Signal → eskaliert den Gesamtstatus nie. Secret-frei (nur key/enabled/default/description/constraint).
- Tests: `featureFlags.test.js` (Default-Assertions an Consumer-Realität angeglichen + on-by-default-Spiegel in listFeatureFlags). `systemHealth.test.js` — neuer Block „feature_flags-Introspektion" (4 Tests, `{config,env}`-Injektion gegen Docker-`FEATURE_GATE_BYPASS=true`): Array+secret-freie Shape, leeres env→Consumer-Defaults, explizites env gewinnt, kein Gesamtstatus-Eskalat.
- Checks: node --check OK · 42/42 (featureFlags+systemHealth+envValidator) + 59/59 (staffControlService+healthService — kein Regress über `loadOperationsSnapshot`: das reine `feature_flags`-Feld berührt den Pool nicht, index-basierte Pool-Sequenz-Tests bleiben gültig) · ESLint 0 Warnings.
- Offen (owner-gated): `config/index.js` + die 3 Consumer parsen die Flags weiterhin unabhängig (eigene Parse-Pfade). Single-Source-Konsolidierung auf `isFeatureFlagEnabled` würde Security-Gates umverdrahten → größerer Blast-Radius, separater Slice mit Owner-Freigabe.

### Teil 2 — Rollen-Konsolidierung (Claude = einziger KI-Agent; OpenAI/ChatGPT + Warp/Oz-Agent entfernt)
- ANLASS (Owner): „entferne openAI und Warp bzw Oz und übernehme jetzt dauerhaft deren Aufgaben." Ziel = AGENT-Rolle entfernen, NICHT das funktionale WARP_SSH-Feature.
- Geändert: `AGENTS.md` §7 (Claude einziger Full-Stack-Agent, Co-Author-Zeile → Claude), `CLAUDE.md` (repo-root: ChatGPT-Zeile raus, Datum → 2026-06-01), `C:\Users\…\CLAUDE.md` (AI-Stack-Tabelle auf eine Claude-Zeile kollabiert), `finalization/00_RULES.md` §7, `finalization/WAVE_01_release_hygiene.md` (Z.4), `api/app.js` (Header „Built by Claude (Anthropic) & Dennis Stegemann", Emoji gemäß No-Emoji-Regel entfernt).
- ABGRENZUNG (kritisch, bewusst NICHT angefasst): funktionales `WARP_SSH_*` / `warpExecutionService.js` / `RATE_LIMIT_WARP_EXEC` / `api/routes/occ/warp.js` / `infrastructureSnapshotService.js` / `WARP-TASKS-PERMANENT.md` = reales Infra-Feature → bleibt. Nur die AGENT-Rolle „Warp/Oz" wurde entfernt. Post-Edit verifiziert: 0 verbleibende Agent-Rollen-Treffer.
- Checks: keine Codeänderung in Teil 2 außer 1 Header-Kommentar → node --check app.js OK.

KEIN Commit (Owner-Gate: erst bei 100%/Marktstart-Reife + persönlicher Owner-Check).

## 2026-06-02 — Phase I / R6 Triage (Incident-Modell — read-only, KEIN Code)
- ANLASS: R6 („Incident-Modell incident_events/status/notes/notifications Vollständigkeit unklar", Phase I, „110_soc_phase3 als Basis"). Preserve-first geprüft, BEVOR ein neues Datenmodell gebaut wird (CLAUDE.md: „Existiert das Pattern schon?"). Keine `incident_events`/`incident_status`/`incident_notes`-Tabelle in irgendeiner Migration (grep bestätigt).
- BEFUND — zwei strikt getrennte Welten:
  - **Support-Incident-Domäne = VOLLSTÄNDIG (Mig 110):** `support_cases` (Status-Maschine new→…→escalated→resolved/closed/reopened, priority, SLA-Deadlines), `support_case_notes` (internal/external/system), `support_case_events` (Timeline), `support_escalations` (Status pending→acknowledged→resolved/rejected, target decisions/commercial/ops/owner), `support_audit_log`. Deckt „Vorfall + Status + Notizen + Events + Eskalation" für KUNDEN-Support komplett ab. **Keine Lücke.**
  - **Operativer/Plattform-Incident = DÜNN:** einziges Signal `staffControlService.js:14/57-63` — `incidents_24h` aus `audit_log` abgeleitet (action LIKE '%.error%'/'%.incident%' bzw. severity error|critical, 24h) → `platform_status` (>10 degraded, >0 warning, sonst ok). KEIN operativer Incident-Record (kein open→ack→mitigated→resolved-Lebenszyklus, keine Incident-Notizen, kein Postmortem-Trail).
  - **Alert-/Notify-Verdrahtung = FEHLT:** kein ALERT_EMAIL/notify-on-critical-Pfad. Einzige „Alert"-Dienste = Marketplace Match-Alerts (`matchAlertService.js`, fachfremd). `getSystemDiagnostics` (Slice 1) berechnet `status`, aber NICHTS benachrichtigt bei critical.
- ERGEBNIS: R6 ist **teilweise** abgedeckt — Support-Seite fertig (Mig 110). Residual = (a) operativer Incident-Lebenszyklus + (b) Ops-Alert-Verdrahtung. Beides = NEUES Datenmodell/Notify-Pfad = Architekturentscheidung mit Folgeprojekt-Tragweite → **owner-gated** (CLAUDE.md: „Architekturentscheidungen … immer Owner fragen"). KEIN Code ohne Scope-Freigabe; `support_cases` NICHT wiederverwenden (Kunde≠Betrieb, „Keine Vermischung").
- EMPFEHLUNG (zur Owner-Entscheidung, minimal/preserve-first): schlanke `ops_incidents`-Tabelle (status open/acknowledged/mitigated/resolved + severity + notes-JSONB + source health_transition|manual) + Alert-Hook am critical-Übergang in `getSystemDiagnostics` (ALERT_EMAIL über bestehenden `emailProviderService` — KEIN neuer Provider, KEINE neue Dependency). Eigener Slice, erst nach Greenlight.
- Offen (Phase I-Rest, unverändert): Storage/Worker-Health-Tiefe. Nächster Triage-Kandidat: R7 (Hetzner-Action-Gating + AI-Unsafe-Prompt-Classifier, Phase G/H — AI-Hälfte erst prüfbar, wenn Phase H verdrahtet ist).

## 2026-06-02 — Phase E/F: SCC Mail Center (read-only Operator-Sicht)
- ANLASS: Phase E lieferte die Email-Provider-Abstraktion (Backend), aber es gab KEINE Operator-Sicht auf den Mailversand. Phase F listet „Mail & Notifications" als SCC-Modul. Lücke verifiziert: `subscription_notification_log` (Mig 103) hat Schreiber (subscriptionNotificationService schreibt mail_status/dispatched_via/mail_error als Idempotency- + Beobachtbarkeits-Schicht), aber kein Read-Aggregat. Echte Phase-E/F-Lücke, ohne Migration/Mutation schließbar.
- BEFUND/ENTSCHEIDUNG: bewährtes Read-only-SCC-Aggregat-Muster 1:1 von Billing übernommen — `staffMailCenterService.getMailOverview(pool, opts)` zieht `describeEmail(config)` (Provider-Status) + 3 parallele Read-Queries aus `subscription_notification_log`: (1) Summen je `mail_status` GROUP BY COALESCE(mail_status,'none') — NULL = nur in-app, kein Mailversuch; (2) Kanäle je `dispatched_via`; (3) letzte Notifications ORDER BY created_at DESC LIMIT (nutzt vorhandenen Index `…_created_idx`). Entkoppelt: importiert `describeEmail` direkt aus emailProviderService (nicht den Transport emailService) → isolierter Mail-Diff.
- PII: `maskEmail` maskiert Empfänger auf `a***@domain` (erste Stelle + Domain bleiben für Domain-Plausibilität sichtbar, Local-Part nicht geleakt); `mail_error` auf 240 Zeichen begrenzt. Operator sieht Zustell-Beobachtbarkeit ohne volle Personen-Adresse (CLAUDE.md „keine sensiblen Daten"). Test beweist die Maskierung (`!recipient.includes("anna")`).
- Geändert (additiv, read-only): NEU `api/services/staffMailCenterService.js` + `api/test/staffMailCenter.test.js` (10/10) + `frontend/src/staff/modules/mail/index.tsx`. `api/routes/staffControlCenter.js` (Import staffMail + GET /mail/overview + /mail/meta, requireStaff). Sidebar.tsx/AppShell.tsx (AreaKey `mail` + „Mail & Notifications", Gruppe Operations + lazy/Branch). Kein neues CSS, keine Migration, keine Mutation, kein Versand.
- Nachtrag (gleiche Welle, read-only): Event-Typ-Breakdown `events_by_key` ergänzt — 4. paralleler GROUP-BY-Query auf der bereits indizierten `event_key`-Spalte (`subscription_notification_log_event_idx`), je Event-Typ `total`/`failed`/`no_smtp`, sortiert nach Häufigkeit. Service-Promise.all bleibt call-index-stabil (Event-Query ans Ende → `pool.calls[2]` weiterhin recent, bestehende Param-Assertions gültig). Frontend: Tabelle „Nach Event-Typ" zwischen Zustellkanäle und letzte Notifications (Fehler-/Kein-SMTP-Zellen farbcodiert). Damit ist die ehemals offene „Template-Status-Sicht" abgedeckt — welcher event_key am häufigsten/häufigsten-fehlerhaft feuert.
- Checks: node --check Service+Route OK · staffMailCenter.test.js 10/10 (inkl. neuer events_by_key-Assertions) · ESLint meine 2 Backend-Dateien 0 Warnings (Restwarnung `flagRating`/M-07 unberührt) · `npm run build:scc` grün (63 Module = +1 Mail-Chunk, kein TS6133). Acceptance-Report + changed_files_index + worklog + insights_inbox gepflegt.
- Offen (Phase E/F-Rest, eigene Slices): SendGrid-Event-Webhook (Bounces/Opens — braucht echten Key = Owner/Gate-50), generische `notifications`-Tabelle (worker/marketplace) als zweite Mail-Quelle. KEIN Commit (Owner-Gate).

## 2026-06-02 — Phase G/H / R7 Triage (Hetzner-Action-Gating + AI-Unsafe-Prompt-Classifier — read-only, KEIN Code)
- ANLASS: R7 (Stop-Regel-relevant: „keine destruktiven Aktionen" + „AI Unsafe-Prompt-Classifier verifizieren", Phase G/H). Vor jeder Härtung zuerst das bestehende Gating gelesen (`warpExecutionService.js` komplett), statt anzunehmen.
- BEFUND — Hälfte A (Hetzner/Warp-Action-Gating) = **safe-by-design, mehrschichtig erzwungen, NICHT dekorativ:**
  - **Eingefrorene Aktions-Whitelist:** `DEFAULT_ACTION_COMMANDS` (Z.11-16) = genau 4 Aktionen (deploy/restart_service/backup_trigger/health_check). `buildCommandForAction` (Z.160-166) gibt für jeden nicht gelisteten Key `null` → kein freitext-Kommando möglich. Nur `{{APP_DIR}}`/`{{HEALTH_URL}}`-Substitution.
  - **Keine Shell-Injektion:** `runSshCommand` (Z.168) nutzt `spawn("ssh", argv)` mit Argument-Array, KEIN `shell:true`; BatchMode=yes, Timeouts, 16KB-Output-Cap. Regex-Validierung (`SAFE_HOST/USER/PATH/ACTION_PATTERN`, Z.4-7) an der Eingangsgrenze.
  - **Per-Host-Allowlist erzwungen:** `runExecution` (Z.316) Gate-Kette = Plan-Aktionen müssen gelistet sein → `validateSshConfig` → Host muss in `warp_hosts`-Registry + `ssh_ready=true` → `isActionAllowedOnHost` (Z.310-314) prüft `allowed_actions` je Host → Verstoß = `ACTION_NOT_ALLOWED_ON_HOST` (Z.395). 
  - **Off-by-default:** `resolveSshConfig` (Z.92-93) `WARP_SSH_ENABLED` Default false → ohne explizites Opt-in kein Remote-Exec. Deckt sich mit Register (Teil 1 oben) + open_risks R7.
- BEFUND — Hälfte B (AI-Unsafe-Prompt-Classifier) = **noch nicht anwendbar:** kein AI-Ops-Code im Repo (Phase H nicht verdrahtet, vgl. R5: `AI_OPS_ENABLED` nicht real verdrahtet). Ein Classifier ohne AI-Ausführungspfad wäre spekulativ (CLAUDE.md „Keine spekulativen Features"). Korrekt deferred bis Phase H Code bringt.
- ERGEBNIS: R7 = **keine akute Lücke, keine Stop-Regel-Verletzung** (Stop-Regel 2/3 erfüllt: destruktive Aktionen nur über eingefrorene+per-Host-allowlisteten Pfad, off-by-default). Hälfte A verifiziert/abgehakt; Hälfte B bleibt Phase-H-gebunden. KEIN Code nötig.
- Offen (Phase G/H-Rest): Hälfte B erst prüfbar/baubar, wenn Phase H einen AI-Ausführungspfad liefert (dann Classifier am Eingang dieses Pfads, nicht vorher). Phase-G-Härtung von `warp_hosts`-Seed/Provisioning = Owner/Infra (reale Hosts).
KEIN Commit (Owner-Gate: erst bei 100%/Marktstart-Reife + persönlicher Owner-Check).

## 2026-06-02 — Incident-Modell: Freigabe-Entwurf (Owner-Gate, KEIN Code/Commit)
- ANLASS: Nach Abschluss Mail Center (Phase E/F) ist der einzige genuin wertvolle Rest in Gruppe SCC-Ops das persistente Incident-Tracking — read-only nicht schließbar (braucht Tabelle). Owner-Greenlight „gerne weiter machen" auf das angebotene Vorgehen „Migrations-Entwurf + Rollback zur Freigabe, noch ohne Commit".
- REPO-VERANKERUNG (nicht spekulativ): `warp_executions.incident_id UUID` (Mig 108, Z.126) = verwaister Hook ohne FK/Zieltabelle (Schema sah Incidents vor, nie angelegt). `reportingService.alerts[]` (Z.1187+) = org-skopiert + flüchtig (kein Überdauern/Quittieren). `risk_signals` existiert, ist aber OCC-verdrahtet (`/risk/signals`) → SCC-Klon wäre Parallelstruktur. Plattformweite Alert-Reuse = N× Executive-Dashboard, zu teuer.
- LIEFERUNG: `docs/finalization/proposal_incident_model_owner_gate.md` — konkrete Migration `ops_incidents` (spiegelt Mig-108-Konventionen: gen_random_uuid PK, severity-CHECK = warp risk_level, audit_id FK), Rollback-Plan, Service/Route/UI-Design (read-only Liste + Mutationen open/ack/resolve mit CSRF+reason+Audit), Pfeiler-Check, 5 offene Owner-Entscheidungen. KEIN Live-Migrationsfile, KEINE Code-Verdrahtung, KEIN Commit.
- GATE-GRUND: neue Tabelle = Architektur-/Produktentscheidung (4-Ausnahmen-Regel: irreversibel). Wartet auf Owner-„ja", dann Migration+Service+Route+UI+Tests in eigenem Slice.

## 2026-06-02 — Skalierungs-Härtung: Admin-Org-Liste paginiert (300-Kunden-Defekt, read+fix, KEIN Commit)
- ANLASS: Skalierungs-Sweep (10→300 Kunden). Muster „bei 10 ok, bei 300 kaputt" gezielt gesucht: plattformweite Entitäts-Liste mit hardcodiertem `LIMIT <n>` ohne Offset/Total/Clamp = stille Datentruncation. `/admin/organizations` zog `LIMIT 200` als Volldump, ohne Pagination — Schwester `/admin/users` war bereits korrekt paginiert. Defekt = die Org-Liste deckelt bei 200 still ab, sobald die Plattform >200 Orgs hat.
- ENTSCHEIDUNG: kein neues Muster — die funktionierende Schwester `/admin/users` 1:1 gespiegelt: `parseIntegerParam`-geklammerte limit (default 50, min 1, max 200) + offset (default 0, min 0, max 500000) + separater `COUNT(*)::int`-Query + `{ items, total, limit, offset }`-Envelope. KEIN Org-Scoping/RBAC-Eingriff hinzugefügt (das wäre Security-Entscheidung = Owner-Gate); nur Pagination, identisches Auth-Gate (requireAdmin) unberührt.
- Geändert (4 Dateien, additiv/spiegelnd): `api/routes/admin.js` (`/admin/organizations` Body), `frontend/public/js/pages/adminPanel.js` (orgsOffset/orgsLimit-State, generisches `renderPaging(total,offset,limit,loadFn)` + dünne Wrapper renderUsersPaging/renderOrgsPaging, `loadOrgs(offset)` async mit try/catch wie loadUsers, ensureTabLoaded), `frontend/public/admin_panel.html` (`#orgsPaging`-Div), `api/test/adminUsers.route.test.js` (Block „GET /admin/organizations": 4 Tests — paginiert+Total+bounded LIMIT/OFFSET, Clamp invalider Params, Cap bei 200, stabiler 500-Envelope).
- SWEEP-ERGEBNIS (Rest der `LIMIT \d+`-Treffer geprüft, KEIN weiterer Defekt dieser Klasse): approvals.js Z.73/109/142/178 (`LIMIT 50`) = org+user-skopierte „my-pending"-Inbox-Widgets (wachsen mit Workload EINER Org, nicht mit Kundenzahl → andere Skalierungsachse); support.js Z.1216 (`LIMIT 10`) = bewusste „10 neueste Cases"-Vorschau in bereits paginiertem Outer-Query (Z.1199 LIMIT/OFFSET); vendorPool.js Z.88 (`LIMIT 20`) = Typeahead/Autocomplete-Cap. → admin-orgs war die EINZIGE plattformweite stille Truncation.
- Checks: node --check admin.js + adminPanel.js OK · admin-Route-Test 11/11 grün · ESLint admin.js + adminUsers.route.test.js exit 0. NICHT im Live-Browser klick-getestet (kein laufender Dev-Server bestätigt) — Code-Ebene verifiziert, UI-Smoke offen.
- Offen: UI-Klicktest der Org-Pagination im Admin-Panel (Owner/Smoke). KEIN Commit (Owner-Gate: erst bei 100%/Marktstart-Reife + persönlicher Owner-Check).

## 2026-06-02 — Incident Kern-Slice umgesetzt (Owner-freigegeben „weiter gehts", KEIN Commit)
- ANLASS: Owner-Greenlight auf den Freigabe-Entwurf (vorheriger Eintrag). Damit ist die Architektur-/Produktentscheidung „neue Tabelle" getroffen → Bau des operativen Incident-Tracks, der die seit Mig 108 vorgezeichnete Lücke (`warp_executions.incident_id` ohne Zieltabelle) schließt. Persistenter open→acknowledged→resolved-Lebenszyklus, plattformweit (SCC Operations), read-only nicht erreichbar (braucht Tabelle).
- MIGRATION (`sql/migrations/121_ops_incidents.sql`): `ops_incidents` als Staff-Ops-Tabelle = KEINE RLS (wie `staff_control_audit_log`/`warp_executions`; org_id NUR informativ, KEINE Tenant-Grenze) + KEINE `audit_id`-Spalte (Audit verlinkt über `staff_control_audit_log` entityType='ops_incident'). gen_random_uuid-PK; CHECK-Enums severity(low/medium/high/critical = warp risk_level)/status(open/acknowledged/resolved)/source(manual/sla/staffing/infra/automation/email); opened_reason NOT NULL; opened/acknowledged/resolved_by → users(id) ON DELETE SET NULL; details JSONB. 3 Indizes (status+created_at DESC, severity, org_id partial). In-File-Rollback `DROP TABLE … CASCADE`. `warp_executions.incident_id`-FK bewusst NICHT verdrahtet (Proposal §6.3, eigener Slice).
- SERVICE (`api/services/staffIncidentService.js`, read+mutate): folgt exakt der etablierten SCC-Linie. Lesepfad `listIncidents` = plattformweites Aggregat via Promise.all (Summen je status + je severity + paginierte Liste), Zero-State by construction, `scope:{platform:true,…}`+`generated_at`; `meta()` Zero-Query. Mutationen geben diskriminiertes `{ok,row|error[,current]}` zurück (wie staffCombinedInbox) — Audit schreibt der ROUTER, NICHT der Service. Statusübergänge streng open→acknowledged→resolved über `transition()` mit `SELECT … FOR UPDATE` in `withTransaction` (kein Doppel-Verarbeiten konkurrierender Operatoren; macht ack/resolve natürlich 2-Statement). Eingangs-Sanitisierung (Title-/Reason-/Note-Längen, Enum-Whitelist, MIN_REASON_LEN=10).
- ROUTEN (`api/routes/staffControlCenter.js`, +5): `GET /incidents` + `/incidents/meta` (requireStaff, read-only). 3 Mutationen `POST /incidents` (201) · `/incidents/:id/acknowledge` · `/incidents/:id/resolve` mit voller Kette `requireStaff, mfaGuard, requireStepUp, requireConfirmAndReason`. Audit-Area `operations`, Actions `staff_control.incident.{open,acknowledge,resolve}`, entityType `ops_incident`, riskLevel `medium` — auf Erfolgs- UND Fehlerpfad (konsistent mit allen SCC-Mutationen). Status-Mapping NOT_FOUND→404, INVALID_TRANSITION/NO_CHANGE→409, sonst 400.
- FRONTEND (`frontend/src/staff/modules/incidents/index.tsx` + Sidebar + AppShell): SCC-Modul „Incidents" (Gruppe Operations). KPI-Grid (Offen/Quittiert/Gelöst/Gesamt) + Severity-Pillenzeile (riskTone) + Status/Severity-Filter + Tabelle mit Per-Row-Aktion (Quittieren wenn open / Schließen wenn acknowledged / „Gelöst <Datum>" wenn resolved). ack/resolve über globales `useConfirm` (reason-only) + `useStepUp`; „+ Neuer Incident" über dediziertes lokales Modal (title/severity/source/reason, `stepUp()`+`sccApi.post`). KEIN neues CSS, nur bestehende `scc-*`-Klassen + Symbole.
- Checks: node --check Service+Route OK · `staffIncidents.test.js` 20/20 (meta, Zero-State, Aggregation, Input-Sanitisierung/Clamp, openIncident inkl. JSON.stringify-Param, ack/resolve happy 2-Query + NOT_FOUND/NO_CHANGE/INVALID_TRANSITION mit korrekter Query-Zahl unter sequencePool/withTransaction) · ESLint neue Dateien 0 Warnings (3× `async` von den Transition-Wrappern entfernt → require-await sauber; Restwarnung `flagRating`/M-07 unberührt) · `npm run build:scc` grün, 64 Module (+1 Incidents-Chunk), TS-Typecheck 0 Errors · 4 SCC-Router-bootende Suites (subscriptionLifecycle/staffSubscriptionRequests.routes/staffCombinedInbox/subscriptionNotifications) 107/107 = kein Regress durch +5 Routen.
- Offen (eigene, owner-gated Slices): `warp_executions.incident_id`-FK verdrahten (§6.3); ALERT_EMAIL bei critical (braucht echten Mail-Key, Gate-50, §6.4); reale org-Tabelle für org_id-FK bestätigen (§6.5); optionale read-only „offene Signale ohne Incident"-Vorschlagsliste aus alerts[]/failed warp_executions/Mail-Failures (§6.2). KEIN Commit (Owner-Gate).

## 2026-06-02 — Incident §6.2: „Offene Signale ohne Incident"-Feed (read-only, KEIN Commit)
- ANLASS: Owner-Greenlight „immer diese Art von Schritten … zukunftssicher, harte Effizienz, wenig Token, max Output". Logischste, billigste Ergänzung zum gerade gebauten Incident-Track = die andere Halbseite des Schließens: dem Operator zeigen, *was er eröffnen sollte*. §6.2 aus dem Proposal — read-only, KEINE Migration, KEIN externer Key, KEINE Mutation.
- ENTSCHEIDUNG (preserve-first, keine Parallelstruktur): den bestehenden `staffIncidentService` + das bestehende Incidents-Modul ERWEITERT statt neue Dateien. Zwei genuin plattformweite, bereits indizierte Quellen (statt teurem N×-Executive-Dashboard für org-skopierte alerts[]): (1) fehlgeschlagene `warp_executions` (`status='failed' AND incident_id IS NULL` — nutzt den seit Mig 108 vorgezeichneten Hook + idx_warp_executions_status/_started); (2) `subscription_notification_log` (`mail_status='failed'`, nach `event_key` aggregiert, nutzt …_created/_event-Index). Beide parallel via Promise.all → deterministische Call-Indizes für Tests.
- SERVICE: `listOpenSignals(pool, {window_hours?, limit?})` → `{available, signals:{warp_failed[],mail_failed[]}, totals:{warp_failed,mail_failed_events,mail_failed_total}, scope:{platform,window_hours,limit}, generated_at}`. Fenster default 7 d (max 30 d), Limit default 20 (max 100), beide geklammert. Jedes Signal trägt einen `suggested`-Block (title/severity/source=infra|email/signal_code) zum Vorbefüllen. Mail-Schweregrad heuristisch nach Häufigkeit (≥10 high, ≥3 medium, sonst low); Warp übernimmt das echte `risk_level`. KEINE Mutation, KEIN Versand.
- ROUTE: `GET /incidents/signals` (requireStaff, read-only, try/catch SCC_INTERNAL_ERROR) — spiegelt exakt `GET /incidents`.
- FRONTEND (`incidents/index.tsx` erweitert): Panel „Offene Signale ohne Incident" (Tabelle Quelle/Signal/Vorkommen/Zuletzt/Aktion) zwischen Schweregrad-Verteilung und Filter; pro Zeile „→ Incident" öffnet das Create-Modal **vorbefüllt** (title/severity/source/signal_code) — Begründung bleibt Pflicht (≥10). Liste + Feed werden via `Promise.allSettled` geladen, damit ein Signal-Fehler die Incident-Liste nicht kippt (Feed verschwindet still). `CreateIncidentModal` um `prefill`-Prop + `signal_code`-Durchreichung erweitert.
- Checks: node --check Service+Route OK · `staffIncidents.test.js` 25/25 (+5: Zero-State, Aggregation+suggested+Totals, gebundene `status='failed'`/`incident_id IS NULL`/`GROUP BY event_key`-SQL + deterministische Call-Reihenfolge, window/limit-Clamp + Garbage-Default, Mail-Severity-Schwellen + null event_key→`unbekannt`) · ESLint Service+Test 0 Probleme, Route nur Restwarnung `flagRating`/M-07 · `npm run build:scc` grün (64 Module, TS 0 Errors) · 4 Router-Boot-Suites 107/107 (kein Regress durch +1 Route).
- Offen (weiterhin owner-gated): §6.3 FK-Verdrahtung, §6.4 ALERT_EMAIL (Gate-50), §6.5 org-FK. KEIN Commit (Owner-Gate).

## 2026-06-02 — Skalierungs-Härtung: Admin-Requests + Strategic-Collaboration paginiert (stiller Seite-1-Defekt, KEIN Commit)
- ANLASS: Fortsetzung Skalierungs-Sweep (10→300 Kunden), Schwester-Defekt zur admin-org-Liste. Zwei plattformweite Operator-Listen — `/admin/requests` (Backoffice-Anfragen) und `/admin/strategic-collaboration/requests` — waren zwar geclamped (limit ≤ 200), gaben aber nur `{ items }` OHNE `total` zurück. Folge: das Operator-UI kann keine Pagination aufbauen und sieht ab Datensatz 51/201 STILL nur immer die erste Seite (kein Fehler, kein Hinweis — restliche Datensätze existieren, sind aber unerreichbar). Anders als die admin-org-Truncation (DB-seitiges Abschneiden) ist das ein UI-seitiger „Seite-1-only", gleiche kanonische Korrektur.
- ENTSCHEIDUNG (preserve-first, kein neues Muster): die bereits korrekte Schwester `/admin/users` / `listOverrides`-Envelope gespiegelt. Service: paralleler `SELECT COUNT(*)::int AS total` mit DERSELBEN where-Klausel (vor limit/offset gebaut → status sitzt auf $1), countParams = `status ? [status] : []`; Count-Query lässt die LEFT JOINs weg (billiger, Prädikat liegt auf der Basistabelle). Return `{ items, total }`. Route: `{ items, total, limit, offset }`-Envelope. Rückwärtskompatibel — bestehende Frontend-Leser von `data.items` brechen nicht. KEIN Org-Scoping/RBAC-Eingriff (Security-Entscheidung = Owner-Gate); Auth-Gate (requireAdmin) unberührt.
- BEWUSST NICHT angefasst: `/admin/feature-overrides` (liefert bereits `total`; limit/offset wäre kosmetisch + Test-Bruch-Risiko), `/admin/audit-log` + `/admin/activity-feed` (liefern bereits `total`).
- Geändert (7 Dateien, additiv/spiegelnd): `api/services/requestService.js` (`listRequestsAdmin` → {items,total}, status uppercased), `api/services/strategicCollaborationService.js` (`listAllRequestsAdmin` → {items,total}), `api/routes/admin.js` (2 Handler → Voll-Envelope), `frontend/public/js/pages/adminPanel.js` (boReq*/sc*-State, renderBackofficeRequestsPaging/renderStrategicPaging via generisches `renderPaging`, `loadBackofficeRequests(offset)`/`loadStrategicRequests(offset)` async mit Paging-Render + catch-Clear, ensureTabLoaded force?0:offset, Post-Mutation-Reloads behalten Seite), `frontend/public/admin_panel.html` (`#boRequestsPaging` + `#strategicPaging` Divs), `api/test/requestService.test.js` (+2 Tests: {items,total}+bounded COUNT+status-uppercase, Clamp 5000→200 + status weglassen), `api/test/strategicCollaborationService.test.js` (Test auf paginiert+total+bounded COUNT umgestellt).
- Checks: ESLint 5 JS-Dateien exit 0 · node --check alle Backend + adminPanel.js OK · betroffene Unit-Suites 59/59 grün (requestService + strategicCollaborationService + adminUsers.route). NICHT im Live-Browser klick-getestet (kein laufender Dev-Server bestätigt) — Code-Ebene verifiziert, UI-Smoke offen.
- Offen: UI-Klicktest beider Paginierungen im Admin-Panel (Owner/Smoke). KEIN Commit (Owner-Gate: erst bei 100%/Marktstart-Reife + persönlicher Owner-Check).

## 2026-06-02 — Skalierungs-Härtung: Usage-Limit-Scan paginiert (unbegrenzter Cron-Voll-Scan, KEIN Commit)
- ANLASS: Fortsetzung Skalierungs-Sweep (10→300 Kunden). `scanAndEnforceUsageLimits(pool)` (usageMeteringService.js, aufgerufen vom Cron `POST /internal/usage-limit-scan`) war der **einzige** /internal-Sweep OHNE Batch-Grenze: ein `SELECT … FROM users u LEFT JOIN LATERAL(subscriptions) WHERE is_demo=FALSE` OHNE LIMIT/OFFSET, danach JS-`for`-Loop über ALLE Zeilen. Kosten wachsen linear mit der Kundenzahl (klassisch „ok bei 10, bricht bei 300" — alle Schwester-Sweeps recompute-compliance/worker-document-expiry/subscription-lifecycle clampen längst `Math.min(500, …||100)`).
- ENTSCHEIDUNG (preserve-first, kanonische Spiegelung): Service-Signatur → `scanAndEnforceUsageLimits(pool, {limit?, offset?})`, geklammert (`USAGE_SCAN_DEFAULT_LIMIT=100`, `USAGE_SCAN_MAX_LIMIT=500`, offset ≥0), Query `ORDER BY u.id LIMIT $1 OFFSET $2` (stabile Sortierung = deterministisches Paging), Envelope `{scanned, over_limit, has_more, next_offset}` (Cron kann selbst weiterblättern). Default-Bound greift auch bei parameterlosem Aufruf → safe-by-default. KEINE Migration, KEINE Mutation an Bestandsdaten, rückwärtskompatibel (einziger Caller = internal.js).
- Geändert (3 Dateien): `api/services/usageMeteringService.js` (Batch-Bound + Paging-Envelope + 2 Konstanten), `api/routes/internal.js` (`usage-limit-scan` clamped `batch_size`≤500 + `offset`, reicht `{limit,offset}` durch, loggt beide), NEU `api/test/usageMetering.test.js`.
- Checks: node --check 3 Dateien OK · `usageMetering.test.js` 9/9 grün (Default-LIMIT/OFFSET-Bindung, Clamp MAX/Garbage/0→Default + neg. offset→0, over_limit-Zählung DEMO=0/BASIS=5/PRO=-1-unlimited, has_more+next_offset Paging, Graceful-Catch Zero-Envelope, unbekannter Plan→FREE/DEMO) · ESLint 3 Dateien exit 0 (keine neue Warnung).
- Offen: keine — abgeschlossen. KEIN Commit (Owner-Gate: erst bei 100%/Marktstart-Reife + persönlicher Owner-Check).

## 2026-06-03 — Phase J / Block 2 (SCC-Scope): Ultra-Premium im Operator-Shell + Tier-2 Env-Kill-Switch (KEIN Commit)
- ANLASS: Block-1 lieferte das `ultra_premium`-Theme nur für die Plattform (design-system.css). Der SCC-Operator-Shell hatte einen EIGENEN, parallelen dark/light-Toggle (`Topbar.tsx`), der die Block-1-Registry NICHT kannte → ultra_premium war im Shell, das der Owner täglich nutzt, unerreichbar. Block-2-Scope laut PHASES_A_TO_R.md §J: „SCC Theme Control (Owner): aktives Theme je Bereich sehen/ändern … + Env THEME_SWITCHER_ENABLED/ULTRA_PREMIUM_THEME_ENABLED".
- SCOPE-ENTSCHEIDUNG (bewusst eingegrenzt): NUR der **scc-Scope** wird geliefert — ein einzelnes Vite-App, in dem das Theme via `data-theme` trivial & vollständig wirkt (kein Architektur-Risiko). Die Scopes **platform/worker_portal** bleiben Owner-gated: sie brauchen einen Runtime-Config-Auslieferungsweg an ~80 statische HTML-Seiten (kein Shared-Head/Bootstrap vorhanden) — das ist eine Architekturentscheidung mit Wirkung auf 20+ Folgeprojekte → NICHT autonom gebaut, kein toter Schalter, keine Migration auf Verdacht.
- DESIGN (preserve-first, kein neues CSS-System): `scc.css` um `[data-theme="ultra_premium"]`-Block ergänzt — exakt dieselben 22 `--scc-*`-Tokens wie der `:root`/light-Block, Palette harmonisiert mit Block-1 „Obsidian Sapphire" (Grund #05080f, Saphir-Akzent #5fa8ff, ruhige Enterprise-Tiefe). Self-contained (alle --scc-* überschrieben → kein Mischmasch mit dem dark-Default).
- TIER-2 ENV-KILL-SWITCH (Config-Taxonomie, end-to-end für scc): `config.THEME_SWITCHER_ENABLED` + `ULTRA_PREMIUM_THEME_ENABLED` (Default-AN-Semantik: nur explizit `false/0/no/off` schaltet ab) → SCC-Bootstrap (`staffControlCenter.js` `\bootstrap` `data.theme={switcher_enabled,ultra_premium_enabled}`) → SCC-Shell. KEIN statische-Seiten-Injektionsproblem, weil der Weg über das React-Bootstrap läuft.
- FRONTEND: `main.tsx` Pre-Paint-Init akzeptiert nun auch gespeichertes `ultra_premium` (kein Flash). `Topbar.tsx`: der parallele 2-State-Toggle → Registry-Zyklus dark→light→ultra_premium, gegated durch Bootstrap-Flags (Switcher aus → Toggle unsichtbar; Ultra-Premium aus → nur dark/light); `useEffect` setzt ein serverseitig deaktiviertes gespeichertes Theme auf `dark` zurück. Persistenz unverändert `localStorage['scc-theme']`. `types/bootstrap.ts`: `theme?`-Feld ergänzt.
- Checks: node --check config+route OK · NEU `api/test/themeFlags.config.test.js` 4/4 (Default-AN, explizit-false-aus, off/0/no vs. nicht-gelistet, Trim/Case via cache-bustendem ESM-Query-Import) · `staffSubscriptionRequests.routes.test.js` 12/12 (SCC-Router bootet mit neuem config-Import sauber) · ESLint config+test 0 Probleme, Route nur Restwarnung `flagRating`/M-07 · `npm run build:scc` grün (tsc 0 Errors, 64 Module).
- Offen (Owner-gated, eigene Slices): platform/worker_portal-Theme-Scopes (Runtime-Config-Auslieferung an statische Seiten = Architekturentscheidung); „Vorschau/Reset/Audit + je-Bereich-Persistenz" als SCC-Control-Modul (braucht Settings-Store/Migration). KEIN Commit (Owner-Gate).

## 2026-06-03 — Skalierungs-Sweep (HTTP-Listen) abgeschlossen + Release-Ack N+1 entfernt (KEIN Commit)
- ANLASS: Abschluss der Skalierungs-Sweep-Serie (10→300 Kunden), dritte/gefährlichste Variante der Listen-Defekt-Familie: plattformweiter Read GANZ OHNE `LIMIT` (komplette Tabelle über HTTP). Methode: alle `list*`/`getAll*`/`search*`-Service-Funktionen enumeriert + jede gegen den Skalierungs-Diskriminator geprüft (wächst die Liste mit der Kundenzahl?) + `LIMIT`-Grep über services/.
- ERGEBNIS Sweep (VERIFIKATION, kein Neubau): ALLE kundenskalierenden HTTP-Listen sind bereits sauber gebunden (clampLimit / `Math.min(200,…)` / `COUNT(*) OVER()` / explizites LIMIT). Die einzige verbleibende unbegrenzte HTTP-Quelle ist `productReleaseService`: `listVisibleForUser` (Hot-Path via getInboxSummary) + `listAllAdmin` — beide sind **editoriale Volumina** (Release-Notes, wächst mit Owner-Redaktion, NICHT mit Kundenzahl) → KEIN akuter Defekt. `listVisibleForUser` filtert zudem Sichtbarkeit/Plan/Audience in JS NACH dem Read → ein naives `LIMIT` vor dem Filter wäre ein KORREKTHEITS-Bug (zu wenige Zeilen); echte Lösung = Filter in SQL pushen = leak-sensitiv → Owner-gated, NICHT autonom.
- ISOLIERTER FIX (behavior-preserving): `markAllSeenForUser` (productReleaseService.js:208) war ein N+1-Write-Loop (`for (id of ids) await insertOne(id)`). → ein einziges Bulk-`INSERT … SELECT $1, rid, NOW() FROM UNNEST($2::uuid[]) AS rid ON CONFLICT (user_id,release_id) DO UPDATE SET seen_at = COALESCE(…)`. Spaltentyp via Mig 063 verifiziert (user_id/release_id beide UUID → `::uuid[]` typsicher). Verhalten identisch (nur published+sichtbare Einträge, COALESCE bewahrt bestehendes seen_at).
- Geändert (2 Dateien): `api/services/productReleaseService.js` (markAllSeenForUser N+1→Bulk-UNNEST), `api/test/productReleaseService.test.js` (+2 Tests „bulk ack, no N+1": recordingPool zählt genau 4 Queries statt 4+N, prüft UNNEST-SQL + Params; + Null-Visible→kein INSERT).
- Checks: node --check OK · `productReleaseService.test.js` 8/8 grün (war 6) · ESLint exit 0. NICHT im Live-Browser getestet (Code-Ebene verifiziert).
- HINWEIS (operativ): während dieser Slice lief eine PARALLELE Claude-Session auf demselben Branch/Worklog (Einträge „Usage-Limit-Scan paginiert" + „Phase J / Block 2" oben sind von ihr) — KEINE Datei-Überschneidung mit dieser Slice (productReleaseService.js + Test vs. usageMeteringService/internal/SCC-Theme).
- Offen (deferred, Owner-gated): `listVisibleForUser`/`getInboxSummary` Sichtbarkeits-Pushdown in SQL (Korrektheits-/Leak-kritisch); `listAllAdmin` Admin-Pagination (editoriales Volumen, niedrige Prio). KEIN Commit (Owner-Gate: erst bei 100%/Marktstart-Reife + persönlicher Owner-Check).

## 2026-06-03 — R5-Nachzug: Theme-Tier-2-Env in .env*.example + envValidator (KEIN Commit)
- ANLASS: Phase J Block 2 hat `THEME_SWITCHER_ENABLED` + `ULTRA_PREMIUM_THEME_ENABLED` in `config/index.js` code-verdrahtet. R5 (Provider-/Flag-Env-Modell) hielt genau diesen Schritt offen: „THEME_SWITCHER_ENABLED (J) — erst bei Code-Verdrahtung". Jetzt verdrahtet → die Vars gehören dokumentiert (.env*.example) + validiert (envValidator), sonst Drift Doku↔Code + stiller Footgun.
- ENTSCHEIDUNG (preserve-first, kanonische Spiegelung des BILLING_PROVIDER/EMAIL_PROVIDER-Musters): `envValidator.js` — beide Flags als optionale `z.string()` + superRefine-Schleife über `THEME_FLAG_KEYS` gegen `THEME_FLAG_TOKENS=[true,false,1,0,yes,no,on,off]`. Bewusst gegen Boolean-Tokens validiert (nicht nur „bekannt/unbekannt" wie Provider): die config-Parse-Semantik ist `!["false","0","no","off"].includes(...)` = ALLES außer den vier Off-Tokens → AN. Ein Tippfehler wie `THEME_SWITCHER_ENABLED=nein`/`=disabled`/`=aus` würde also STILL auf AN auflösen (Whitelabel-Abschaltung schlägt unbemerkt fehl) → Fail-Fast beim Boot statt Silent-On. Leer/ungesetzt passiert (= Default-AN).
- ENV-DOKU: in allen 3 `.env*.example` ein „THEME SYSTEM (Tier-2 Env-Kill-Switch)"-Block, beide Flags auskommentiert (default-AN, nur zum Abschalten setzen). ASCII-transliteriert (waehlbar) konsistent zu den restlichen .env-Kommentaren. prod: Whitelabel/Abschaltung-pro-Instanz-Hinweis; dev: „lokal alle Themes testbar" + Token-Liste.
- Geändert (5 Dateien, additiv): `api/config/envValidator.js` (2 Konstanten + 2 Schema-Keys + superRefine-Schleife), `.env.example` / `.env.dev.example` / `.env.prod.example` (THEME-Block), `api/test/envValidator.test.js` (+5 Tests, 13→18).
- Checks: node --check OK · `envValidator.test.js` 18/18 grün (gültige Tokens, leer=Default-AN, `=nein`-Footgun→throw, `enabled`→throw, Trim+Case `  OFF  `/` No `) · ESLint config+test exit 0 (keine neue Warnung).
- Offen: keine — schließt die THEME-Hälfte von R5. Verbleibend in R5 (nicht verdrahtet): INFRASTRUCTURE_PROVIDER (G), AI_OPS_ENABLED (H). KEIN Commit (Owner-Gate).

## 2026-06-03 — Skalierungs-Sweep (N+1-Write-Loops): SLA-Cron-Scans set-based (KEIN Commit)
- ANLASS: Fortsetzung der Skalierungs-Sweep-Serie (10→300 Kunden), zweite Defekt-Klasse nach den HTTP-Listen: N+1-Write-Loops in Cron-Jobs (sequentielles `await <write>` pro Zeile, Round-Trips wachsen mit Daten-/Kundenvolumen). Methode: sub-agent-Triage über 23 Service-Dateien gegen den Diskriminator „skaliert die Loop-Länge mit Kunden/Daten?" → einzige überlebende kundenskalierende Klasse: die SLA-Scan-Cron-Familie. Befund direkt am Code verifiziert (nicht der Zusammenfassung vertraut).
- ISOLIERTER FIX (behavior-preserving, 2 saubere nicht-verzweigte Scans): `searchSlaScan` (slaSearchService.js) + sein verbatim-Spiegel `demandSlaScan` (marketplaceService.js) waren `SELECT … LIMIT $1` + `for (row of rows)` mit pro-Zeile `UPDATE … WHERE id=$1 AND sla_status='RUNNING' RETURNING id` + bedingtem Event-Insert (1 SELECT + 2N Writes). → je ein einziges geschütztes `UPDATE … SET sla_status='BREACHED' WHERE sla_status='RUNNING' AND id IN (SELECT … LIMIT $1) RETURNING id` + ein Bulk-`INSERT … SELECT id,'SLA_BREACHED',$2::jsonb FROM UNNEST($1::uuid[])` (2 Statements statt 1+2N).
- CONCURRENCY-ERHALT (kritisch): der `sla_status='RUNNING'`-Guard steht bewusst AUF dem äußeren UPDATE (nicht nur im LIMIT-Subselect) — bei überlappenden Cron-Läufen scheitert das Re-Check der zweiten Runde für bereits geflippte Zeilen → keine doppelten `SLA_BREACHED`-Events (identische No-Duplicate-Semantik wie das alte `rowCount>0`-Pattern, ohne FOR UPDATE, da das Original ebenfalls nicht lockte). Spaltentypen verifiziert: `sla_search_jobs.id`/`demand_requests.id` UUID, beide *_sla_events `payload JSONB` + CHECK enthält `SLA_BREACHED` (Mig 014/015) → `UNNEST($1::uuid[])` + `$2::jsonb` typsicher.
- BEWUSST NICHT angefasst (verzweigt / nicht kundenskalierend, dokumentierter Follow-up): `slaService.js` `slaScan` (per-Zeile BEGIN/COMMIT) + `notdienstEscalationScan` (verzweigt lvl 0→1 vs 1→2), `marketplaceService.js` `notdienstEscalationDemands` (verzweigt, `LEAST(...)`) — nicht sauber bulkbar ohne Verhaltensänderung; kein Misch-Fix auf Verdacht.
- Geändert (3 Dateien): `api/services/slaSearchService.js` (searchSlaScan N+1→set-based), `api/services/marketplaceService.js` (demandSlaScan N+1→set-based), `api/test/marketplaceService.test.js` (demandSlaScan-Block neu: recordingPool zählt genau 2 Queries, prüft RUNNING-Guard+LIMIT+RETURNING im UPDATE, UNNEST+SLA_BREACHED+ids im Insert, 0-Breach=1 Query/kein Insert). NEU: `api/test/slaSearchService.test.js` (searchSlaScan war komplett ungetestet — 4 Tests: 2-Query-Set-Based, 0-Breach-skip-Insert, Cap 500, Default 100).
- Checks: node --check 4 Dateien OK · `slaSearchService.test.js` + `marketplaceService.test.js` zusammen 52/52 grün · ESLint 4 Dateien exit 0 (keine neue Warnung). NICHT im Live-Browser getestet (Cron-Pfad, Code-Ebene verifiziert).
- Offen (deferred, dokumentiert): die 3 verzweigten Scans oben (eigener Slice, falls je nötig — niedrige Prio, da Escalation-Volumina klein). KEIN Commit (Owner-Gate: erst bei 100%/Marktstart-Reife + persönlicher Owner-Check).

## 2026-06-03 — Phase Q / Index-Review (Cron-Sweep-Stütz-Indizes, Mig 122, KEIN Commit)
- ANLASS: Dritte Skalierungs-Defektklasse nach HTTP-Listen + N+1-Write-Loops: **Cron-Sweeps ohne Stütz-Index**. Ein Praedikat ohne Index ist bei 10 Kunden ein billiger Seq-Scan, bei 300 ein voller Table-Scan + Sort bei JEDEM Tick (api/routes/internal.js, ~20 Timer-Sweeps). Phase Q „Index-Review" (Gate 100) der Readiness-Matrix.
- METHODE (trust-but-verify): sub-agent-Audit erwies sich als unzuverlässig (behauptete fälschlich „webhook_deliveries hat nur FK-Index" — Mig 047 hat zwei; zitierte Staffing-Indizes falsch). Daher JEDE Lücke direkt gegen Boden-Wahrheit verifiziert: Sweep-Query im Service + Tabellen-/Index-Definition in der jeweiligen Migration gelesen, bevor ein Index geschrieben wurde. Kein Index „auf Verdacht".
- NEU: `sql/migrations/122_cron_sweep_indexes.sql` — 5 verifizierte Partial-Indizes (reines Performance-Add-on, kein Schema-/Verhaltenswechsel, add-only, IF NOT EXISTS idempotent, Rollback-Block mit DROP INDEX je Index):
  1. `webhook_deliveries(next_retry_at) WHERE status='failed'` — retryFailedDeliveries; Bestand (Mig 047 webhook_del_retry_idx) ist partiell auf `status IN ('pending','retrying')`, schließt `failed` aus.
  2. `assignment_staffing_invites(expires_at) WHERE expires_at IS NOT NULL AND status IN ('sent','viewed','interested','accepted')` — expireStaleStaffingState (Invites, unbounded); Bestand (Mig 087) führt mit assignment_id/worker_user_id. Status-Set spiegelt exakt den partiell-UNIQUE „offener Invite" (Mig 087 Z.182).
  3. `assignment_staffing_reservations(expires_at) WHERE expires_at IS NOT NULL AND status='reserved'` — expireStaleStaffingState (Reservierungen, unbounded); gleiche Lücke.
  4. `demand_requests(created_at) WHERE status='open' AND sla_status='RUNNING' AND urgency='notdienst'` — notdienstEscalationDemands (ORDER BY created_at); Bestand (Mig 014 sla_scan_idx) führt mit sla_due_at, kein urgency, erzwingt Sort. Partial deckt nur das kleine Notdienst-Subset → winziger Index.
  5. `sla_search_jobs(created_at) WHERE status='open'` — runSearchJobsBatch (WHERE status='open' ORDER BY created_at); Bestand (Mig 015) führt mit owner/target bzw. ist partiell auf sla_status='RUNNING' → deckt offene MET/BREACHED/NULL-Jobs nicht, voller Scan+Sort.
- BEWUSST NICHT gebaut (dokumentierte Folge-/Owner-Entscheidungen): (a) recompute-compliance requests-Sweep = Designschwäche (re-sortiert ganze requests-Tabelle ohne WHERE), richtiger Fix ist Bounding (Verhaltensänderung), kein Index. (b) notdienst-escalate requests.priority — Spalten-Provenienz unbestätigt + Write-Amplification auf heißer Kerntabelle, erst Owner-Klärung. (c) webhook-cleanup created_at — Tabelle selbstbegrenzend (~30 Tage), Insert-Kosten ohne ROI.
- KONSISTENZ: plain `CREATE INDEX` in BEGIN/COMMIT (NICHT CONCURRENTLY — im Txn-Block unmöglich; Mig-Runner sql/migrate.sh wendet jede Datei file-level-idempotent via _migrations-Tabelle an, lexikalisch sortiert → 122 nach 121). Header/Rollback-Konvention exakt wie Mig 121.
- Checks: alle 5 Spalten + Status-Werte gegen Mig 014/015/047/087 verifiziert (existieren, CHECK-Subsets gültig), Indexnamen kollidieren mit keiner Bestandsmigration (Grep über sql/migrations), Rollback-Block vollständig. Kein SQL-node--check (nicht existent) → strukturelle Verifikation. KEIN Commit (Owner-Gate).

## 2026-06-03 — Phase Q / Index-Review ABGESCHLOSSEN (Rest-Sweeps geprüft + BRIN Mig 123, KEIN Commit)
- ABSCHLUSS des Cron-Sweep-Audits: nach Mig 122 (5 Status-Prädikat-Lücken) die **restlichen ~12 Sweeps** in internal.js am echten Code + Quell-Migration geprüft. Diskriminator: „wächst die gescannte Menge unbegrenzt mit Kunden/Daten?" Ergebnis — der Codebase ist für Cron-Sweeps bereits sehr gut indiziert (zweckgebaute Partials in Mig 009/011/013/030/076):
  - invoice-overdue-scan (`markOverdueInvoices`, UPDATE WHERE status='issued' AND due_at<NOW()) → **gedeckt** Mig 030 `invoices(status, due_at)` (exaktes Komposit).
  - expire-reservations (`expireReservationsBatch`, WHERE status='active' AND expires_at<NOW() ORDER BY expires_at) → **gedeckt** Mig 009 `capacity_reservations(expires_at) WHERE status='active'` (genau die Gold-Standard-Partial-Form).
  - cleanup-idempotency (`cleanupExpired`, WHERE expires_at<NOW()) → **gedeckt** Mig 012 `idempotency_keys(expires_at)`.
  - worker-document-expiry-scan (`scanWorkerDocumentDeadlines`, WHERE status='verified' … ORDER BY valid_until) → **gedeckt** Mig 076 `worker_profile_documents(status, valid_until) WHERE status='verified' AND valid_until IS NOT NULL` (zweckgebaute Steering-Migration für genau diesen Scan).
  - sla-scan (`slaService.slaScan`, WHERE status='SENT' AND sla_status IN('OK','RUNNING') AND sla_respond_by<NOW() ORDER BY sla_respond_by) → **gedeckt** Mig 013 `requests(receiver_id, sla_respond_by) WHERE sla_status IN('OK','RUNNING') AND status='SENT'` (Partial-WHERE exakt deckungsgleich). Führende receiver_id erzwingt zwar einen Sort, ABER über das **selbstbegrenzende SLA-Arbeitsset** (SENT+OK/RUNNING-Zeilen brechen/lösen sich binnen ~60min auf → Größe = Durchsatz×SLA-Fenster, NICHT kumulative Zeilen). Kein neuer Index auf die heißeste Schreib-Tabelle des Systems nur um einen Sort über ein kleines Set zu sparen (Write-Amplification > Nutzen).
  - subscription-lifecycle-tick (5 Sub-Sweeps `expire/activate/cancel/trialEnds/hardLocks`) → **kundengrößen-cardinality** (subscriptions ~1/Kunde, subscription_requests wenige/Kunde → wenige Tsd. Zeilen bei 300 Kunden, alle clampBatch-LIMIT-begrenzt). Scan trivial, KEIN Index nötig (Diskriminator: wächst nicht event-, nur kundenproportional).
  - pilot-expiry (`expireStalePilots`, UPDATE organizations) → **organizations = Kunden-Cardinality** (~300 Zeilen max), Full-Scan trivial → kein Index.
  - recompute-supplier-metrics / product-analytics-rollup / infrastructure-snapshots-ingest → Aggregat/zeitfenster-begrenzt bzw. INSERT-Ingest (kein wachsender Scan).
- EINZIGE echte Rest-Lücke = die **event-cardinality** Tabelle `product_analytics_events` (wächst mit Plattform-Nutzung, nicht Kundenzahl). ZWEI Sweeps scannen sie nur über occurred_at: product-analytics-retention (`cleanupAnalyticsRetention` DELETE occurred_at<cutoff) + product-analytics-rollup (`runDailyAnalyticsRollup` SELECT occurred_at>=NOW()-N, mehrfach). Bestand (Mig 066/068): 8 Indizes, ALLE mit occurred_at als ZWEITER Spalte → keiner führt mit Zeit → Full-Scan.
- NEU: `sql/migrations/123_analytics_events_time_brin.sql` — **BRIN** (nicht btree) auf `product_analytics_events(occurred_at)`. Begründung (Blueprint-Muster): occurred_at ist append-only/zeitkorreliert → BRIN ist winzig + belastet den HEISSEN Event-Insert-Pfad praktisch nicht (nur Block-Range-Summaries, keine Per-Zeile-Wartung), während DELETE (alt) und Rollup (jung) nicht-passende Block-Bereiche überspringen. Ein btree(occurred_at) wäre die falsche Wahl (Write-Amplification auf der volumenstärksten Insert-Tabelle nur für einen täglichen Wartungs-Scan). Add-only, IF NOT EXISTS, Rollback-Block, Header/Konvention wie 121/122.
- Checks: occurred_at-Spalte + Tabellen-/Index-Bestand gegen Mig 066/068 verifiziert (kein führender Zeit-btree, kein BRIN vorhanden), beide Sweep-Queries am Service bestätigt (retention DELETE + rollup mehrfach-SELECT), Indexname kollisionsfrei, BEGIN/COMMIT/CREATE/COMMENT/DROP balanciert. KEIN Commit (Owner-Gate). → Phase Q Cron-Sweep-Achse damit vollständig abgearbeitet (gefixt 122+123, Rest dokumentiert gedeckt/low-risk, 3 Items als Folge-/Owner-Tickets deferred).

## 2026-06-03 — Phase O / Test-Sicherung der Skalierungs-Index-Slices (Mig 122 + 123, KEIN Commit)
- ANLASS: Die Index-Migrationen 122 (5 btree-Partials) + 123 (1 BRIN) waren ohne Laufzeit-Test. Reine Performance-Add-ons haben keinen Mock-Pool-prüfbaren Verhaltens-Effekt — der einzige sinnvolle Test ist die Verifikation gegen das REALE Schema NACH Migrations-Anwendung.
- ENTSCHEIDUNG (zukunftssicher, low-brittleness): KEIN DB-freier Struktur-Meta-Test über die Migrationsdateien (würde Legacy-Migrationen vor der 121er-Konvention false-flaggen) und KEIN EXPLAIN-Nutzungstest (planer-/datenabhängig, brüchig auf leeren Tabellen). Stattdessen ein **DB-gestützter Index-Existenz-/Form-Test** im bestehenden `test/integration/`-Pfad (real `pg.Pool` via `createPool`, `skip: !hasDb`).
- NEU: `api/test/integration/scaling-indexes.flow.test.js` — introspektiert `pg_indexes.indexdef` für alle 6 neuen Indizes und prüft je: Existenz (sonst Migration nicht angewendet / Index-/Tabellenname falsch), Zugriffsmethode (btree vs. **BRIN** — plus eigener Assert, dass der Analytics-Index NICHT versehentlich btree ist → schützt die bewusste Insert-Pfad-Schonung), Tabelle, Spalte und jedes Partial-Praedikat-Fragment. Robuste Teilstrings gegen pg-Normalisierung: `spalte = 'wert'` (überlebt `::text`-Cast), IN-Werte einzeln (`'sent'`/`'viewed'`/… statt der zu `= ANY (ARRAY[…])` normalisierten IN-Syntax), `expires_at IS NOT NULL`.
- WIRKUNG: Läuft im CI-`integration`-Job (Postgres 16, nach allen Migrationen) als echtes Sicherheitsnetz — fängt Spalten-/Tabellen-/Zugriffsmethoden-Tippfehler in den Migrationen ab (die sonst still einen funktionslosen Index erzeugen). Skippt sauber im DB-freien `unit`-Job. Verifiziert: `node --check` ok + Lauf ohne DB = Block „No database configured", 0 fail / 0 pass. KEIN Commit (Owner-Gate).

## 2026-06-03 — Phase P / Restore-Drill-Fidelity (R8): Drill an echten Restore angeglichen (KEIN Commit)
- ANLASS (R8 — Disaster-Recovery-Probe): Backup/Restore-Harness existiert bereits reif (`scripts/backup.sh`, `backup-verify.sh`, `restore.sh`, `restore-test.sh` + Docs BACKUP/BACKUP_DISASTER_RECOVERY). KEIN Neubau (inkrementell, keine Parallelstruktur). Audit gegen den Diskriminator „gibt der Drill ECHTE Wiederherstell-Konfidenz?" → ein Fidelity-Bug.
- BEFUND: `restore.sh` (echter Restore) nutzt `pg_restore --single-transaction` (atomar, bricht bei Fehler ab). `restore-test.sh` (der Drill, der genau das validieren soll) nutzte SCHWÄCHERE Flags (nur `--no-owner --no-privileges`). Folge: pg_restore überspringt non-fatale Fehler und endet mit Exit 0 → der Drill konnte einen nur TEILWEISE eingespielten Dump fälschlich als „bestanden" melden. Der Drill testete damit eine ANDERE Operation als die im Ernstfall → klassischer „ok bei 10, Katastrophe bei 300"-Blindfleck (am Tag des Restores merkt man, dass das Backup nie vollständig restaurierte).
- FIX: `scripts/restore-test.sh` pg_restore-Aufruf um `--single-transaction --exit-on-error` ergänzt (+ Kommentar, dass der Drill die Flags des echten Restores spiegeln MUSS). Drill ist jetzt atomar + fail-loud — besteht der Drill, besteht auch `restore.sh`; schlägt er fehl, ist ein reales Backup-Problem VOR dem Ernstfall gefangen. Triage: Bug (Drill nicht faithful), kein Ausbau. Verifiziert: `bash -n` → SYNTAX_OK.
- WIRKUNG/Blueprint (20 Folgeprojekte): „Der Restore-Drill MUSS dieselben pg_restore-Flags nutzen wie der echte Restore, sonst validiert er die falsche Operation." Add-only Verhaltens-Härtung des Drills, keine Datenberührung. KEIN Commit (Owner-Gate).
- FOLGE-BEOBACHTUNG (low-severity, NICHT in diesem Slice gefixt — kein Misch-Diff): `scripts/backup-verify.sh` ~Z.164 prüft `if [ "$?" -eq 0 ]` NACH einer `VAR=$(tar … | wc -l | tr -d ' ')`-Zuweisung. Unter `set -euo pipefail` bricht das Skript bei beschädigtem tar bereits an der Zuweisung ab → der „fail"-Zweig ist toter Code. Der Fehler wird zwar erkannt (non-zero Exit), aber ohne saubere Zusammenfassung. Eigenes Mini-Ticket.

## 2026-06-03 — Phase P-Test / Severity-Matrix-Pinning für infrastructureSnapshotService (R8, KEIN Commit)
- ANLASS: Repo-genaue Verifikation des Backup-Frühwarnsignals (R8) ergab: Backup-Staleness IST bereits ein First-Class-Health-Signal — der Host-Collector liefert `backup_age_h` (aus `.last_success_epoch`), `infrastructureSnapshotService.computeRiskState()` stuft es zu critical/high/medium (Schwellen 12/24/48h) und schreibt es nach `warp_hosts` + OCC-Infra-Modul. KEIN Feature zu bauen — aber der Service hatte NULL Testabdeckung (Glob `*napshot*`/`*nfrastructure*` unter `api/test/` = leer). Echte Lücke = fehlende Absicherung der Schwellen, die das R8-Signal definieren.
- NEU: `api/test/infrastructureSnapshotService.test.js` — DB-freier Unit-Test (läuft im unit-Job, kein DB nötig) über den einzigen Export `ingestInfrastructureSnapshots` mit branching `recordingPool` (INSERT→id, UPDATE warp_hosts→leer; erfasst sql+params). Pinnt fest:
  1. **Backup-Staleness-Boundaries (R8-Kern):** 49→critical, 48→high, 25→high, 24→medium, 13→medium, 12→ok, 6→ok, 0→ok (+ host_status degraded/warning/healthy). Die strikten `>`-Grenzen (48 ist NICHT >48 → high, 24 NICHT >24 → medium, 12 NICHT >12 → ok) sind als Boundary-Tests fixiert — dürfen nicht still wegrefactored werden. Plus: absenter `backup_age_h` = null = neutral (triggert KEIN Backup-Risiko).
  2. **Übrige Signal-Matrix** (cpu/ram/disk/docker_unhealthy/tls) je critical/high/medium + gesund=ok/healthy + gar-kein-Signal=unknown/unknown + `docker_running_count>0` zählt als Signal→ok.
  3. **Input-Härtung (Produktionspfeiler 6 / Zero-State):** Prozent>100→100, <0→0; nicht-numerisches/negatives `backup_age_h`→null; numerische Count-Strings→Integer, Garbage→Fallback 0; unbekanntes env→'production', gültiges→lowercase; ungültiges `collected_at`→ISO-Fallback (kein null in NOT-NULL); metadata.source spiegelt Ingest-Quelle (internal_collector default / host_collector via option).
  4. **Validierung & Aggregat:** Nicht-Objekt→ok:false INVALID_SNAPSHOT_OBJECT (kein Throw); fehlender host_name→MISSING_HOST_NAME; Nicht-Array→sicheres leeres Aggregat; gemischte Charge zählt inserted/failed/critical_hosts korrekt; DB-Fehler wird pro Host gefangen (ok:false), nie geworfen.
- Verifiziert: `node --test --test-force-exit` → **36/36 grün, 0 fail** beim Erstlauf (Boundary-Erwartungen waren korrekt). KEIN Commit (Owner-Gate). Blueprint-Wert: das Severity-Matrix-Pinning ist 1:1 auf jedes Folgeprojekt mit Host-Health-Ingest übertragbar.

## 2026-06-03 — Skalierungs-Sweep (N+1-Read-Loops): Support-Org-Lookup batched (KEIN Commit)
- ANLASS: Fortsetzung der Skalierungs-Defekt-Serie (10→300 Kunden) — Schwester-Achse zum bereits gesweepten N+1-Write-Loop: N+1-READ-Loops im HTTP-Request-Pfad (eine SELECT je Zeile zum Zusammenbauen einer Response, Round-Trips wachsen mit Daten). Methode: Explore-Sub-Agent enumerierte Loop-Kandidaten, JEDER gegen den Diskriminator „wächst die Loop-Länge mit Kunden/Daten?" am echten Code verifiziert (trust-but-verify: der Agent labelte 3 N+1-WRITE-Loops fälschlich als Read + nannte den Route-Pfad leicht falsch → nur 1 echter Read-Defekt überlebte die Prüfung).
- BEFUND (1 echter Defekt): `GET /support/lookup/orgs` (routes/support.js) lud eine Seite Orgs (per_page bis 100) und feuerte DANN `for (row of pageRows) await pool.query(recent-cases … LIMIT 10)` — bei voller Seite bis zu 100 sequentielle SELECTs PRO Request. Bei 10 Kunden billig, bei 300 + aktiven Support-Agents ein Round-Trip-Sturm.
- FIX (behavior-preserving, batched): den Per-Org-Block in eine EXPORTIERTE Helper-Funktion `loadRecentOpenCasesByOrg(pool, orgIds, agent)` gezogen, die ALLE Orgs der Seite in EINER Query bedient: `${createCaseBaseSelect()} WHERE reporter_org_id = ANY($1::uuid[]) AND <scope> AND status NOT IN('resolved','closed')` als innere Subquery, gewrappt in `ROW_NUMBER() OVER (PARTITION BY reporter_org_id ORDER BY updated_at DESC, id DESC)`, außen `WHERE rn <= 10`. Rückgabe Map<org_id, rawRow[]> (je Liste jüngste-zuerst). `buildScope` wird EINMAL statt pro Zeile aufgerufen (identische Scope-Bedingungen). Handler mappt dann `(recentByOrg.get(row.id) || []).map(caseSummaryFromRow)` — Maskierung/Shaping bleibt im Handler, da caseSummaryFromRow die Rolle aus `req` braucht. Output-Shape byte-identisch (org_id_masked/context/open_case_count/recent_cases). `id DESC` nur als deterministischer Tie-Breaker (Verbesserung, kein Verhaltenswechsel der Semantik „10 jüngste offene Fälle").
- TESTS: NEU `api/test/infrastructureSnapshotService.test.js`-Muster gespiegelt — DB-freier `api/test/support.recentCasesByOrg.test.js` (recordingPool, 6 Tests): leere/nicht-Array-ids→KEINE Query, **genau 1 Query unabhängig von der Org-Zahl (Anti-N+1-Garantie)**, SQL-Form (ANY($1::uuid[]) + ROW_NUMBER PARTITION BY + rn<=10 + open-only), Scope-Param-Push, Gruppierung jüngste-zuerst. PLUS DB-gated `api/test/integration/support-recent-cases.flow.test.js` (2 Tests): Helper mit NICHT existierender UUID gegen reales Schema → 0 Treffer, aber Postgres parst/plant die volle verschachtelte Window-Query → fängt Spalten-/Alias-Fehler ab, die der Mock-Pool durchlässt (scope-frei UND scope-behaftet). Begründung: Mock-Pool beweist ZÄHLUNG+FORM, nicht SQL-GÜLTIGKEIT.
- Checks: `node --check routes/support.js` OK · Unit 6/6 grün · ESLint (support.js + Test) exit 0 · integration-Test skippt sauber ohne DB · Regression `test/security/support-occ-boundaries.test.js` 3/3 grün (Routing/Imports intakt). NICHT im Live-Browser getestet (interner Support-Ops-Endpunkt, Code-Ebene verifiziert).
- OFFEN (deferred, agent-flagged aber UNVERIFIZIERT — eigener N+1-Write-Slice falls nötig): 3 Write-Loop-Kandidaten (assignmentStaffingService.createStaffingCampaignInternal INSERT je Worker bei Bulk-Invite; notifications.js PUT /notification-preferences UPSERT je Pref; requests.js POST /requests/broadcast INSERT je Target). Bewusst NICHT in diesem Slice mit-gefixt (kein Misch-Diff Read+Write; Triage erst am echten Code). KEIN Commit (Owner-Gate).

## 2026-06-03 — N+1-Write-Achse / Triage ABGESCHLOSSEN (3 Kandidaten verifiziert, KEIN Code, KEIN Commit)
- ANLASS: die 3 oben deferred-markierten Write-Loop-Kandidaten am ECHTEN Code verifiziert, BEVOR irgendetwas gefixt wird (zukunftssichere Empfehlung statt Bauchgefühl; die letzte Runde zeigte, dass der Agent-Report 3 von 4 falsch labelte). Diskriminator-Achsen: (1) Skalierungsachse — INPUT-skaliert (User wählt N) vs. KUNDEN-skaliert (wächst mit Mandantenzahl; nur DAS ist die Phase-5-Defektklasse); (2) Entanglement-Risikostufe (Transaktion / RETURNING-Verflechtung / Audit-pro-Zeile).
- KANDIDAT 1 — `notifications.js` PUT /notification-preferences (`for(p of prefs) await pool.query(INSERT…ON CONFLICT(user_id,event_category) DO UPDATE)`): Loop-Länge = Anzahl `event_category`-Werte = **festes Enum** (~10), wächst NIE mit Kunden/Daten. → **KEIN Defekt.** Kein Fix.
- KANDIDAT 2 — `requests.js` POST /requests/broadcast (`for(t of targets){ if(created>=remainingSlots) break; await insertBroadcastRequest(...); created++ }`): bereits durch das **Plan-Sendelimit** gekappt (`remainingSlots = requests_send===-1 ? 9999 : limit-usage`, `break` beim Erreichen). Loop-Länge skaliert mit Plan-Limit, nicht unbegrenzt mit Kunden. → **KEIN akuter Defekt** (bereits gebunden). Kein Fix.
- KANDIDAT 3 — `assignmentStaffingService.createStaffingCampaignInternal` (~L2480: `for(worker of eligibleWorkers){ INSERT assignment_staffing_invites … RETURNING * ; inviteMap.set(...); invites.push({...invite}); await writeStaffingEvent(invite.id) }`): **echter N+1-Write**, ABER (a) **input-skaliert** (Disponent wählt N Worker für EINE Kampagne — realistisch zweistellig), nicht kunden-skaliert; (b) liegt in `withTransaction` und JEDE `RETURNING`-Zeile speist `inviteMap` + `invites[]`-Shape (downstream `upsertWaitlistCandidates`, `queueStaffingInviteDeliveries`) + Per-Zeile-`writeStaffingEvent`-Audit. Ein Batch (multi-row `INSERT…RETURNING` + Reihenfolge exakt erhalten + inviteMap neu bauen + Event-Batch, atomar in der Txn) ist ein **Mutations-/Money-naher-Pfad-Umbau** → **Owner-gated**, NICHT der billige behavior-preserving Read-Batch von gestern. Risiko/Nutzen kippt (seltene Bulk-Aktion, Tail-Gewinn) → bewusst NICHT autonom gefixt.
- ENTSCHEIDUNG (Owner-bestätigt: „perfekt deine Empfehlung -> machen"): N+1-Write-Achse als **verifizierte Triage geschlossen** statt „alle drei fixen". #1+#2 = dokumentierte Nicht-Defekte; #3 = bewusst zurückgestellter Owner-Kandidaten (eigenes isoliertes Ticket mit Tests, falls Owner es priorisiert). Lernschleife mit der Write-Loop-Risikotriage-Regel gefüttert. KEIN Code, KEIN Commit. → Weiter auf risikofreier Achse (read-/list-Pfad-Sweep).

## 2026-06-03 — N+1-Read-Sweep VOLLSTÄNDIG (ganze Codebase, KEIN Code, KEIN Commit)
- ANLASS: nach dem support.js-Fix die N+1-Read-Achse über die GESAMTE Codebase abschließen (nicht nur die eine vom Agenten gemeldete Datei), um „verifiziert sauber" statt „stichprobenartig" zu dokumentieren. Selbst gesweept (kein Agent — der labelte zuletzt 3/4 falsch).
- METHODE: multiline-Grep nach dem Antipattern „Loop-Keyword … bridge … `await <pool|client>.query`" über `routes/` (der echte Request-Pfad) UND einem schärferen Read-Enrichment-Signature-Grep („`for/forEach/map(async` … `await query("SELECT … = $N`") über alle 38 `services/`. Jeden Treffer am echten Code gegen den Diskriminator „wächst die Loop-Länge mit Kunden/Daten auf einem Request-Pfad?" klassifiziert.
- BEFUND `routes/` (6 Treffer): admin.js:517 / workers.js:879 / offerAssets.js:185 = **False Positives** (der `for`-Loop baut nur die UPDATE-SET-Param-Liste/JS-Map, die Query steht NACH dem Loop). notifications.js:80 = Write-UPSERT, festes event_category-Enum (bereits triagiert, kein Defekt). marketplace.js:739 = `for(m of matches.slice(0,15))` SELECT email — **gekappt auf 15** + E-Mail-Send dominiert. marketplace.js:1444 = `for(party of parties)` mit **fest 2-elementigem** Array. → KEIN kundenskalierender Defekt im Request-Pfad außer dem bereits gefixten support.js.
- BEFUND `services/` (5 Read-Enrichment-Treffer): dealDossier:167 + staffSubscriptionRequests:161 = **False Positives** (Query nach JS-only-Loop). instantMatchService = nutzt bereits `ANY($2)` (das GUTE Batch-Muster) + reine JS-Gruppierungs-Loop. slaSearchService:367 = `for(m of matches.slice(0,15))` SELECT email — **gekappt auf 15**, BACKGROUND-Batch (`runSearchJobsBatch`, kein Request-Pfad), E-Mail-dominiert. subscriptionLifecycleService:639 = Lifecycle-**Cron** (LIMIT-batch-bounded, subscriptions = Kunden-Cardinality ~1/Kunde, Per-Zeile-State-Machine-Write-Sweep). → KEIN kundenskalierender Request-Pfad-Read-N+1.
- ERGEBNIS: Die N+1-Read-Achse ist **vollständig verifiziert** — der EINZIGE echte kundenskalierende Request-Pfad-Read-N+1 war `support.js` /support/lookup/orgs (letzte Runde behoben + getestet). Alles andere ist bounded (`.slice(0,N)`/festes Enum/fixe Parteienzahl), bereits `ANY()`-gebatcht, ein Grep-False-Positive (Query NACH JS-only-Loop) oder ein Kunden-Cardinality-batch-bounded-Cron. Bestätigt die Reifes-Repo-These erneut: Skalierungsarbeit = VERIFIKATION + Dokumentation, nicht Neubau. KEIN Code, KEIN Commit (Owner-Gate).
