# Changed Files Index — Phase 5

> Jede in Phase 5 geänderte/neue Datei mit Grund. Vor Release (Phase P) vollständige Review-Liste.

| Datei | Änderung | Grund | Risiko | Test |
|---|---|---|---|---|
| docs/finalization/10_300_customer_readiness_matrix.md | neu | Phase A Reifeprüfung | keine | n/a (Doku) |
| docs/finalization/finalization_worklog.md | neu | Projektgedächtnis | keine | n/a |
| docs/finalization/open_risks_and_blockers.md | neu | Risiko-/Blocker-Register | keine | n/a |
| docs/finalization/changed_files_index.md | neu | Änderungs-Index | keine | n/a |
| docs/finalization/final_acceptance_report_10_300_customers.md | neu (Skelett) | Abschlussbericht-Gerüst | keine | n/a |
| docs/finalization/phase5_manual_tasks_checklist.md | neu | Owner-Entscheidungen Tracking-Sheet (kanonisch, von `finalization/phase5_scale/MANUAL_TASKS.md` Zeile 136 referenziert; Status-Tabelle + „bereits gebaut/wartet auf Freigabe"-Mapping je externer Aufgabe) | keine | n/a (Doku) |
| .claude/learning/insights_inbox.md | neu | Lernschleife (cheap append) | keine | n/a |
| .claude/learning/config.md | neu | Lern-Konfiguration (Auto-Approve nur EFFIZIENZ) | keine | n/a |
| frontend/public/css/design-system.css | geändert | Phase J Block 1: `[data-theme="ultra_premium"]` Tokenblock + restrained Component-Refinements | niedrig (additiv, Default `:root` unverändert) | Brace-Balance 332/332 + themeRegistry.test.js |
| frontend/public/js/theme.js | geändert | Phase J Block 1: Theme-Registry (dark/light/ultra_premium) + `__TC_THEME_FLAGS__`-Gating + `cycle()`/`list()` | niedrig (rückwärtskompatibel: `toggle()` bleibt dark↔light, Default dark) | api/test/themeRegistry.test.js 9/9 + node --check |
| api/test/themeRegistry.test.js | neu | Phase J Block 1 Tests (Default aktiv, Ultra Premium selectable, Fallback, Flag-Gating, cycle) | keine | 9/9 grün, eslint 0 |
| docs/THEME-SYSTEM.md | geändert | Doku: ultra_premium + Registry/Flags + cycle-Verhalten + Block-2-Ausblick | keine | n/a |

| api/services/staffCustomerOperationsService.js | neu | Phase B: read-only Kundenroster (Aggregat über organizations.customer_stage/plan + subscription_requests + org_memberships) | niedrig (read-only, KEINE Migration) | staffCustomerOperations.test.js 11/11 |
| api/routes/staffControlCenter.js | geändert | Phase B: GET /customers, /customers-meta, /customers/:orgId (requireStaff, read-only) | niedrig (additive Routen, keine Mutation) | node --check + Service-Tests |
| api/test/staffCustomerOperations.test.js | neu | Phase B Tests (Zero-State, Mapping, Filter-Bindings, Enum-Sanitisierung, Clamp, Detail-404/UUID, Risiko-Ableitung) | keine | 11/11 grün, eslint 0 |
| frontend/src/staff/modules/customer-operations/index.tsx | neu | Phase B Slice 2: SCC Customer-Operations-Modul (Master-Detail, Filter/Suche/Pagination, Drilldown ins Abo-Modul) | niedrig (read-only, eigenes Code-Split-Chunk) | build:scc grün (tsc+vite) |
| frontend/src/staff/components/shell/Sidebar.tsx | geändert | Phase B Slice 2: AreaKey + AREAS-Eintrag „Customer Operations" (Gruppe Arbeitsplatz) | niedrig (additiver Nav-Eintrag) | build:scc grün |
| frontend/src/staff/components/shell/AppShell.tsx | geändert | Phase B Slice 2: lazy-Import + ActiveModule-Branch für customer-operations | niedrig (additive Route) | build:scc grün |

| api/config/index.js | geändert | Phase D Slice 0: `BILLING_PROVIDER`-Env (stripe/manual/disabled, leer=Ableitung) | niedrig (additiv, rückwärtskompatibel) | config-Validierung unberührt |
| api/services/billingProviderService.js | neu | Phase D Slice 1: Provider-Abstraktion (resolve/describe/mapStripeEvent), rein funktional, manual-first | niedrig (kein DB/IO, additiv) | billingProviderService.test.js 26/26 |
| api/routes/payment.js | geändert | Phase D Slice 1: `/payment/config` +`billing`-Block; Webhook-Dispatch über mapStripeEvent (seiteneffekt-identisch); Dunning-Kommentar | niedrig (additiv + behavior-preserving) | payment.route.test.js 31/31 (Backwards-Compat) |
| api/test/billingProviderService.test.js | neu | Phase D Slice 1 Tests (Auflösung, Capabilities, Event-Mapping, Garbage-Safety) | keine | 26/26 grün, eslint 0 |

| api/services/staffBillingOverviewService.js | neu | Phase D Slice 2: read-only SCC-Billing-Aggregat (describeBilling + invoices-Summen je Status + listInvoices) | niedrig (read-only, KEINE Migration/Mutation, Zero-State) | staffBillingOverview.test.js 8/8 |
| api/routes/staffControlCenter.js | geändert | Phase D Slice 2: GET /billing/overview, /billing/meta (requireStaff, read-only) | niedrig (additive Routen) | node --check + Service-Tests |
| api/test/staffBillingOverview.test.js | neu | Phase D Slice 2 Tests (Provider-Shape, Zero-State, Aggregation/Mapping, Status-Sanitisierung+Clamp, Param-Forward) | keine | 8/8 grün, eslint 0 |
| frontend/src/staff/modules/billing/index.tsx | neu | Phase D Slice 2: SCC Billing-Modul (Provider-Karte + Totals-KPI + Summen-je-Status + Letzte-Rechnungen, read-only) | niedrig (read-only, eigenes Code-Split-Chunk, kein neues CSS) | build:scc grün (62 Module) |
| frontend/src/staff/components/shell/Sidebar.tsx | geändert | Phase D Slice 2: AreaKey + AREAS-Eintrag „Billing" (Gruppe Strategie) | niedrig (additiver Nav-Eintrag) | build:scc grün |
| frontend/src/staff/components/shell/AppShell.tsx | geändert | Phase D Slice 2: lazy-Import + ActiveModule-Branch für billing | niedrig (additive Route) | build:scc grün |

| api/services/staffBillingOverviewService.js | geändert | Phase D Slice 3: `loadAttention`/`mapAttention`/`ATTENTION_LIMIT` + 3. parallele Query → `attention[]`+`scope.attention_limit` (überfällige Forderungen, älteste zuerst) | niedrig (read-only, additiv, KEINE Mutation/Migration) | staffBillingOverview.test.js 8/8 |
| api/routes/payment.js | geändert | Phase D Slice 3: `payment_failed`-Webhook-Zweig = logger.warn (Observability), KEIN Seiteneffekt/Auto-Cancel/Mail | niedrig (behavior-preserving, returnt weiter received:true) | payment.route.test.js 32/32 |
| api/test/staffBillingOverview.test.js | geändert | Phase D Slice 3: sequencePool 3-Antworten + Attention-Assertions + Zero-State `attention:[]` + `scope.attention_limit` | keine | 8/8 grün, eslint 0 |
| api/test/payment.route.test.js | geändert | Phase D Slice 3: Test `invoice.payment_failed` = received:true + warnCalled + keine DB-Berührung + keine Mail | keine | 32/32 grün, eslint 0 |
| frontend/src/staff/modules/billing/index.tsx | geändert | Phase D Slice 3: Abschnitt „Überfällig · Inkasso-Worklist" (days_overdue-Severity >14 danger, Zero-State, Limit-Hinweis), AttentionRow-Typ, `dunningTone()` | niedrig (read-only, kein neues CSS) | build:scc grün (62 Module) |

| api/services/emailProviderService.js | neu | Phase E: Email-Provider-Abstraktion (resolve/describe, console/smtp/sendgrid/disabled), rein funktional, KEINE neue Dependency | niedrig (kein DB/IO, additiv) | emailProviderService.test.js 21/21 |
| api/config/index.js | geändert | Phase E: `EMAIL_PROVIDER` + `SENDGRID_API_KEY`-Env (leer=Ableitung) | niedrig (additiv, rückwärtskompatibel) | node --check |
| api/services/emailService.js | geändert | Phase E: provider-fähiger Transport (SendGrid via SMTP-Relay, SMTP unverändert, console/disabled=Log-only) + `describe()`-Export; sendMail-Signatur identisch | niedrig (preserve-first, Default-Pfad byte-identisch) | subscriptionNotifications.test.js 43/43 (kein Regress) |
| api/test/emailProviderService.test.js | neu | Phase E Tests (Auflösung+Präzedenz, Capabilities-Ehrlichkeit, Degradierung, unbekannter Provider, Garbage-Safety) | keine | 21/21 grün, eslint 0 |
| .env.example | geändert | Phase E: Provider-Auswahl-Block (EMAIL_PROVIDER/SENDGRID_API_KEY) im E-Mail-Abschnitt (adressiert R5 Email-Dimension) | keine | n/a (Doku) |

| api/services/healthService.js | geändert | Phase I Slice 1: billing/email-Readiness in `getSystemDiagnostics` (countTrue/billingHealth/emailHealth-Mapper + Integrations-Block + `opts.config`-Injektion); unconfigured zieht Gesamtstatus nicht | niedrig (additiv, read-only, kein Secret-Leak, kein Public-Surface-Change) | systemHealth.test.js 17/17 |
| api/test/systemHealth.test.js | geändert | Phase I Slice 1: Block „Provider-Readiness (billing/email)" (no-config→unconfigured, echte Keys→ok, erzwungen-ohne-Key→degraded+Gesamtstatus, Secret-Safety, Shape-Garantie) | keine | 17/17 grün, eslint 0 |

| api/services/staffControlService.js | geändert | Phase I Slice 2: `loadOperationsSnapshot` um `service_health = getSystemDiagnostics(pool)` erweitert (Import healthService; als letzte pool-Berührung; Soft-Fail→errors[]) | niedrig (additiv, read-only, kein Zyklus, Pool-Sequenz erhalten) | staffControlService.test.js 48/48 |
| frontend/src/staff/modules/operations/index.tsx | geändert | Phase I Slice 2: Abschnitt „Live-Service-Health" (Karten db/redis/api/process/billing/email, Status-Pills + Detail + secret-freie Provider-Warnungen) | niedrig (read-only, kein neues CSS, keine Emojis) | build:scc grün |
| api/test/staffControlService.test.js | geändert | Phase I Slice 2: +2 Tests im loadOperationsSnapshot-Block (service_health befüllt db/billing/email; Soft-Fail DB-Fehler→database critical, Snapshot kippt nicht) | keine | 48/48 grün, eslint 0 |

| api/services/staffMailCenterService.js | neu | Phase E/F: read-only SCC-Mail-Aggregat (describeEmail + subscription_notification_log: Summen je mail_status inkl. NULL→none + Kanäle je dispatched_via + Event-Typ-Breakdown events_by_key total/failed/no_smtp + letzte Notifications, Empfänger PII-maskiert) | niedrig (read-only, KEINE Migration/Mutation/Versand, Zero-State) | staffMailCenter.test.js 10/10 |
| api/routes/staffControlCenter.js | geändert | Phase E/F: Import staffMail + GET /mail/overview, /mail/meta (requireStaff, read-only) | niedrig (additive Routen, keine Mutation) | node --check + Service-Tests |
| api/test/staffMailCenter.test.js | neu | Phase E/F Tests (meta, Provider-Shape, Zero-State, Aggregation Status/Kanal/Recent + events_by_key-Breakdown + PII-Maskierung, null-recipient, Status-Sanitisierung+Clamp, gebundener Status-Param, Garbage-Safety) | keine | 10/10 grün, eslint 0 |
| frontend/src/staff/modules/mail/index.tsx | neu | Phase E/F: SCC Mail-Modul (Provider-Karte + Zustell-KPI + Summen-je-Status + Zustellkanäle + „Nach Event-Typ"-Tabelle + letzte Notifications PII-maskiert mit Status-Filter, read-only) | niedrig (read-only, eigenes Code-Split-Chunk, kein neues CSS) | build:scc grün (63 Module) |
| frontend/src/staff/components/shell/Sidebar.tsx | geändert | Phase E/F: AreaKey `mail` + AREAS-Eintrag „Mail & Notifications" (Gruppe Operations) | niedrig (additiver Nav-Eintrag) | build:scc grün |
| frontend/src/staff/components/shell/AppShell.tsx | geändert | Phase E/F: lazy-Import + ActiveModule-Branch für mail | niedrig (additive Route) | build:scc grün |
| docs/finalization/proposal_incident_model_owner_gate.md | neu→umgesetzt | Freigabe-Entwurf Incident-Modell (`ops_incidents`-Migration + Rollback + Service/Route/UI-Design); verankert am verwaisten `warp_executions.incident_id`-Hook (Mig 108). Owner-freigegeben + gebaut (siehe Zeilen unten). | keine (Doku) | n/a |

| sql/migrations/121_ops_incidents.sql | neu | Incident Kern-Slice: `ops_incidents`-Tabelle (Staff-Ops, KEINE RLS, KEINE audit_id; CHECK-Enums severity/status/source; opened_reason NOT NULL; *_by→users ON DELETE SET NULL; details JSONB; 3 Indizes; In-File-Rollback DROP CASCADE). Schließt Mig-108-Lücke. | mittel (neue Tabelle, additiv; FK warp_executions.incident_id bewusst NICHT verdrahtet) | via staffIncidents.test.js (sequencePool) |
| api/services/staffIncidentService.js | neu | Incident Kern-Slice: read-only Aggregat `listIncidents` (Summen je status/severity + paginierte Liste, Zero-State, scope.platform) + `meta()` + Mutationen openIncident/acknowledgeIncident/resolveIncident (diskriminiertes `{ok,row\|error}`, Audit im Router; strenge Übergänge open→ack→resolved via SELECT…FOR UPDATE in withTransaction) | niedrig (Service rein, Audit/CSRF im Router) | staffIncidents.test.js 20/20 |
| api/routes/staffControlCenter.js | geändert | Incident Kern-Slice: Import staffIncidents + 5 Routen: GET /incidents, /incidents/meta (requireStaff) · POST /incidents (201), /incidents/:id/acknowledge, /incidents/:id/resolve (requireStaff,mfaGuard,requireStepUp,requireConfirmAndReason; Audit area=operations action=staff_control.incident.* entityType=ops_incident risk=medium auf Erfolg+Fehler; Status-Mapping 404/409/400) | niedrig (additive Routen, etablierte Mutations-Kette) | node --check + staffIncidents.test.js + 107/107 Router-Boot-Suites |
| api/test/staffIncidents.test.js | neu | Incident Kern-Slice Tests (meta, Zero-State, Aggregation, Input-Sanitisierung/Clamp, openIncident inkl. JSON.stringify-Param + TITLE_REQUIRED/REASON_TOO_SHORT ohne Query, ack/resolve happy 2-Query + NOT_FOUND/NO_CHANGE/INVALID_TRANSITION mit korrekter Query-Zahl) | keine | 20/20 grün, eslint 0 |
| frontend/src/staff/modules/incidents/index.tsx | neu | Incident Kern-Slice: SCC Incidents-Modul (KPI-Grid + Severity-Pillen + Status/Severity-Filter + Tabelle mit Per-Row Quittieren/Schließen via useConfirm+useStepUp + „+ Neuer Incident"-Modal) | niedrig (eigenes Code-Split-Chunk, kein neues CSS) | build:scc grün (64 Module) |
| frontend/src/staff/components/shell/Sidebar.tsx | geändert | Incident Kern-Slice: AreaKey `incidents` + AREAS-Eintrag „Incidents" (Gruppe Operations) | niedrig (additiver Nav-Eintrag) | build:scc grün |
| frontend/src/staff/components/shell/AppShell.tsx | geändert | Incident Kern-Slice: lazy-Import + ActiveModule-Branch für incidents | niedrig (additive Route) | build:scc grün |

| api/services/staffIncidentService.js | geändert | Incident §6.2: `listOpenSignals` + `loadFailedWarpSignals`/`loadMailFailureSignals` + Clamp/Severity-Helfer → read-only Feed offener Signale OHNE Incident (failed warp_executions + nach event_key aggregierte Mail-Fehler, je mit `suggested`-Block) | niedrig (read-only, additiv, KEINE Migration/Mutation) | staffIncidents.test.js 25/25 |
| api/routes/staffControlCenter.js | geändert | Incident §6.2: GET /incidents/signals (requireStaff, read-only; spiegelt GET /incidents) | niedrig (additive Route) | node --check + 107/107 Router-Boot |
| api/test/staffIncidents.test.js | geändert | Incident §6.2: +5 Tests (Zero-State, Aggregation+suggested+Totals, gebundene SQL `status='failed'`/`incident_id IS NULL`/`GROUP BY event_key` + deterministische Call-Reihenfolge, window/limit-Clamp+Garbage-Default, Mail-Severity-Schwellen + null event_key→`unbekannt`) | keine | 25/25 grün, eslint 0 |
| frontend/src/staff/modules/incidents/index.tsx | geändert | Incident §6.2: Panel „Offene Signale ohne Incident" (Warp+Mail, „→ Incident" öffnet Create-Modal vorbefüllt) + `CreateIncidentModal` `prefill`-Prop/`signal_code`-Durchreichung + Feed-Fetch via Promise.allSettled (Signal-Fehler kippt Liste nicht) | niedrig (read-only Feed, kein neues CSS) | build:scc grün (64 Module) |

| api/routes/admin.js | geändert | Skalierungs-Härtung (10→300): `/admin/organizations` + `/admin/requests` + `/admin/strategic-collaboration/requests` → `{items,total,limit,offset}`-Envelope (paginiert/geklammert, Schwester `/admin/users` gespiegelt) | niedrig (additiv, requireAdmin unberührt, KEIN RBAC/Org-Scope-Eingriff) | adminUsers.route 11/11 |
| api/services/requestService.js | geändert | Skalierungs-Härtung: `listRequestsAdmin` → {items,total} (paralleler bounded COUNT, status uppercased) | niedrig (additiv) | requestService.test.js |
| api/services/strategicCollaborationService.js | geändert | Skalierungs-Härtung: `listAllRequestsAdmin` → {items,total} (bounded COUNT) | niedrig (additiv) | strategicCollaborationService.test.js |
| api/services/usageMeteringService.js | geändert | Skalierungs-Härtung: `scanAndEnforceUsageLimits(pool,{limit,offset})` Batch-Bound (DEFAULT 100/MAX 500, `ORDER BY u.id LIMIT/OFFSET`) + `{scanned,over_limit,has_more,next_offset}`-Envelope — vorher unbegrenzter Cron-Voll-Scan über alle Nutzer | niedrig (additiv, safe-by-default, einziger Caller internal.js) | usageMetering.test.js 9/9 |
| api/routes/internal.js | geändert | Skalierungs-Härtung: `usage-limit-scan` clamped `batch_size`≤500 + `offset`, reicht `{limit,offset}` durch + loggt beide | niedrig (additiv) | usageMetering.test.js + node --check |
| frontend/public/js/pages/adminPanel.js | geändert | Skalierungs-Härtung: generisches `renderPaging(total,offset,limit,loadFn)` + Orgs/Backoffice/Strategic-Paging-State + async `loadOrgs/loadBackofficeRequests/loadStrategicRequests(offset)` (Post-Mutation-Reloads behalten Seite) | niedrig (additiv) | node --check; UI-Smoke offen |
| frontend/public/admin_panel.html | geändert | Skalierungs-Härtung: `#orgsPaging` + `#boRequestsPaging` + `#strategicPaging` Paging-Divs | niedrig (additiv) | n/a |
| api/test/adminUsers.route.test.js | geändert | Skalierungs-Härtung: +Block „GET /admin/organizations" (4 Tests: paginiert+Total+bounded LIMIT/OFFSET, Clamp, Cap 200, 500-Envelope) | keine | 11/11 grün |
| api/test/requestService.test.js | geändert | Skalierungs-Härtung: +2 Tests ({items,total}+bounded COUNT+status-uppercase, Clamp 5000→200) | keine | grün |
| api/test/strategicCollaborationService.test.js | geändert | Skalierungs-Härtung: Test auf paginiert+total+bounded COUNT umgestellt | keine | grün |
| api/test/usageMetering.test.js | neu | Skalierungs-Härtung: 9 Tests (Default-LIMIT/OFFSET-Bindung, Clamp MAX/Garbage/0/neg-offset, over_limit-Zählung DEMO/BASIS/PRO, has_more+next_offset-Paging, Graceful-Catch Zero-Envelope, unbekannter Plan→DEMO) | keine | 9/9 grün, eslint 0 |

| api/config/index.js | geändert | Phase J Block 2: `THEME_SWITCHER_ENABLED` + `ULTRA_PREMIUM_THEME_ENABLED` (Tier-2 Env-Kill-Switch, default-AN; nur `false/0/no/off` schaltet ab) | niedrig (additiv, rückwärtskompatibel, Default-Semantik = beide an) | themeFlags.config.test.js 4/4 + node --check |
| api/routes/staffControlCenter.js | geändert | Phase J Block 2: `import { config }` + `theme:{switcher_enabled,ultra_premium_enabled}` im /bootstrap-Response (Tier-2-Flags zum SCC-Client) | niedrig (additiv, requireStaff unberührt, kein Secret) | staffSubscriptionRequests.routes.test.js 12/12 (Router-Boot) |
| frontend/src/staff/styles/scc.css | geändert | Phase J Block 2: `[data-theme="ultra_premium"]`-Block (22 `--scc-*` Tokens, Obsidian-Sapphire) — macht Block-1-Premium-Theme im Operator-Shell erreichbar | niedrig (additiv, Default-Themes unverändert, kein neues Klassen-Set) | build:scc grün |
| frontend/src/staff/types/bootstrap.ts | geändert | Phase J Block 2: `theme?:{switcher_enabled,ultra_premium_enabled}` in StaffBootstrap (fehlt → beide an) | keine (Typ-only, optional) | build:scc tsc 0 Errors |
| frontend/src/staff/main.tsx | geändert | Phase J Block 2: Pre-Paint-Theme-Init akzeptiert `ultra_premium` (kein Flash bei gespeichertem Premium-Theme) | niedrig (additiv, Default dark unverändert) | build:scc grün |
| frontend/src/staff/components/shell/Topbar.tsx | geändert | Phase J Block 2: parallele dark/light-Toggle → Registry-Cycle (dark→light→premium, Premium nur wenn Flag an); `switcherEnabled`-Gating blendet Toggle aus; gespeichertes deaktiviertes Theme fällt auf dark zurück | niedrig (ersetzt Parallelstruktur, kein neues CSS) | build:scc grün (64 Module) |
| api/test/themeFlags.config.test.js | neu | Phase J Block 2: 4 Tests (Default beide AN bei ungesetzter ENV, explizit `false`→beide aus, `off`/`1` gemischt, `  FALSE  ` Trim/Case→aus) via cache-bustendem Query-Import | keine | 4/4 grün, eslint 0 |

| api/config/envValidator.js | geändert | R5 (Phase J Block 2 Nachzug): `THEME_SWITCHER_ENABLED` + `ULTRA_PREMIUM_THEME_ENABLED` als optionale Keys + superRefine-Schleife gegen bekannte Boolean-Tokens (true/false/1/0/yes/no/on/off; leer=an) — Tippfehler wie `=nein` → Fail-Fast statt still-AN | niedrig (additiv, gesetzte gültige Werte/leer passieren wie zuvor) | envValidator.test.js 18/18 + node --check |
| .env.example | geändert | R5: „THEME SYSTEM (Tier-2 Env-Kill-Switch)"-Block (default-AN-Semantik, Abschalt-Hinweis), beide Flags auskommentiert | keine | n/a (Doku) |
| .env.dev.example | geändert | R5: THEME-SYSTEM-Block (lokal alle Themes testbar, Token-Liste) | keine | n/a (Doku) |
| .env.prod.example | geändert | R5: THEME-SYSTEM-Block (Whitelabel/Abschaltung pro Instanz) | keine | n/a (Doku) |
| api/test/envValidator.test.js | geändert | R5: +5 Theme-Tests (gültige Tokens, leer=Default-AN, `=nein`-Footgun→throw, `enabled`→throw, Trim+Case `  OFF  `/` No `) — 13→18 | keine | 18/18 grün, eslint 0 |
| docs/THEME-SYSTEM.md | geändert | R5/Block-2-Doku-Wahrheit: „Block-2-Ausblick" → „Block-2-Status" (Env-Flags umgesetzt; nur SCC-Scope geliefert via eigenes Bootstrap/`scc-theme`, getrennt vom Static-Page-System; platform/worker_portal-`__TC_THEME_FLAGS__`-Injektion + Control-Modul owner-gated) | keine | n/a (Doku) |

| api/services/slaSearchService.js | geändert | Skalierungs-Härtung (N+1-Write-Loop): `searchSlaScan` von `SELECT … LIMIT` + per-Zeile-UPDATE/Event-Insert auf ein geschütztes set-based `UPDATE … WHERE sla_status='RUNNING' AND id IN (SELECT … LIMIT) RETURNING id` + Bulk-`INSERT … UNNEST($1::uuid[])` (1+2N → 2 Statements) | niedrig (verhaltensgleich; RUNNING-Guard auf äußerem UPDATE erhält No-Duplicate-Event-Semantik unter überlappenden Crons) | slaSearchService.test.js 4/4 + node --check |
| api/services/marketplaceService.js | geändert | Skalierungs-Härtung (N+1-Write-Loop): `demandSlaScan` (verbatim-Spiegel von searchSlaScan) identisch set-based gemacht; `demand_sla_events`/`demand_requests.id` UUID+JSONB+CHECK verifiziert | niedrig (verhaltensgleich; gleicher äußerer RUNNING-Guard) | marketplaceService.test.js grün + node --check |
| api/test/slaSearchService.test.js | neu | Skalierungs-Härtung: `searchSlaScan` war ungetestet — 4 Tests (recordingPool zählt genau 2 Queries, prüft RUNNING-Guard+LIMIT+RETURNING im UPDATE + UNNEST+SLA_BREACHED+ids im Insert; 0-Breach=1 Query/kein Insert; Cap 500; Default 100) | keine | 4/4 grün, eslint 0 |
| api/test/marketplaceService.test.js | geändert | Skalierungs-Härtung: `demandSlaScan`-Block neu geschrieben für set-based Shape (2-Query-Zählung + SQL-Form-Assertions statt alter 5-Query-N+1-Sequenz) | keine | 52/52 (mit slaSearchService) grün |

| sql/migrations/122_cron_sweep_indexes.sql | neu | Phase Q / Index-Review: 5 Partial-Indizes für Cron-Sweeps ohne Stütz-Index (webhook-retry status=failed · staffing-invites/-reservations expires_at · demand notdienst-escalate created_at · sla-search-run status=open). Reines Performance-Add-on, add-only, IF NOT EXISTS, Rollback-Block (DROP INDEX je Index). Jede Lücke gegen Mig 014/015/047/087 verifiziert (Bestandsindizes decken Praedikat/Ordnung nicht) | niedrig (additiv, kein Schema-/Verhaltenswechsel; plain CREATE INDEX in BEGIN/COMMIT konsistent zu Mig 121) | strukturell: Spalten+Status-Werte+Indexnamen-Eindeutigkeit verifiziert; kein SQL-node--check existent |
| sql/migrations/123_analytics_events_time_brin.sql | neu | Phase Q / Index-Review ABSCHLUSS: BRIN-Zeitindex `product_analytics_events(occurred_at)` für die 2 zeitbasierten Sweeps (retention-DELETE occurred_at<cutoff + rollup-SELECT occurred_at>=NOW()-N). Einzige echte Rest-Lücke nach Audit aller ~12 verbleibenden Sweeps (Rest gedeckt durch Mig 009/011/013/030/076 oder kunden-cardinality/trivial). BRIN statt btree: occurred_at append-only/zeitkorreliert, heißer Insert-Pfad → keine Write-Amplification | niedrig (additiv, kein Schema-/Verhaltenswechsel; BRIN belastet Insert-Pfad praktisch nicht) | strukturell: occurred_at-Spalte + kein führender Zeit-btree/BRIN gegen Mig 066/068 verifiziert; beide Sweep-Queries am Service bestätigt; Indexname kollisionsfrei |
| api/test/integration/scaling-indexes.flow.test.js | neu | Phase O / Test-Sicherung: DB-gestützter Index-Existenz-/Form-Test für Mig 122+123 — introspektiert `pg_indexes.indexdef`, prüft je Index Existenz + Zugriffsmethode (btree vs. BRIN, eigener Anti-btree-Assert für den Analytics-Index) + Tabelle/Spalte + Partial-Praedikat (robuste Teilstrings gegen pg-Normalisierung). DB-gated (`skip: !hasDb`) → läuft im CI-integration-Job (Postgres 16, nach allen Migrationen), skippt im DB-freien unit-Job | keine (read-only Introspektion, additiv, keine Mutation/kein Schema) | node --check ok; Lauf ohne DB = sauberer Skip (0 fail/0 pass) |
| scripts/restore-test.sh | geändert | Phase P / Restore-Drill-Fidelity (R8): pg_restore im Drill um `--single-transaction --exit-on-error` ergänzt — gleicht den Drill an den echten Restore (`restore.sh`) an. Vorher konnte der Drill einen nur teilweise eingespielten Dump fälschlich als „bestanden" melden (False-Confidence). Triage: Bug (Drill nicht faithful) | niedrig (additive Flags, härtet Drill = strenger/korrekter, keine Datenberührung) | bash -n SYNTAX_OK |
| api/test/infrastructureSnapshotService.test.js | neu | Phase P-Test / R8-Severity-Matrix-Pinning: DB-freier Unit-Test (branching recordingPool) über `ingestInfrastructureSnapshots`. Pinnt die Backup-Staleness-Schwellen 12/24/48h (strikte `>`-Boundaries: 48→high, 24→medium, 12→ok) + restliche Signal-Matrix (cpu/ram/disk/docker/tls) + Input-Härtung (Prozent-Clamp, garbage→null/Fallback, env-Default, collected_at-ISO-Fallback, metadata.source) + Validierung/Aggregat (INVALID_SNAPSHOT_OBJECT/MISSING_HOST_NAME, kein Throw, inserted/failed/critical_hosts). Service war zuvor komplett ungetestet | keine (read-only, mock-pool, additiv) | node --test → 36/36 grün, 0 fail |

| api/routes/support.js | geändert | Skalierungs-Härtung (N+1-Read-Loop): `GET /support/lookup/orgs` lud eine Org-Seite (per_page≤100) und feuerte DANN eine recent-cases-SELECT PRO Org (bis zu 100 Round-Trips/Request). Per-Org-Block in neue exportierte Helper `loadRecentOpenCasesByOrg(pool,orgIds,agent)` gezogen, die alle Orgs in EINER windowed Query bedient (ROW_NUMBER PARTITION BY reporter_org_id, rn≤10, WHERE reporter_org_id=ANY($1::uuid[])); Handler-Loop durch `.map` über die gruppierte Map ersetzt; Output-Shape byte-identisch, Maskierung bleibt im Handler | niedrig (verhaltensgleich; buildScope einmal statt pro Zeile, id DESC nur als deterministischer Tie-Breaker) | node --check OK · eslint exit 0 · support-occ-boundaries 3/3 grün |
| api/test/support.recentCasesByOrg.test.js | neu | Skalierungs-Härtung: DB-freier recordingPool-Unit-Test für `loadRecentOpenCasesByOrg` — 6 Tests: leere/nicht-Array-ids→keine Query, **genau 1 Query unabhängig von der Org-Zahl (Anti-N+1-Garantie)**, SQL-Form (ANY($1::uuid[])+ROW_NUMBER PARTITION BY+rn≤10+open-only), Scope-Param-Push, Gruppierung jüngste-zuerst | keine | node --test → 6/6 grün, 0 fail |
| api/test/integration/support-recent-cases.flow.test.js | neu | Skalierungs-Härtung: DB-gated SQL-Validitäts-Smoke für `loadRecentOpenCasesByOrg` — führt die verschachtelte Window-Query mit NICHT existierender UUID gegen das reale Schema aus (0 Treffer, aber Postgres parst/plant die volle Query → fängt Spalten-/Alias-Fehler, die der Mock-Pool durchlässt; scope-frei + scope-behaftet). `skip: !hasDb` → CI-integration-Job, skippt im unit-Job | keine (read-only Introspektion) | node --check OK; Lauf ohne DB = sauberer Skip (0 fail/0 pass) |

| frontend/public/enterprise.html | geändert | Marktplatz-Rollenlabel-Fix: `id="hub-card-marketplace-desc"` + `id="hub-card-marketplace-note"` an der Hub-Card ergänzt, damit die Agentur-Sicht (Zeitarbeitsfirma) Beschreibung/Fussnote umschalten kann (Titel-id existierte bereits) | niedrig (nur 2 id-Attribute, kein HTML-Textwechsel, Default-Sicht unverändert) | node --check JS-Consumer |
| frontend/public/js/pages/enterpriseHub.js | geändert | Marktplatz-Rollenlabel-Fix: neue `applyMarketplaceCopy(me)` — Agentur (org_type=agency) erhält Hub-Card-Titel + Aktivierungs-Nudge-CTA „Arbeitsplatz finden" via `TC.terminology.get("marketplace",…)` (kein Duplikat-Konstant, spiegelt bestehende Nav-Rollenlogik) + agentur-spezifische Desc/Fussnote; Company/unbekannt = HTML-Default „Personal finden". Aufruf in `/api/me` .then UND .catch (Muster wie `applyAssignmentsCopy`) | niedrig (additive Funktion + 2 Aufrufe; No-Op bei fehlendem TC/Element; Company-Default-Pfad unverändert) | node --check SYNTAX_OK |

| api/routes/reporting.js | geändert | Entitlement-Leak-Fix (P1.7 HIGH): `requireOrgFeature("enterprise_analytics")` auf `/reporting/dashboard` + `/reporting/finance-truth/export` (Position hinter requireAuth, Muster `spendAnalytics`). NUR die 2 `report.executive`-Routen; operationale `report.operational`-Routen bewusst ungegated (mischen mit plan-übergreifendem `sla_access`) | niedrig-mittel (Commercial: zahlende Tarife < INDIVIDUELL verlieren Executive-Reporting; pilot-aware → DEMO-Piloten behalten Zugang; Surface war ohnehin `requires_individuell`) | node --check OK; reporting.route.test.js 28/28 grün (regressionsfrei, da Tests den Handler direkt aufrufen) |
| api/routes/assignments.js | geändert | Entitlement-Leak-Fix (P1.7 HIGH): `requireOrgFeature("assignments")` NUR auf `POST /assignments` (create) + `PATCH /assignments/:id` (edit) = käufer-exklusive Schreibpfade. Lesen (view) + Lifecycle (transition/complete) bleiben offen, weil zweiseitig (`supplier_org_id`/Agentur legitim beteiligt) | niedrig-mittel (Commercial; zweiseitige Pfade absichtlich offen → Agenturseite bricht NICHT; pilot-aware) | node --check OK; assignment-Service-Tests grün |
| api/test/entitlementLeakGates.route.test.js | neu | 12 Tests für die 2 geschlossenen INDIVIDUELL-Leaks: inhaltsbasierter Mock-Pool (robust gegen Doppel-Snapshot des Guards), Gate an Stack-Position [1] direkt ausgeführt — PRO=403 FEATURE_NOT_ENABLED / INDIVIDUELL=next() / DEMO+Pilot=next(); Struktur-Checks beweisen ungegatete Routen (view/transition/complete/operational reports) tragen kein Gate | keine | 12/12 grün |

| api/services/notificationSurfaceMap.js | neu | Benachrichtigungs-Card-Badges: einzige Wahrheitsquelle `type → Hub-Surface`. `surfaceForType()` mappt die notificationMatrix/Mig-019/071/072-Typen auf enterprise.html `data-surface` (requisition_*→requisitions, offer_*/deal_*→deals, capacity_*/demand/emergency_*→marketplace, vendor_pool_*→vendor_pool, compliance_*→trust_center, sla_*→my_company, timesheet_*→assignments; general/system/worker-only→null=bell-only). `summarizeBySurface(rows)` faltet gruppierte Counts → `{surfaces,total}` (total Glocken-konsistent inkl. unmapped) | keine (reine Funktion, keine Dependency) | node --check OK; 19/19 grün |
| api/routes/notifications.js | geändert | Neuer read-only Endpoint `GET /notifications/surface-summary` (requireAuth, user-scoped): EINE `GROUP BY type`-Query auf ungelesene → `summarizeBySurface` → `{surfaces,total,generated_at}`. Static-Import des Surface-Maps (pure helper, kein per-Request-Import). Kein Org-Leak (nur `user_id=$1`), Glocken-Total bleibt konsistent | niedrig (additiv, keine bestehende Route berührt; nutzt vorhandenen Partial-Index `notifications_user_unread_idx`) | node --check OK; bestehende notif-Tests 120/120 + 4 neue grün |
| api/test/notificationSurfaceMap.test.js | neu | 15 Tests Mapping+Fold: jeder Surface-Key ist echte Hub-Card, total-zählt-unmapped/surfaces-nicht, pg-String-Counts, Null/Negativ ignoriert, robust gegen leere Eingabe | keine | 15/15 grün |
| api/test/notifications.surfaceSummary.route.test.js | neu | 4 Route-Tests: Fold-Verhalten (bell-only im total), Zero-State, User-Scope-Param + SQL-Form (GROUP BY type/is_read=FALSE), Route-Registrierung (requireAuth+Handler) | keine | 4/4 grün |
| frontend/public/js/pages/enterpriseHub.js | geändert | Card-Badges: `loadSurfaceBadges()` lädt `/api/notifications/surface-summary` nach `applyHubVisibility`, `injectSurfaceBadge()` setzt kleine Zahl (≤99+) auf sichtbare `[data-surface]`-Cards (versteckte/aria-hidden ausgenommen). Card-href = Deep-Link „direkt da hin". Design-Token `var(--ds-danger,#f43f5e)`, idempotent (re-render-safe), soft-fail | niedrig (additiv im bestehenden IIFE, nur authentifizierter Boot-Pfad; No-Op ohne Daten/Card) | node --check SYNTAX_OK |
| frontend/public/js/notifications.js | geändert | Glocken-Konsolidierung: das reiche Dropdown blendet nach Mount die statische pageShell-Link-Glocke `#tc-notif-bell` plattformweit aus (guarded `if(legacyBell)` → strandet keine Seite ohne pageShell; SSE-Live-Toasts in pageShell bleiben aktiv). Erfüllt „eine Glocke kann entfernt werden" | niedrig (1 guarded DOM-Hide; keine API-/Datenänderung) | node --check SYNTAX_OK |

> Hinweis: Phase A war read-only. Erste Code-Einträge ab Phase J (Design-first, Owner-Vorgabe): isolierter Design-Diff (kein Misch-Diff über Billing/SCC/Auth). Block 2 liefert NUR den SCC-Scope end-to-end (Tier-2-Theme-Flags → /bootstrap → Topbar-Registry-Cycle + ultra_premium-Theme im Operator-Shell). Owner-gated bleiben: platform/worker_portal-Theme-Scopes (brauchen Runtime-Config-Delivery für ~80 statische Seiten — keine Shared-Head/Public-Config-Route) + das SCC-Theme-Control-Modul mit Preview/Reset/Audit (braucht Settings-Store/Migration).
> Hinweis Skalierungs-Härtung: drei plattformweite Listen/Sweeps (admin-orgs, admin-requests+strategic, usage-limit-scan) auf bounded Pagination/Batch gebracht — alle uncommitted (Owner-Gate), Backend code-verifiziert, Admin-UI-Paginierung noch ohne Live-Browser-Smoke. Zweite Defekt-Klasse (N+1-Write-Loops) ebenfalls gesweept: `productReleaseService.markAllSeenForUser` + die SLA-Cron-Scans (`searchSlaScan`/`demandSlaScan`) auf Bulk-`UNNEST`/set-based UPDATE gebracht (verhaltensgleich, No-Duplicate-Event-Semantik erhalten). Verzweigte Scans (slaService `slaScan`/`notdienstEscalationScan`, marketplace `notdienstEscalationDemands`) bewusst als dokumentierter Follow-up belassen. Dritte Defekt-Klasse (Cron-Sweeps ohne Stütz-Index) **vollständig abgearbeitet**: ALLE ~20 Sweeps in internal.js gegen den Diskriminator „wächst die gescannte Menge unbegrenzt mit Daten?" geprüft. Mig 122 = 5 verifizierte Partial-Indizes (webhook-retry/staffing-expiry×2/demand-notdienst/sla-search-open). Mig 123 = 1 BRIN auf `product_analytics_events(occurred_at)` (einzige event-cardinality Rest-Lücke; BRIN statt btree wegen append-only/heißer-Insert-Pfad). Alle übrigen Sweeps bereits gedeckt durch zweckgebaute Partials (Mig 009/011/013/030/076) oder kunden-cardinality/trivial (organizations/subscriptions/subscription_requests). Jede Lücke direkt gegen die Quell-Migration geprüft (sub-agent-Audit war unzuverlässig). Bewusst NICHT gebaut (deferred): recompute-compliance (Bounding statt Index = Verhaltensänderung), notdienst-escalate requests.priority (Provenienz unbestätigt + heiße Tabelle), webhook-cleanup (Tabelle selbstbegrenzend), sla-scan-Neuindex (Arbeitsset selbstbegrenzend, Write-Amplification > Nutzen). Alle uncommitted (Owner-Gate).
